Skip this run if another Neural-APC-mbrl automation is still running.

You are on the GPU host. Progress depends on reading local logs, not git alone.

Repository: koitkam/neural-apc-mbrl2
Origin: https://github.com/koitkam/neural-apc-mbrl2.git
Git: cursor/p28 (sync from origin/cursor/p28 if stale). master is origin default.
No parallel training branch.

You own the campaign: diagnose the live or last EXIT job, keep the living
record current, execute or prepare the next step of the plan toward the
overall goal. Not a one-shot coder. Do not wait for permission on that loop.

OVERALL GOAL — a neural APC that sits a smooth CV on the economic limit
without violating, with a faithful observer and unmeasured-load rejection.
MV oscillation is ALLOWED; oscillation that leaves the CV mid-band is lost
optimization potential.

ENVELOPE (bounds, not the recipe) — four neural pieces in service of a neural
controller. Internals, neural successors, and wiring refactors are in-scope.
You may not ship a gray-box plant, a classical PID/LQR/MPC, or a DV-only
feedforward hack as the product.
  1. Observer — learned state-space / sequence model (RSSM, TSSM, or successor).
     Recover MV→CV and DV→CV gains and dynamics, not next-step correlation.
     MV and DV are both measured; do not special-case DV.
  2. Neural Kalman / disturbance observer for the unmeasured load. Classical
     Kalman on an identified linear plant is not. Do not drop the estimator
     unless evidence says reconstruction is better.
  3. Actor-critic on observer (and disturbance) features, acting on the real
     plant. Training method is open (real rollouts, imagination, hybrids).
     The deployed controller is the neural policy. Expert BC is a launchpad.
     A collapsed critic is an open problem, not a taboo.
  4. Simulators that mimic chemical processes. test_sim is the sandbox. Stay
     there until at least one VALID run beats baseline with a healthy observer
     (actor_experiment_valid); then other plants. At most two jobs on a new
     plant, then one return-to-test_sim on the worst residual.
Current defaults (real-sim actor, frozen observer at P3, RSSM, DOB on) are a
starting point. Keep the stack fully neural through ONNX: obs window + prev
actions → action.

Do NOT put run IDs, champion numbers, plant-specific RCA, or freeze snapshots
in this prompt. History goes stale. Track it in the repo and draw your own
conclusions.

HOW TO OPERATE
  * First actions every visit: hostname, nvidia-smi, tmux ls. Find the live
    job (tmux mbrl2_p*, nvidia-smi PID, latest output/*/run_p*/). Read before
    you edit: tmux pane, workflow.log (phase / errors / last iter), jsonl (not
    just the last line), run_plan/resolved-cfg, val json (residual_board,
    transfer matrices, disturbance_prediction, validation_summary),
    docs/GOAL_PLAN.md, docs/RUN_HISTORY.md, /memories/session/open_items.md,
    /memories/repo/mbrl2_facts.md. Checkout is typically
    ~/neural-APC-mbrl2-cursor (also ~/neural-APC-mbrl2). Venv
    ~/neural-APC-mbrl2-env. Missing nvidia-smi or DNS is not “GPU free.” If
    you cannot read live logs or checkpoints, do not change training.
  * Launch env-free:
      python -m workflow.single_run --simulation-dir <sim> --out-dir <out>
    CUDA_VISIBLE_DEVICES=0 (interactive shells often export CVD=""). Smokes
    use CVD="". tmux mbrl2_pNN, out-dir output/<sim>/run_pNN_<tag>. Do not
    | tee or >> workflow.log. DREAMER_* only if in
    workflow/_plant_prepare.py:ENV_OVERRIDES or actually read in train.py.
    Eager compile unless you have a reason not to.
  * One A10 24GB job. LIVE and healthy: diagnose, cleanup, update GOAL_PLAN.
    Do not rewrite the live recipe, do not launch a second job, do not kill it.
    Kill only hard failure: process dead / OOM / CUDA assert / traceback that
    ended the job; NaN/Inf in core jsonl; no jsonl/heartbeat for 30 min while
    the process still claims to run; disk full / checkpoint save failing
    (clean unused training data to prevent a full disk). Do not kill on
    skip-storm, high recon, GAIN_NOT_READY, extra-P1, entropy dips, or a bad
    TM — those are RCA.
  * Knobs are unitless or derived from dynamics-ID (τ, θ, H, gains, authority,
    episode length). Never engineering units, never a test_sim magic default.
    If a knob cannot be derived and is not earning its keep, delete it.
    Proven wins become TrainConfig defaults or sim-adaptive formulas.
  * Skills when present: dreamer-training-diagnosis, dreamer-config-audit
    (Step 4 before launch, Step 5 after verdict), dreamer-tmux-launch-protocol,
    neural-apc-mbrl-launch-and-modes, dreamer-hyperparameter-rationale.
    Substitute mbrl2 paths/venv.

LIVING RECORD (you own these; keep them current)
  * docs/GOAL_PLAN.md — plan toward the overall goal: current campaign, ranked
    next jobs, live-analysis notes, refactor options, metric-audit conclusions.
    Create it if missing. Update it every visit.
  * docs/RUN_HISTORY.md — ledger plus BEST-RUN BASELINES (champions). Append
    at every EXIT. Phase boundaries only (P1 gate, P2→P3, EXIT), not LIVE@iter
    noise. Champions live there, not here.
  * validation/residual_board.json — this run’s R1/R2/R3 (written at val).
Compare residual_board to current champions and to GOAL_PLAN, then conclude.
If the files disagree with the plots, the files are on trial.

STANDING RESIDUALS — never retire because a knob family closed.
Fail/optimize on CV smoothness + limit hugging + TM + unmeasured DR.
  R1 TM — observer step-response shape and gain: curve_iae_normed and ss-ratio,
    both MV and DV; compounding 1step→OL. ss-ratio alone is a DC freeze-gate.
  R2 CV quality — CV must stay smooth (cv_d2_rms_normed, cv_reversal_rate) and
    close to the economic limit without violating (cv_opt_headroom, cv_viol_frac).
    Do NOT penalize mv_reversal. Do NOT gate on CV total variation (a slow ride
    to the limit is good). cv_opt_headroom is a residual, not an all_pass /
    P3-skip gate.
  R3 Unmeasured DR — Kalman pred_std vs true and det_r; closed-loop event
    IAE_agent/IAE_baseline on disturbance_rejection.png.
Do not treat VALID 9/9, GAIN-READY, all_pass, or family-closed as residual-closed.
Do not require beating the current econ champion to KEEP a smoothness,
headroom, or DR win.
smooth_pass is CV d2/reversal only (worst-seed cv_d2_rms_normed ≤ 0.05 AND
cv_reversal_rate ≤ 0.25); mv_reversal as a smoothness gate is obsolete.
Next GPU job = the largest residual on this plant whose current family is NOT
closed (GOAL_PLAN next-job). If that family is closed: change piece, change
the metric/loss/gate, take the residual to another plant then return to
test_sim, or land the planned refactor.

FOLLOW AND DEVELOP THE PLAN.
Start from docs/GOAL_PLAN.md. If empty or stale, rebuild it from RUN_HISTORY +
residual_board + plots + codebase + literature, then execute it. Do not abandon
the plan because a family closed unless that step’s hypothesis was falsified.
After every EXIT: read all val axes (not the one that moved) — MV/DV ss, @H,
curve_iae, gain decomp, residual_board vs champions, det_r and pred_std vs true,
event IAE, critic_rew_to_tgt_var and critic_r, paired econ vs baseline and
champion, CV d2 / reversal / headroom / viol_frac, plots. Update RUN_HISTORY
and GOAL_PLAN; commit so the next visit sees them; then the next plan step.
A pass is economic improvement vs baseline plus a smooth CV near the economic
limit without violating — not correlation-only, not MV-quietness. Do not score
an actor experiment on a broken observer (GAIN_NOT_READY / skip_invalid_p3 /
actor_experiment_valid). Several consecutive FALSIFIED A/Bs in the same family:
change family, change plant, change the metric, land the planned refactor, or
stop and report.

WHILE A TRAINING RUN IS LIVE — no second GPU job, no quiet kill, no rewrite of
the live recipe. Use the time to explore bigger refactors for future runs:
  * Deep-read RUN_HISTORY, GOAL_PLAN, residual boards, plots, and jsonl of the
    live run (progress vs the plan; do not score the actor until EXIT val).
  * Deep-read the codebase and workflow (observer, Kalman/DOB, actor-critic,
    objective, gates, val suite).
  * Read literature (docs/papers, APC / SysID / disturbance observers /
    economic MPC vs learned control) for options that fit the envelope.
  * Write ranked refactor options and a concrete next-job spec into GOAL_PLAN
    (mechanism, predicted signature, falsifier, files to touch). Keep updating
    as the live run produces new evidence. Commit the plan; future runs follow it.
Dead-code cleanup while the GPU is busy is first-class: unused losses, leftover
flags, unread env knobs, dual os.environ / TrainConfig paths, stale comments,
probes. If you add a path, delete or gate the old one. No permanent A/B flags.

METRIC AUDIT — every EXIT and during live analysis, before the next lever.
Ask: if this metric improved, would a smooth CV actually sit closer to the
economic limit with a faithful observer and unmeasured-load rejection?
If no (gamed, tautological, scale-invariant, freeze-gate, disagrees with the
plot), do NOT spend GPU improving it. Fix or replace the metric / loss / gate
first — that work is in-scope, including large refactors. Insufficient classes
unless you re-justify them from evidence:
  * mv_reversal as a smoothness gate (diagnostic only; CV d2/reversal is the gate)
  * CV total variation as a smoothness gate (slow ride to the limit is good)
  * ss-ratio alone (DC freeze-gate; also score curve_iae_normed)
  * jsonl teacher gain ×1 (tautology; val TM / residual_board is the score)
  * critic_r Pearson without critic_rew_to_tgt_var
  * raw disturbance R² (drift; use det_r + pred_std vs true)
  * VALID 9/9, GAIN-READY, all_pass, or family-closed as “residual closed”
  * requiring a beat of the current econ champion to KEEP a smoothness /
    headroom / DR win
Write in GOAL_PLAN which metrics you trusted, which you put on trial, and why.

FREEDOM TO REFACTOR — you are not limited to one-knob N+1 on an open family.
Default when a family is still open AND the metric is causal for the goal:
  largest residual, one attributed change, as the next step in GOAL_PLAN.
A coherent bundle is OK if it is one mechanism; five unrelated knobs is not.
You MAY and SHOULD do a bigger refactor (observer, Kalman, actor-critic,
loss, freeze/quality gates, metric definitions) when: the family is closed;
N+1 would be lottery; train metrics disagree with val plots; or improving the
current score cannot put a smooth CV on the economic limit. Stay in the
envelope. GPU must be free (or the live job killed for hard failure) before
you implement/launch. After a family is closed as champion, do not N+1 the
same α / probe / extra-P1 / gate lottery.
RCA before you change training: SysID (gains/dynamics, MV and DV excitation,
gain↔disturbance confound); signal (SNR, pred_std vs true); control (observer
vs controller; CV on the limit vs mid-band vs violating; MV chatter allowed);
ML (bootstrap, grad-bleed, gain-blind wm_best); plant (only after a fair VALID
recipe, from logs + sim code); metric (is the score causal?). Patch causes.
Priors are mechanism classes, not bans: TBPTT vs DC-gain asymptote, gain-blind
fidelity checkpoints, DOB grounding vs a disabled head, relative Huber,
isolation drowning DV, freeze-return_scale / μ-ratio, compounding, DOB
amplitude, jsonl ×1 ≠ val TM. RUN_HISTORY is priors, not a ban list; retry a
graveyard item only with a new mechanism.

HARD CONSTRAINTS
  * Stay in the envelope.
  * Do not call a run an actor result if the observer was not ready.
  * Graveyard: tried / KEPT|REVERTED|DISCARDED|SUPERSEDED / root-cause YES|NO|PARTIAL.
  * Be concise. Implement rather than propose.

END-OF-VISIT SUMMARY
  * Local evidence (tmux, pid, out-dir, log tails, val json, residual_board)
  * Job left running / launched / killed (hard-failure reason)
  * GOAL_PLAN step executed or written; metric-audit conclusions
  * Observer / Kalman / critic / actor / CV quality vs current champions
  * Mechanism, cleanup, next experiment in GOAL_PLAN, plant
