# GOAL_PLAN — neural-APC-mbrl2

Living plan. Update every visit (live analysis and EXIT). Champions live in `docs/RUN_HISTORY.md`. **`RUN_HISTORY` appends at P1-gate / P2→P3 / EXIT only** — not LIVE@iter snapshots.

**Product:** simulator-agnostic neural APC — smooth CV on the economic limit without violating; faithful observer; unmeasured-load rejection. Envelope: learned observer + neural Kalman/DOB + neural actor-critic. No gray-box plant, no PID/LQR/MPC as the product, no DV-only FF.

**Current (2026-09-12T19:14Z):** **P134 `limcert` LIVE P3** pid **658156** sha `c29f426` tmux `mbrl2_p134` `output/test_sim/run_p134_limcert`. Cert **true** — entered P3 (probe **ready=False 0.77@MV**; `[p3-skip]` did **not** fire). Score actor only at EXIT if P3 ran. **P133 `limobs` EXIT `[p3-skip]` INVALID** pid **656936 DEAD** sha `8324e76`. jsonl **0**. Loaded probe **0.76@MV** = P130 val TM. Val is **loaded P130 actor re-eval** (paired **−28.77 vs −181.45**; critic r **+0.476** slope **0.697**; policy unimodal ~−0.5) — generic `[p3-skip]` “expert-BC” text is **wrong**. **Not** a #12 score. Do **not** identity-retry P133 / `skip_invalid_p3=0` / load `wm_last_ok` as the whole ckpt (drops the P130 actor).

Plant this cycle: **return-to-`test_sim`**. **P128 `critsplit` EXIT VALID** pid **624485 DEAD** sha **`bb5667e`** `output/test_sim/run_p128_critsplit`. Env-free #8. EXIT=0 376 jsonl ES `p3_plateau` best **176** det **−1220**. nvidia **0 MiB**. tmux gone. `best.pt` + `final.pt`. Val ckpt **`best.pt`**. `actor_experiment_valid=true`. jsonl NaN/Inf **0**. skip_iter **0**. **opdev=0.0**. `[opscale] held identity` compile=eager `skip_invalid_p3=True` H=**56** sr=4. No `DREAMER_*`. Orig-P1@**88 FAIL 0.75@MV** unlocked; extra-P1@**99 PASS GAIN-READY 0.81@MV** last_ok **99 unlocked** (P41 confound). Wrap#1 recovered; skip-storm 1 cap-deferred. P2→P3@**155 PASS** MTP **1.418** no `[p3-skip]`. rscale FREEZE **6.160** (S-on-hunt). Last P3 clip **0.67** pos_adv **0.026** ratio **0.41** tgt_var **0.001** Pearson **0.99** ent **−0.108**. Calib smoking gun: `raw_min −1035` / p95 **964** `reward_scale` **0.389** (hunt sat) while honest val G is **−180**. **Val (`best.pt`, score actor — VALID):** MV **×0.798 / ×0.815** curve **0.174** (wm −0.255 vs real −0.320; H=56; freeze 0.81→val 0.798 held-ish). DV **×0.978 / ×0.959** curve **0.054**. 1step→OL **×0.789** lever=**compounding**. R2 d2 **0.037 PASS** rev **0.469 FAIL** head **0.439** viol **0.213** `smooth_pass=False` econ=**hi** MV rev **0.842**. Offline orbit (npz, static hi; val JSON predates orbit write) worst **0.294 /τ** (better than P125 **0.644** / P127 **0.671**; still fails parked 0.15). R3 det_r **0.504** pred_std **2.78 vs 1.39** event IAE **24.9** ret_h **0.394** ret_t **0.515**. Paired **−79.89 vs −139.40** **9/9** `beats_baseline=True` on reversal-free `raw_reward`; cum_raw **−36k** (vs hunting P125 **−302k**). `critic_r` **+0.058 FAIL** V **−2.48** vs G **−180** slope **22.88 FAIL** (`critic_pass` = r≥0.3 AND slope ∈ [0.25,4]). reward MTP r **+0.152 FAIL**. Plots agree (TM SS ×0.80/×0.98; dist tracks with lag, not crushed; critic V drowned; policy bimodal ~±0.5–0.75). ✅ **KEEP split** (honest econ: |G| 15318→180; paired beats baseline on reversal-free raw). ❌ **FALSIFIED critic-scale** (Pearson collapsed; V drowned; slope ≫1 after |G| already dropped). Remaining hunt / clip_frac~0.67 is mixed A → **#10 after #8b KEEP**, not an #8 smoothness FALSIFY. Close nothing on #4. **P129 `econcal` EXIT VALID** pid **638866 DEAD** sha launch **`af3f254`** `output/test_sim/run_p129_econcal`. Env-free #8b. EXIT=0 441 jsonl budget `early_stop_reason=null` best **391** det **−545**. nvidia **0 MiB**. tmux gone. `best.pt` + `final.pt`. Val ckpt **`best.pt`**. `actor_experiment_valid=true`. jsonl NaN/Inf **0**. skip_iter **0**. **opdev=0.0**. `[opscale] held identity` **1.171** compile=eager `skip_invalid_p3=True` H=**54** sr=4. No `DREAMER_*`. No `[env-override]`. Calib p95 **305** scale **1.157** `bound_econ_ref` **305** (vs P128 hunt-sat **964** / **0.389**). Orig-P1@**85 FAIL** compounding-edge; extra-P1@**107 PASS GAIN-READY 0.88@MV** last_ok **107 unlocked** `compound_ok` 1step→OL **1.14** (P41 confound). Skip-storm 1 cap-deferred restore **95**; freeze walked **107**. `[phase] 1→2` **915970**. P2→P3@**162 PASS** MTP **1.481** no `[p3-skip]`. first dobg **0.0032** skip **0**. End-P2 leftover **0.115** K **0.178/3.75** std **0.738**. rscale FREEZE **7.007** (hunt S until #10). Last P3 clip **0.39** pos_adv **0.360** ratio **0.84** tgt_var **0.0016** Pearson **0.97** ent **−0.066**. **Val (`best.pt`, score actor — VALID):** MV **×0.754 / ×0.760** curve **0.218** (wm −0.241 vs real −0.320; H=54; freeze 0.88→0.754 dropped). DV **×0.965 / ×0.913** curve **0.072**. 1step→OL **×1.245** OL **×0.749** lever=**compounding**. R2 d2 **0.035 PASS** rev **0.312 FAIL** head **0.406** viol **0.490** orbit **0.537 /τ** `smooth_pass=False` econ=**hi**. R3 det_r **0.169** pred_std **1.45 vs 1.28** event IAE **38.6** ret_h **0.381** ret_t **0.273**. Paired **−104.25 vs −122.43** **9/9** reversal-free. cum_raw **−104k**. `critic_r` **+0.447 PASS** V **−12.39** vs G **−4.52** slope **0.540 PASS** (`critic_pass` TRUE). Plots agree (TM SS ×0.75/×0.96; critic |V|~|G_train|; policy still bimodal; dist tracks with lag, det_r weak; DR hunts through hi). ✅ **KEEP #8b critic-scale** (slope ∈ [0.25,4]; Pearson recovered vs P128 +0.058; honest paired beats baseline). ❌ Do **not** KEEP-econ vs P64 / P128 **−80**. ❌ **Not** a TM KEEP vs P124 ×0.871. Remaining hunt / viol is mixed A → **#10 `actorecon` as P130**. Skip **#8c**. Close nothing on #4. **P130 `actorecon` EXIT VALID** pid **643440 DEAD** sha **`0248876`** `output/test_sim/run_p130_actorecon`. Env-free #10. EXIT=0 461 jsonl ES `p3_plateau` best **261** det **−876**. nvidia **0 MiB**. tmux gone. `best.pt` + `final.pt`. Val ckpt **`best.pt`**. `actor_experiment_valid=true`. jsonl NaN/Inf **0**. skip **0**. **opdev=0.0**. `[opscale] held identity` **1.205** compile=eager `skip_invalid_p3=True` H=**54** sr=4. No `DREAMER_*`. Orig-P1@**88 PASS GAIN-READY 0.84@MV** last_ok **88 unlocked**; extra-P1 **0**. Wrap#1+#2 recovered. P2→P3@**144 PASS** MTP **1.475** no `[p3-skip]`. rscale FREEZE **4.595** (econ S). Last P3 clip **0.062** pos_adv **0.532** ratio **0.997**. **Val (`best.pt`, score actor — VALID):** MV **×0.763 / ×0.798** curve **0.198** (wm −0.244 vs real −0.320; freeze 0.84→0.763). DV **×0.925 / ×0.901** curve **0.106**. 1step→OL **×0.857** lever=**compounding**. R2 d2 **0.033 PASS** rev **0.388 FAIL** head **0.207** viol **0.263** orbit **0.602 /τ** `smooth_pass=False` econ=**hi**. R3 det_r **0.215** pred_std **1.61 vs 1.39** event IAE **15.5** ret_h **0.136** ret_t **0.182**. Paired **−29.67 vs −134.76** **9/9** reversal-free. cum_raw **−29k**. `critic_r` **+0.353 PASS** V **−7.15** vs G **−3.57** slope **0.608 PASS**. Plots agree (TM SS ×0.76/×0.92; critic |V|~|G_train|; policy unimodal ~−0.75; dist tracks with lag; DR seed 10004 hunts through hi). ✅ **KEEP #10 as mixed-A / honest econ**. ❌ **FALSIFIED residual-closed smoothness** (rev **0.388** worse than P125 **0.304**; orbit **0.602** ≫ 0.15). **P131 `limcost` EXIT `[p3-skip]` INVALID** pid **647479 DEAD** sha **`8debbe0`** `output/test_sim/run_p131_limcost`. Env-free #12. EXIT=0 167 jsonl ES `p3_skipped_invalid_observer`. nvidia **0 MiB**. tmux gone. No `best.pt`. Val ckpt **`final.pt`** expert-BC. `actor_experiment_valid=false`. jsonl NaN/Inf **0**. skip **0**. **opdev=0.0**. `[opscale] held identity` **1.133** compile=eager `skip_invalid_p3=True` H=**56** sr=4. No `DREAMER_*`. Orig-P1@**88 FAIL 0.69@DV**; extra-P1@**101 FAIL 0.70@DV**; cap@**111 CAPPED** freeze last_ok **108 unlocked** **0.74@DV**. `[phase] 1→2` **958170**. P2→P3@**167 PASS** MTP **1.511** then `[p3-skip]`. End-P2 leftover **0.286** (`|d_slow|/|d|` 0.082/0.287) K **0.073/1.54** std **0.786**. **Val (do not score actor / #12):** MV **×0.765 / ×0.795** curve **0.203** (wm −0.245 vs real −0.32; freeze 0.74→0.765 held-ish). DV **×0.909 / ×0.940** curve **0.112**. 1step→OL **×0.864** OL **×0.724** lever=**free_bits**. R2 d2 **0.035 PASS** rev **0.358 FAIL** orbit **0.486** head **0.216** viol **0.367** `smooth_pass=False` econ=**hi**. R3 det_r **0.078** pred_std **2.19 vs 1.29** event IAE **24.1** ret_h **0.250** ret_t **0.134**. Paired **−43.50 vs −132.67** **9/9** expert-BC. `critic_r=nan` V **0**. Plots agree (TM SS ×0.77/×0.91; dist overshoots; critic V=0; policy expert-BC ~−0.9; DR seed 10004 hunts through hi). ✅ KEEP `skip_invalid_p3`. ❌ **Not** a #12 FALSIFY (P126-class lottery not-READY). **P132 `limcost` EXIT `[p3-skip]` INVALID** pid **652156 DEAD** sha `a89c321` `output/test_sim/run_p132_limcost`. Identity #12. EXIT=0 166 jsonl ES `p3_skipped_invalid_observer`. nvidia **0 MiB**. tmux gone. No `best.pt`. Val ckpt **`final.pt`** expert-BC. `actor_experiment_valid=false`. jsonl NaN/Inf **0**. skip **0**. **opdev=0.0**. Orig-P1@**87 FAIL 0.37@MV** noisy; extra-P1@**98 FAIL 0.66@MV**; cap@**110 CAPPED** freeze last_ok **110 unlocked** **0.63@MV**. P2→P3@**166 PASS** MTP **1.483** then `[p3-skip]`. **Val (do not score actor / #12):** MV **×0.515 / ×0.668** curve **0.373** (wm −0.165 vs real −0.320; H=56; freeze 0.63→**0.515 dropped**). DV **×0.955 / ×0.932** curve **0.084**. 1step→OL **×0.705** lever=**compounding**. Lineage `wm_gain_pass=True` rel_err **0.485** ≠ GAIN-READY. R2 d2 **0.038 PASS** rev **0.291 FAIL** orbit **0.813 /τ** head **0.161** viol **0.344** `smooth_pass=False` econ=**hi**. R3 det_r **0.194** pred_std **1.86 vs 1.36** event IAE **19.9** ret_h **0.096** ret_t **0.162**. Paired **−80.66 vs −135.11** **9/9** expert-BC; cum_raw **−61k**. `critic_r=nan` V **0** vs G **−2.23**. Policy mean **−0.35** (expert-BC). ✅ KEEP `skip_invalid_p3`. ❌ **Not** a #12 FALSIFY (second full-recipe lottery miss). ❌ **Not** a TM KEEP (×0.515 worse than P131 ×0.765). Extra-P1 lottery **closed**. Do **not** third identity / jump to #13 / special-case DV. Next GPU **#12b P133 `limobs`**. Skip **#8c**. Skip **#9**. **P127 `rev1b` EXIT VALID** pid **618741 DEAD** sha **`830b1b1`**. 441 jsonl budget best **246** det **−842.8**. Extra-P1 freeze **0.82@MV**. Val MV **×0.764** rev **0.349 FAIL** paired **−285 vs −441** 9/9 hunting `raw_reward`. slope **289**. ❌ **#4b FALSIFIED smoothness**. Close **#4 family**. **P126 `rev1` EXIT `[p3-skip]` INVALID**. **P125 `cvrev` EXIT VALID** KEEP hug/DR vs P124; last VALID smoothness baseline rev **0.304**. **P124 `medhuber` EXIT VALID** KEEP #1c freeze/TM; FALSIFIED actor-econ. Extra-P1 lottery **closed**. **P120 `gop` PARKED.** Skip **#1e**. Skip **#4c**. Skip **#7** (P128 lever=**compounding**).

## Residual board vs champions (from `validation/residual_board.json`)

| Axis | Score we trust | Champion | P125 VALID | P127 VALID | P128 VALID (`best.pt`) | Status |
|---|---|---|---|---|---|---|
| R1 TM MV ss/@H / curve | val TM + `curve_iae_normed` | P26 ×0.973 / @H ×0.880 | **×0.754 / ×0.795** curve **0.204**. freeze 0.81→0.754 | **×0.764 / ×0.781** curve **0.207**. freeze extra-P1 0.82→0.764 | **×0.798 / ×0.815** curve **0.174**. wm −0.255 vs real −0.320. H=56. freeze 0.81→0.798. lever=**compounding**. P124 was **×0.871 / ×0.906** | OPEN vs P26; **not** a TM KEEP vs P124 |
| R1 TM DV ss/@H / curve | same | P64 ×0.893 / @H ×0.962 | **×0.998 / ×1.016** curve **0.051** | **×0.990 / ×0.908** curve **0.052** | **×0.978 / ×0.959** curve **0.054** | Do **not** swap P64 |
| R1 compounding | 1step→OL | P64 ×0.85 | **×0.762** lever=**compounding** | **×0.926** lever=**free_bits** | **×0.789** OL **×0.808** post→1step **×1.017** real→post **×1.003** lever=**compounding** | test_sim family **closed** as champ; skip **#7** |
| R2 CV smoothness | worst-seed d2≤0.05 AND rev≤0.25 AND orbit≤0.15 | — | d2 **0.033** PASS; rev **0.304** FAIL. orbit **0.644 /τ** | d2 **0.034** PASS; rev **0.349 FAIL**. orbit **0.671 /τ** | d2 **0.037 PASS**; rev **0.469 FAIL**. `smooth_pass=False`. MV rev **0.842**. orbit npz **0.294 /τ** | **#4 family closed**. **P130** d2 **0.033 PASS** rev **0.388 FAIL** orbit **0.602 /τ**. ❌ **#10 FALSIFIED smoothness**. Last VALID smoothness baseline P125 **0.304**. **#11 orbit in `smooth_pass` landed** |
| R2 headroom / viol | `cv_opt_headroom`, `cv_viol_frac` | — | econ=**hi**. head **0.202** / viol **0.347** | econ=**hi**. head **0.219** / viol **0.271** | econ=**hi**. head **0.439** / viol **0.213**. Plots near hi with remaining hunt (not mid-band) | KEEP hug vs P124 **0.762/0.583**. Skip **#4c**. P128 head worse than P125 is mixed-A leftover, not an #8 FALSIFY |
| R3 Kalman | `det_r` + pred_std vs true | P26 det_r 0.68 | **0.558** / **1.54 vs 1.38** | **0.514** / **1.09 vs 1.35** | **0.504** / **2.78 vs 1.39** | KEEP amp vs P124 crush **3.77**; do **not** swap P26 |
| R3 return-to-limit | `cv_return_headroom` / `cv_return_time_frac` | — | ret_h **0.109** ret_t **0.188** | ret_h **0.163** ret_t **0.242** | ret_h **0.394** ret_t **0.515** | KEEP vs P124 late-window miss **0.962/0.761** |
| Actor econ | paired vs baseline **and** P64; VALID only | P64 −4.54 vs −94 | **−283.78 vs −420.87** **9/9** hunting. `critic_r` **−0.014**. V **−90** vs G **−18490** slope **−15** | **−284.62 vs −440.80** **9/9** hunting. `critic_r` **+0.318**. V **−81** vs G **−15318** slope **289** | **−79.89 vs −139.40** **9/9** reversal-free. cum_raw **−36k**. `critic_r` **+0.058**. V **−2.48** vs G **−180** slope **22.88**. `critic_pass` **FAIL** | **P130** **−29.67 vs −134.76** **9/9**; `critic_r` **+0.353** slope **0.608** `critic_pass` **TRUE**. ✅ KEEP #10 mixed-A econ. ❌ Do **not** KEEP-econ vs P64. P129 **−104 vs −122** KEEP #8b |

P128 board (`best.pt`, VALID; plots agree): MV **×0.798 / ×0.815** curve **0.174** (wm −0.255 vs real −0.320; H=**56**). DV **×0.978 / ×0.959** curve **0.054**. Freeze extra-P1 0.81→val **×0.798 held-ish**. Decomp real→post **×1.003** post→1step **×1.017** 1step→OL **×0.789** OL **×0.808** lever=**compounding**. det_r **0.504** pred_std **2.78 vs 1.39** event IAE **24.9** ret_h **0.394** ret_t **0.515**. R2 d2 **0.037 PASS** rev **0.469 FAIL** head **0.439** viol **0.213** `smooth_pass=False` econ=**hi**. `critic_r` **+0.058 FAIL** V **−2.48** vs G **−180** MAE **178.5** **slope_g_on_v 22.88** / nmae **0.82** (P127 slope **289** / |G| **−15318**; P128 |G| dropped to honest-econ **−180** — critic-scale still FAIL). Paired **−79.89 vs −139.40** **9/9** reversal-free. TM png agrees (MV SS ×0.80; DV ×0.98). Dist png tracks steps with lag (KEEP amp vs P124 crush). `wm_diagnostics.png` critic V drowned; policy bimodal ~±0.5–0.75. `summary.png` cum_raw **−36k** (no hunt-clip spike train). `validation_summary.n_seeds=8` vs 9 unique seeds / board `n_scripted_pairs=9` — residual_board is the score. Skip **#4c** (not mid-band). Close **#4 family**. P129 EXIT KEEP critic-scale → **#10**.

P127 board (`best.pt`, VALID; plots agree): MV **×0.764 / ×0.781** curve **0.207** (wm −0.244 vs real −0.320; H=**55**). DV **×0.990 / ×0.908** curve **0.052**. Freeze extra-P1 0.82→val **×0.764**. Decomp real→post **×0.938** post→1step **×0.817** 1step→OL **×0.926** OL **×0.683** lever=**free_bits**. Lineage HEALTHY rel_err MV **0.236** / DV **0.010**. det_r **0.514** pred_std **1.09 vs 1.35** event IAE **15.3** ret_h **0.163** ret_t **0.242**. R2 d2 **0.034 PASS** rev **0.349 FAIL** head **0.219** viol **0.271** `smooth_pass=False` econ=**hi**. `critic_r` **+0.318** V **−81** vs G **−15318** MAE **15238** **slope_g_on_v 289** / nmae **4.24** (P125 slope **−15** inverted; P124 V **−7** vs G **−1542** slope **−127** — compression predates #4 hunt). Paired **−284.62 vs −440.80** **9/9** — hunting in `raw_reward` (on trial). TM png agrees (MV SS ×0.76; DV ×0.99). Dist png tracks steps with lag (KEEP amp vs P124 crush). DR png seed 10004: CV hunts through hi; MV chatter allowed; cum_raw ≈ **−267k**. `summary.png` cum_raw **−308k**. `validation_summary.n_seeds=8` vs 9 unique seeds / board `n_scripted_pairs=9` — residual_board is the score. Skip **#4c** (not mid-band). Close **#4 family**. P128 EXIT scored above; remaining hunt is **#10** after **#8b KEEP**.

P130 board (`best.pt`, VALID; plots agree): MV **×0.763 / ×0.798** curve **0.198** (wm −0.244 vs real −0.320; H=**54**). DV **×0.925 / ×0.901** curve **0.106**. Freeze orig-P1 0.84→val **×0.763 dropped**. Decomp real→post **×0.961** post→1step **×0.931** 1step→OL **×0.857** OL **×0.743** lever=**compounding**. det_r **0.215** pred_std **1.61 vs 1.39** event IAE **15.5** ret_h **0.136** ret_t **0.182**. R2 d2 **0.033 PASS** rev **0.388 FAIL** head **0.207** viol **0.263** orbit **0.602 /τ** `smooth_pass=False` econ=**hi**. `critic_r` **+0.353 PASS** V **−7.15** vs G **−3.57** MAE **5.18** **slope_g_on_v 0.608** / nmae **1.12** (`critic_pass` TRUE). Paired **−29.67 vs −134.76** **9/9** reversal-free. TM png agrees (MV SS ×0.76; DV ×0.92). Dist png tracks steps with lag (det_r **0.215**; amp **not** crushed vs P124 **3.77**). `wm_diagnostics.png` critic |V|~|G_train|; policy **unimodal ~−0.75** (not P129 bimodal). `summary.png` cum_raw **−29k**. DR seed 10004: head **0.069** viol **0.435** orbit **0.430** — hunts through hi, **not** mid-band. `validation_summary.n_seeds=8` vs board `n_scripted_pairs=9` — residual_board is the score. Skip **#8c**. Skip **#4c**. Skip **#7**. Skip **#9**. P131 EXIT `[p3-skip]` INVALID — **do not** score #12. **P132 EXIT `[p3-skip]` INVALID** — CAPPED **0.63@MV** last_ok **110**; val MV **×0.515** expert-BC; **do not** score actor. **P133 EXIT `[p3-skip]` INVALID** — loaded probe **0.76@MV**; val MV **×0.780 / ×0.810** is P130 actor re-eval (paired **−28.77**; rev **0.408** orbit **0.546**); **do not** score #12. Remaining hunt still needs a **VALID** #12 P3: next **#12c P134 `limcert`**.

P129 board (`best.pt`, VALID; plots agree): MV **×0.754 / ×0.760** curve **0.218** (wm −0.241 vs real −0.320; H=**54**). DV **×0.965 / ×0.913** curve **0.072**. Freeze extra-P1 0.88→val **×0.754 dropped**. Decomp real→post **×0.903** post→1step **×0.901** 1step→OL **×1.245** OL **×0.749** lever=**compounding**. det_r **0.169** pred_std **1.45 vs 1.28** event IAE **38.6** ret_h **0.381** ret_t **0.273**. R2 d2 **0.035 PASS** rev **0.312 FAIL** head **0.406** viol **0.490** orbit **0.537 /τ** `smooth_pass=False` econ=**hi**. `critic_r` **+0.447 PASS** V **−12.39** vs G **−4.52** MAE **8.52** **slope_g_on_v 0.540** / nmae **1.64** (`critic_pass` TRUE; P128 slope **22.88** / r **+0.058**). Paired **−104.25 vs −122.43** **9/9** reversal-free. TM png agrees (MV SS ×0.75; DV ×0.96). Dist png tracks steps with lag (det_r **0.169** weaker than P128 **0.504**; amp **not** crushed vs P124 **3.77**). `wm_diagnostics.png` critic |V|~|G_train|; policy still bimodal ~−0.75/+0.75. `summary.png` cum_raw **−104k** (viol leftover, not hunt-clip spike train). DR png seed 10004: CV hunts through hi (not mid-band); MV chatter allowed. `validation_summary.n_seeds=8` vs board `n_scripted_pairs=9` — residual_board is the score. Skip **#8c**. Skip **#4c**. Skip **#7**. Next **#10**.

P126 INVALID board (`final.pt` expert-BC; do **not** score actor / #4b): MV **×0.785 / ×0.814** curve **0.177** (wm −0.251 vs real −0.320; H=**53**). DV **×0.915 / ×0.939** curve **0.110**. Freeze 0.74–0.76→val **×0.785 held**. Decomp real→post **×0.947** post→1step **×0.983** 1step→OL **×0.789** OL **×0.741** lever=**compounding**. DV decomp faithful. Lineage `wm_gain_pass` True rel_err **0.215** ≠ GAIN-READY. det_r **0.380** pred_std **2.58 vs 1.37** event IAE **27.8** ret_h **0.063** ret_t **0.203**. R2 d2 **0.038 PASS** rev **0.329 FAIL** head **0.162** viol **0.376** `smooth_pass=False` econ=**hi**. `critic_r=nan` (P3 skipped; V **0** vs G **−16737**). Paired **−284 vs −435** **8/9** expert-BC (seed 10003 LOSE; `beats_baseline_pass` is mean). TM png agrees (MV SS ×0.79; DV ×0.91). Dist png overshoots (amp not crushed). DR png seed 10004: expert-BC hunts through hi; MV chatter allowed. `summary.png` cum_raw **−307k**. `validation_summary.n_seeds=8` vs 9 unique seeds / board `n_scripted_pairs=9` — residual_board is the score. P124 board (prior VALID): MV **×0.871 / ×0.906** curve **0.112** DV **×1.073** 1step→OL **×1.234** lever=**free_bits** det_r **0.334** paired **−154 vs −153** **4/9** rev **0.391** head **0.762** viol **0.583**. Do **not** treat P123/P126 lineage `wm_gain_pass=True` as GAIN-READY. Plot `wm_transfer_matrix_combined.png` (P125) agrees (MV SS wm −0.241 / real −0.32 ×0.75; DV ×1.00). `wm_disturbance_prediction.png` P125 det_r **0.56** tracks steps with lag (KEEP amp vs P124 crush). P125 `wm_diagnostics.png` critic V mean **−90** vs G mean **−18490** (Pearson **−0.014**). `summary.png` P125 cum_raw mean **−302k** is the hunting spike-train (on trial). DR pngs: CV near hi with hunting; MV chatter present (allowed).

## Metric audit (trusted vs on trial)

**Trusted:** **P130 EXIT val TM / residual_board / plots agree** (MV SS wm −0.244 / real −0.32 **×0.76**; DV **×0.92**; critic V **−7.15** vs G **−3.57** slope **0.608**; r **+0.353**; paired **−29.67 vs −134.76** reversal-free; cum_raw **−29k**; policy unimodal ~−0.75). **P130 orig-P1 5-level 0.84@MV / 0.88@DV** (unlocked last_ok **88**, recon 4.5×, `not_noisy`, `compound_ok` 1step→OL **0.92**) is the freeze score — jsonl ×1 is not. **#11 `cv_limit_orbit_rate` in `smooth_pass`** (landed; P130 orbit **0.602** FAILS 0.15). **Event IAE (`iae_window_normed` 5τ) + orbit + rev** — not tracking `iae_normed_mean`. **P129 EXIT val TM / residual_board / plots agree** (MV SS wm −0.241 / real −0.32 **×0.75**; DV **×0.96**; critic V **−12.39** vs G **−4.52** slope **0.540**; r **+0.447**; paired **−104 vs −122** reversal-free; cum_raw **−104k**).  (MV SS wm −0.255 / real −0.32 **×0.80**; DV **×0.98**; critic V **−2.48** vs G **−180** slope **22.88**; paired **−80 vs −139** reversal-free; cum_raw **−36k**). val TM ss/@H/`curve_iae_normed` (MV and DV); gain decomp; `det_r` + pred_std vs true; paired econ vs baseline **and** champion when freeze GAIN-READY and P3 ran — **honest reversal-free `raw_reward` after #8**; **CV d2/reversal/orbit** (`cv_d2_rms_normed` + `cv_reversal_rate` + `cv_limit_orbit_rate` — this **is** `smooth_pass` as of #11); `cv_opt_headroom` / `cv_viol_frac` with econ side = **sign(SysID MV→CV gain)** (G<0 hug hi / G>0 hug lo — not `cv_side_scale`); DR `cv_return_headroom` / `cv_return_time_frac` on that same bound; **critic `slope_g_on_v` ~1** and **|V mean| vs |G mean| same order** (`wm_diagnostics.critic_calib`; `residual_board.diagnostic`); **`critic_pass` = r≥0.3 AND slope ∈ [0.25, 4]** (P127 r=+0.318/slope 289 is **FAIL**; does not change `actor_experiment_valid`). Skip-storm 5-level TM is freeze/RCA, not a champ score. **P127 orig-P1 5-level 0.69@MV** (unlocked last_ok, recon 3.2×, `not_noisy`, tight draws) is the P1-gate score — jsonl ×1 is not. **P127 EXIT val TM / residual_board / plots agree** (MV SS wm −0.244 / real −0.32 **×0.76**; DV **×0.99**; critic V **−81** vs G **−15318** slope **289**; DR hunts through hi).

**On trial / do not GPU-optimize:** **P133 generic `[p3-skip]` “expert-BC val”** (**FALSIFIED** — `final.pt` is loaded P130 actor: paired **−28.77** / critic r **+0.476** / policy unimodal ~−0.5, not P131 expert ~−0.9); **P1 `[0.80,1.30]` as a freeze-transfer gate on actor-best** (**FALSIFIED** P133 — probe **0.76@MV** = known val drop 0.84→0.763; tautological); **P133 det_r −0.043 / orbit 0.546 / rev 0.408 as a #12 actor score** (INVALID; P3 never ran); **P132@59 ~3× vs P131@58 12× as an orig-P1 predictor** (**FALSIFIED this visit** — P132 orig-P1 still FAIL **0.37@MV** noisy); **P132 extra-P1 / cap as orig-P1 READY-maker** (lottery **closed** — extra-P1@98 **0.66@MV**, cap@110 **0.63@MV**); **P132 P3-floor clamp `p1_ext_cap` 796080→185954 as an extra-P1 N+1 lever** (truncator, not a recipe; do **not** GPU-optimize `phase3_min_frac`); **P132 orig-P1 jsonl @87 MV ×0.998** while 5-level **0.37@MV** (teacher pin ≠ gate); **extra-P1 as orig-P1 READY-maker** (lottery **closed**); **P132 P1 jsonl teacher ×1 / @22 MV ×1.237 / recon vs P131@30 as an orig-P1 predictor** (identity lottery; 5-level freeze is the score); **jsonl `gpu_mem_mb` ~327 vs reserved 13546 / nvidia 14115** (logger allocated, not a leak); **P132 wm_best@40 gain_fid 0.871 as a freeze/TM**; **distillation mixed-sign median ≈0 as a guaranteed `'lo'`** (`_cv_econ_side` may pin one global `'hi'` when `|median|>1e-12`; `cv_row` only in the near-zero fallback); **P131 EXIT expert-BC paired −43 / orbit 0.486 / det_r 0.078 / rev 0.358 as a #12 actor score** (INVALID `[p3-skip]`; freeze not READY); **P131 lineage `wm_gain_pass=True` ≠ GAIN-READY**; **P131 DR png title IAE=0.00** (tracking tautology; event IAE **24.1** is the score); **P131 cap 0.75@MV / freeze re-probe 0.74@DV as an extra-P1 READY-maker** (CAPPED not READY; family **closed**); **P131 jsonl @88 MV ×1.004 / DV ×0.993** (teacher pin ≠ 5-level **0.69@DV**); **persist @101 3.11** (P73 persist-as-compounding **closed** — not a TM / not persist-coef N+1); **P131 nvidia 14115→4104 MiB** (rest-IC graph released at g-freeze; same class as P130 **14027→4167**; not a leak / not a kill); **P131@72 recon 18.86× as wrap** (5× last_ok pause, locked=False; recovered @74). **P131 P1 jsonl teacher ×1 / wm_best gain_fid** (not a TM / not an actor score). **scalar median MV→CV G as MIMO y_econ** (distillation 12 mixed-sign pairs ≈0 → false `'lo'`; test_sim −0.30 → hi is fine). **P130 residual_board.json `smooth_pass_rule` 2-arg** (historical EXIT; HEAD writer is 3-arg — do not rewrite that JSON). **P130 DR png seed 10004 title IAE=0.00** (tracking IAE; `targets_enabled=False` ⇒ identically 0 on all 9 seeds — tautological; event IAE **7.79** / board **15.5** is the score). **Val-only fix already on HEAD** (`_dr_scripted_title` event_IAE+orbit+rev; P130 pngs stale until next val). **#12 entry-cost mean ~0.017 vs dwell ~0.226 on P130 csv** (orbit↔entry **0.999** / orbit↔dwell **0.15**; do **not** α-scale entry — #13 dwell is occupancy if EXIT FALSIFIES); **P130 EXIT rev 0.388 as KEEP-smoothness vs P125 0.304** (worse; orbit 0.602 vs P125 0.644 is noise, not a KEEP); **P130 paired −30 as KEEP-econ vs P64** (honest vs baseline only); **P130 det_r 0.215 as a P26 swap**; **P130 jsonl last clip/pos_adv as a smoothness score** (train canaries; val orbit/rev is the score); **P130 rscale FREEZE 4.595 as a smoothness score** (econ S — KEEP as #10 mechanism); **GPU-optimize `CV_LIMIT_ORBIT_MAX` 0.15** (gate, not a train knob); **P130 jsonl @88 MV ×1.009 / DV ×0.994** (teacher pin ≠ 5-level **0.84@MV**); **P130 P2 dobg/K/leftover @117** (Kalman-on / g frozen — score at EXIT val); **P130 nvidia drop 14027→4167 MiB** (rest-IC graph released at freeze; not a leak / not a kill); **jsonl `gpu_mem_mb` ~404 / reserved **3598** vs nvidia **4167**** (logger allocated); **wrap#1 last_ok 21 / wrap#2 last_ok 68** recovered — not a TM / not a kill; **P130 LIVE recon/gmatch / wm_best@60** (gain-blind; do not score actor); **P130 vs P129 SysID lottery** (τ_fast **45** vs **47**, Huber MV **−2.62** vs **−2.43**, σ_auth **0.212** vs **0.226**) — env-free identity, not a second knob; **P129 paired −104 as KEEP-econ vs P128 −80 / P64** (honest vs baseline only); **P129 det_r 0.169 as a P26 swap** (amp 1.45 vs 1.28 KEEP vs P128 crush 2.78; R3 below #10); **P129 viol 0.490 as mid-band** (plots hunt through hi); **P129 rscale FREEZE 7.007** (hunt S until #10); **P129 extra-P1 freeze 0.88@MV as orig-P1 KEEP** (P41 lottery; orig-P1 was compounding-edge); **skip-storm restore iter 95 as freeze** (last_ok walked **107 unlocked**); **P2 dobg/K / leftover** (Kalman-on / g frozen — score at EXIT val); **P129 orig-P1 1step→OL printed 0.80 with `compound_ok=False`** (`.2f` of r<0.80; do **not** treat as in-band / loosen `gain_ready_lo`); **P129 jsonl @85 MV ×0.995 / @107 ×0.983** while 5-level **0.86@DV / 0.88@MV** (teacher pin ≠ gate); **retargeting adaptive clip onto bound-path `twohot_bound_active_bins`** (P129-class occupancy **~10–11** would trip `<10` — second mechanism); **P129 calib `twohot_active_bins=43` / `twohot_unhealthy=false`** (scale-path `arr*scale`; unused while bound is on; adaptive clip still keys off these — do **not** retarget that gate while P130 is live). Critic occupancy is bound-path ±B: p95 **305** → **±3**, `symlog(3)≈1.39` of Hafner `[-20,20]` (one-sided **~10–11** bins, not a two-sided ~18). HEAD `twohot_bound_active_bins` is the score for the **next** calib JSON (P129 predates it). Do **not** GPU-optimize scale-path bin-count / raise B; **P128 Pearson +0.058** is scale-invariant so a linear bound map does **not** explain the collapse (hunt-ref 964 barely clips: G_raw std **218** → bound std **~0.68**); **P128 calib `raw_min −1035` / p95 **964** / `reward_scale` **0.389** as twohot support** (hunt sat; `bound_training_reward` bypasses scale — shared hunt `reward_clip` as bound ref drowned milder econ; #8b fixes this); **P128 EXIT slope **22.88** on raw G **−180** vs V **−2.48**** (units mismatch while bound is on; slope gate after #8b is matched `reward_econ_train`); **P128 EXIT clip_frac **0.67** / pos_adv **0.026**** (mixed A; do **not** N+1 clipcoef); **P128 EXIT rev **0.469** as an #8 FALSIFY** (#4 closed; remaining hunt is #10 after #8b KEEP); **P128 paired −80 as KEEP-econ vs P64** (honest vs baseline only); **P128 mid-P3 `actor_ratio_clip_frac` ~0.76** / `actor_pos_adv_frac` ~0.05 / `actor_ratio_mean` ~0.33 vs P127 same-iter **~0.08 / ~0.45 / ~1.0** (mixed-A signature; do **not** N+1 clipcoef / `p3_mu_ratio_clip`); **P128 `critic_rew_to_tgt_var` 0.062@156→~0.002** with Pearson **~0.98** (twohot on econ λ; P127 EXIT **0.008** still VALID — `<0.015` is **not** a kill); **P128 rscale FREEZE 6.160** / P3 jsonl `critic_pred_target_r` (train canaries; S-on-hunt is #8 by design, not EXIT slope); **jsonl `gpu_mem_mb` ~456** vs nvidia **~2891 MiB** (logger allocated, not a leak); **P128 extra-P1 0.81@MV** as an orig-P1 KEEP vs P125 **0.81@MV** (P41 confound; orig-P1 was **0.75@MV**); **P128 orig-P1 jsonl @88 MV ×1.000** while 5-level **0.75@MV**; **1τ-window ≥2 sign-change frac as an orbit gate** (P125 seed 10004 **0.049** while plot hunts / crossings-per-τ **0.600** — **FALSIFIED** this visit; landed `_cv_limit_orbit_rate`); **P127 paired 9/9 hunting-in-`raw_reward`**; **P127 val `critic_r` +0.318 with slope_g_on_v 289** (Pearson without |V|~|G| / slope~1 is **not** residual-closed; **`critic_pass` Pearson-only SUPERSEDED**); **P128 jsonl teacher MV ×0.992 @87** / wrap@68 MV **×0.269** (pin ≠ 5-level; wrap **did** fire); **P127 P3 last jsonl** rtgt **0.0082** Pearson **0.94** / rscale **6.931** — train canaries, not EXIT val; **P127 extra-P1 PASS 0.82@MV** as an orig-P1 KEEP vs P125 **0.81@MV** (P41 confound; lottery closed as READY-maker); **P127 rscale FREEZE 6.931** is hunt-spread S — KEEP as #8 actor-S, **not** a smoothness score; **P127 orig-P1 jsonl @86 MV ×1.007** while 5-level **0.69@MV** (teacher pin ≠ gate; the 5-level is the score); **P127 wrap#1 jsonl** @49 recon **0.4169 = 126×** MV **×0.497** DV **×0.438** then @50 MV **×0.934** while still locked — teacher pin ≠ 5-level (same tautology as P125@16 / P126@24/@81); **P127 wrap#2** lock@92 **23.4×** / jsonl MV **×0.972** — recovered@94, not a TM; jsonl teacher ×1 vs SysID median (**P124 EXIT @88 jsonl MV ×0.986 DV ×1.051** while orig-P1 LIVE **0.89@MV / 1.00@DV**; wrap-era jsonl ×1 on 61× recon was the same tautology — pin ≠ 5-level; val TM is the score); **P125@16 jsonl recon 0.165 / gmatch 0.913 / MV ×0.308 then @17 recovered**; **P125 wrap#1 @32 recon 0.222 / gmatch 0.124 / DV ×0.508 then unlocked @33 / last_ok walked 31→46** (lock wrote disk iter **31**; RAM last_ok **46**); **P125 wrap#2 @73 recon 0.0868 = 35× best 0.00245 / unlocked@74 recon 0.0186 = 7.6× / last_ok stayed 72 until ≤5× @75 then walked 75→76** (disk lock **23:56 iter 72**; jsonl lok **76**; DV jsonl did **not** spike like wrap#1 — still not a 5-level; do **not** score TM); **val `economic_score` = mean(`info['raw_reward']`)** — comments say unshaped economic; code is unshaped **but includes** `cv_reversal_penalty` (P125) / previously `mv_reversal_penalty` (P124). Paired econ that now contains the #4 term is **gamed** for a smoothness KEEP. Trust R2 d2/rev + head/viol for #4; fix `raw_reward` on **#8 land** by skipping reversal subtracts then saturating both (P125 EXIT val runs in pid **608718** / old code — keep comparable to P124); **workflow `persist` = `gain_match_ol_persist_rel`** (P73 persist-as-compounding **closed**; P125@76 persist_rel **0.47** is early-P1, not a persist-coef N+1); **`wm_last_ok.pt` mtime vs jsonl `p1_last_ok_iter`** (P125 pid sha `8b25553` lock-only write; disk **iter 72**; jsonl lok **76**; RAM was freeze SoT. **P126 pid `df7d54f` persist-on-walk CONFIRMED** — `[wm-last-ok] wrote` walk @10/@20/@50/@60/@70/@80; wrap#2 lock last_ok **80**); jsonl `gain_match_mv_ratio` vs local G (teacher pin ≠ GAIN-READY); jsonl `gpu_mem_mb` **~328** vs nvidia-smi **13853 MiB** (logger allocated, not a leak); **wm-fidelity `gain_fid` during wrap** (P125@40 conv=**0.00** gain_fid **0.784**; @60 conv=0.50 gain_fid **0.873** `wm_best` **@60**; @70 conv=0.25 gain_fid **0.901** — still gain-blind; `restore_p2=False` KEEP); ss-ratio alone; lineage `wm_gain_pass` / `wm_gain_healthy` on GAIN_NOT_READY freezes; **mv_reversal as a smoothness gate** (diagnostic only; P124 val MV rev **0.231** while CV rev **0.391 FAIL**; leftover `mv_reversal_rate_max=0.5` **deleted** from `control_quality_gates`); **pre-P125 train `mv_reversal_penalty`** — P116/P124 FAIL is CV hunting with MV already quiet-ish; **`relu(-dCV_t·dCV_prev)` as the #4 retarget** (CPU fixture hunting rev **0.331 FAIL** / d2 **0.005 PASS** / amp-relu **6e-6** vs bang-bang **0.25**; sticky-tanh **0.331** matches val — **landed P125**); **`reward_shaping_coef` / Φ_safe as a mid-band GPU lever** (Ng 1999 potential; Φ_safe is **flat** in the interior, `shaping_safe_margin_frac=0.25`; Φ_econ is margin-gated — policy-invariant, does **not** move the hug optimum; do **not** ablate shaping before #4c/#8 if EXIT is mid-band). CV total variation as a smoothness gate; critic_r without `critic_rew_to_tgt_var`; raw dist R²; event IAE without return-to-limit; VALID 9/9 / GAIN-READY / all_pass / family-closed as “residual closed”; beating P64 to KEEP a smoothness/headroom/DR win; **`cv_side_scale` as the economic riding bound** (it is violation urgency; P116 “headroom 0.82 / never returns” was hugging **hi** scored against **lo**); **`wm_op_scale_dev~0` as a GPU target** (P123 **opdev=0.0 all 95 iters** is **hold-confirmation**, not a TM score; P122/P120/P121 **~0.83–0.91** was the trained-MLP leftover on a linear plant; P124 last Linear zeros ⇒ `scale=1+tanh(0)≡1` already); **jsonl `gain_match_*_ratio` ×1 vs rest-IC plant FD** (P123 Huber still uses `_gain_match_rest_local_g` while `opscale=held`; @85 jsonl MV **×1.018** while orig-P1 TM **0.31@MV** — pin ≠ 5-level); **local/ident as a predicted orig-P1 TM** (local MV **0.87×** ident / DV **0.46×**; orig-P1 was **0.31@MV / 0.85@DV** — not the local-G ratio); **industrial TGP 0.5–2.0 as a freeze band** (valve installed-gain folklore; our READY band stays **[0.80, 1.30]** — P122 cap **0.78** transferring to val **×0.769** is under-gain, not “in TGP”); **rest-IC Huber `|G|` span ≲ 1.3 as an opscale linear-detector** (wrong scale: P123/P122 test_sim span MV **1.49** / |mean| **2.19** vs P119 nonlinear **2.01** / **2.89** — same class; use SysID `|K|=|amp/Δu|` max/min across valid MV repeats instead: P123/P122 **1.170**, P119 **1.858**); **healthy recon / jsonl ×1 as a TM/freeze score** (P123@85 recon **0.0057** jsonl MV **×1.018** then orig-P1 last_ok **85 = 0.31@MV**; P122 @85 recon **0.0081** jsonl MV **×1.007** then **0.32@MV**); **P123 orig-P1 0.31@MV as an opscale TM KEEP/FALSIFY** (P1-gate only FALSIFIES opscale as the *orig-P1 miss*; EXIT val still owns TM vs P116); **P122 cap LIVE 0.78@MV as GAIN-READY** (band is [0.80, 1.30]; `skip_invalid_p3` skipped P3); **P122 val TM ×0.769 as an opscale KEEP or FALSIFY** (INVALID cap freeze; better than P116 ×0.706 is **not** a KEEP — freeze was 0.78 not READY, opscale still trained, `opdev` **0.91** left identity); **P121 val TM ×0.668 / DV ×0.849 / curve 0.295 as an opscale KEEP or FALSIFY** (sr matched P116; wrap-locked last_ok **91** 0.61@MV; expert-BC); **P120 val TM ×0.816** same class (`sr=3 H=74`; last_ok **85**); extra-P1 LIVE TM ≠ freeze TM (P120/P121); P122 freeze **is** the cap LIVE probe (last_ok **109**); residual_board event IAE from npz when JSON already has seed-median IAE (P119 board was **64.36** vs summary **20.3** — **fixed**; JSON IAE is the score); **P116 `validation_summary.fidelity_gates.smooth_pass=true`** (old MV dump) vs `residual_board.smooth_pass=false` — residual_board is the score; current `validate.py` already uses CV. P125 TM png agrees with residual_board (MV SS wm −0.241 / real −0.32 **×0.75**; DV **×1.00**). P125 board agrees with GOAL_PLAN (MV ×0.754 / rev 0.304 / paired −284 vs −421 / critic_r −0.014 / ret_h 0.109). **P125 paired 9/9 is hunting-in-`raw_reward` (on trial)** — DR pngs show a per-step spike-train (seed 10004 `cum_raw≈−257k`; summary mean **−302k**). Seed 10004 rev **0.207 PASS** still hunts through hi on the plot — `smooth_pass` 0.25 is not “no hunting”; trust the plot + worst-seed. **Val `critic_r` is V vs MC of the same hunting-contaminated `info['raw_reward']`** (diagnostics.py `_critic_calibration`). P125 **−0.014** is inversion vs that hunting MC, **not** a train-hunt / val-econ stream split. #8 still needed: honest paired econ + stop twohot fitting hunt spikes. **Do not** form `raw_econ = comps['reward'] + cv_reversal` **after** `objective_runtime`’s outer `_saturate_two_sided` — hunt steps already sit at reward clip 1000, so post-sat add-back does **not** recover pre-sat econ. Split **before** that saturate. **P125 CPU opposite-move already sat at reward clip 1000** with gain 0.3 (`cv_w` 3750); #4b gain 1.0 sits at `cv_base` 12500 and saturates milder `term>0.08` hunts (was `term>0.267`) — it does **not** raise the clipped full-flip. If EXIT still FAIL rev at cap, close family (cannot N+1 gain>1; clip is the ceiling) → **#8**. **last_ok 5× vs 20× lock** GPU-confirmed wrap#1+#2 then orig-P1 unlocked **0.81@MV** — do not retune those ratios. **P126@24** recon **8.9×** / jsonl MV **×0.495** recovered@26 without 20× lock (8.9<20) — same class as P125@16; **P126 wrap#1@40** recon **0.1302 = 29×** locked / unlocked@41 **0.0196** / last_ok walked **41→80** then wrap#2 lock; **P126 wrap#2@80** recon **0.6353 = 282×** / @90 still **0.135 = 60×** locked; jsonl MV **×0.081@81 then ×1.00** still tautology; **orig-P1@85 FAIL 0.74@MV** is last-ok probe (live recon 155×) — **not** a freeze / **not** #4b FALSIFY / **not** an opscale TM score. Do **not** score TM. **P125 val `wm_diagnostics.png`**: V mean **−90** vs G mean **−18490** on hunting MC — scale collapse, not just sign; #8 predicted signature includes |V|~|G| on reversal-free MC. **P125 `validation_summary.fidelity_gates.mv_reversal_rate_max=0.5`** is a val-time leftover in that JSON; current `control_quality_gates` deleted it (`smooth_pass` is CV d2/reversal; smoke asserts the max key is gone).

## Closed families (do not N+1)

**#4 / #4b CV sticky-tanh gain** (P125 KEEP hug/DR vs P124; P125+P127 **FALSIFIED residual-closed smoothness**; P127 rev **0.349** worse than P125 **0.304**; gain already at `cv_base`; cannot N+1 gain>1; skip **#4c** not mid-band); extra-P1 as freeze (P41; **P117**; **P118**); extra-P1 as orig-P1 PASS / GAIN-READY restorer (**P121** srmed; **P122** slready CAPPED **0.78@MV**; **P123** identity-hold extra-P1 **0.60@MV** then CAPPED **0.68@MV**); **opscale-as-orig-P1-PASS-restorer** (**P123** identity-hold orig-P1 **0.31@MV** ≈ P122 trained **0.32@MV** — KEEP hold as linear hygiene / **FALSIFIED** as the miss); **held rest-IC plant-FD Huber** (**P123** orig-P1 0.31@MV — **P124** SysID-median Huber orig-P1 **PASS 0.89@MV**, extra-P1 **0**, val MV **×0.871**); compounding-teacher Huber (P111 traj unpromoted, P115 k1 REVERT, P116 ol1 REVERT); isolation-off KEEP as default; 2TS-α as champ; kfeat as freeze; quiet_env as freeze; decoder FF; TSSM; GRU keep-h. Extra-P1 stay-lock-if-READY is **KEEP hygiene**. Do **not** extra-P1 N+1 / held-Huber N+1 / identity-relaunch P124.

## P124 `medhuber` — EXIT VALID (KEEP #1c freeze/TM; FALSIFIED as actor-econ)

tmux `mbrl2_p124` **gone**. pid **604554 DEAD**. sha **`a8b5ebd`**. **EXIT** 396 jsonl iters ES `p3_plateau` best **196** det **−32.314**. nvidia **0 MiB**. `best.pt` + `final.pt`. Val ckpt **`best.pt`**. `actor_experiment_valid=true`. jsonl NaN/Inf **0**. Env-free. `[resolved-cfg] opscale=held` compile=eager `skip_invalid_p3=True` gmatch_len=56. Train start **2026-09-10 17:16:03**. `run_plan` **sr=4 H=56**. Huber banner **`G_tgt = SysID median (opscale held)`**. `[opscale] held identity` **1.200**. **opdev=0.0 @1–396**. skip_cum **4** recovered. No `[env-override]`. **Do not identity-relaunch.**

**P1 gates (fair unlocked window; extra-P1 did not fire):**
- **Orig-P1 @88 PASS GAIN-READY** last_ok **88 unlocked**. LIVE DC **[0.89,1.00] @H[0.91,0.92]** worst **0.89@MV** DV **1.00/@H 0.91** `not_noisy=True` spread_x**1.1** `compound_ok` 1step→OL **1.18** draws[0.88,0.89,0.90]. Freeze recon **0.0068 ≈ 1.3×** best **0.0050**. Wrap#1 recovered; last_ok walked. `p1_extension=0`. **No extra-P1.** Freeze = orig-P1 LIVE. Warm-restore SKIPPED. `[phase] 1→2` **797040**.

**P2 89–144 then P3:** first dobg **0.0018@89** skip **0** KEEP vs P95. `dob_A` **0.95257** held. End-P2 leftover **0.133** (`|d_slow|/|d|` 0.051/0.383) K **0.181/3.81** std **0.942**. P2→P3@**144 PASS** MTP **1.057**. rscale freeze **1.626**. P3 **145–396** (252 iters). Last P3 ent **−0.136** rtgt **0.00163** clip **0.07**. Val `critic_r` **−0.115** inverted.

**Val (score actor — VALID):** MV TM ss/@H **×0.871 / ×0.906** curve **0.112** (wm −0.279 vs real −0.320; H=**56**). DV **×1.073 / ×0.989** curve **0.056**. Freeze 0.89→val 0.871 **held**. Decomp real→post **×0.904** post→1step **×0.845** 1step→OL **×1.234** OL **×0.748** lever=**free_bits**. DV decomp faithful. Lineage HEALTHY rel_err MV **0.13** / DV **0.07**; observer-gain HEALTHY. R2 d2 **0.035 PASS** rev **0.391 FAIL** head **0.762** viol_frac **0.583** `smooth_pass=False` econ=**hi**. MV rev **0.231** diagnostic. R3 det_r **0.334** pred_std **3.77 vs 1.38** event IAE **30.7** ret_h **0.962** ret_t **0.761**. Paired **−154.03 vs −152.90** **4/9 WIN** (seeds 10003–10006) `beats_baseline_pass=False`. `all_pass=False`. mv_viol mean **1.05**. cum_raw **−159173**. TM json agrees with residual_board.

**Judge:** ✅ KEEP #1c as orig-P1 PASS restorer vs P123 0.31@MV / P116 0.84@DV. ✅ KEEP as TM vs P116 ×0.706 (not a P26 swap). ❌ **FALSIFIED as actor-econ** vs baseline / P116 **−8.24** / P64 **−4.54**. ❌ **FALSIFIED as dist champ** (det_r 0.334 < P26 0.68 / P116 0.676). Skip **#1e**. Skip **#7** this step (lever=**free_bits**, not P116 autoencoder). Next GPU **#4 P125 `cvrev`**. Champion **P64**. Dist champ **P26**.

## P123 `opgate` — EXIT `[p3-skip]` INVALID (do not score actor / do not KEEP opscale TM)

tmux `mbrl2_p123` **gone**. pid **600533 DEAD**. sha **`746d8a3`**. **EXIT=0** 163 jsonl iters ES `p3_skipped_invalid_observer`. nvidia **0 MiB**. No `best.pt`. Val `final.pt` expert-BC. `actor_experiment_valid=false`. jsonl NaN/Inf **0**. Env-free. `[resolved-cfg] opscale=held` `skip_invalid_p3=True` gmatch_len=54. Train start **2026-09-10 13:10:35**. `run_plan` **sr=4 H=54**. locgfix `|du_mv|=0.4000=step`. Huber banner **`G_tgt = rest-IC plant FD`**. **opdev=0.0 @1–163**. skip_storm **0**. **Do not identity-relaunch.**

**P1 gates (lottery closed; freeze = last_ok 109 not READY):**
- **Orig-P1 @86 FAIL** last_ok **85** **0.31@MV** `@H 1.64` DV **0.85/@H 0.82** `not_noisy=False` spread_x**5.5** signflips=1 draws[0.15,0.31,0.33]. Live recon **0.3249 = 65×** best **0.0050** — last-ok probe. jsonl @85 MV **×1.018** ≠ TM **0.31**. Wrap#3 lock@85 **unlocked@87**.
- **Extra-P1 @98 FAIL** LIVE **0.60@MV** `@H 0.63` DV **0.94/@H 0.88** `not_noisy=False` spread_x**1.9** signflips=2 draws[0.60,0.58,0.66]. recon **0.0050 = 1×** — probed LIVE. last_ok **98 unlocked** `grl=False`.
- **Cap @109 CAPPED (P1→P2):** LIVE **0.68@MV** `@H 0.74` DV **0.84/@H 0.87** `not_noisy=True` draws[0.70,0.68,0.57]. Freeze recon **0.0115 ≈ 2.3×** — detonated-restore **did not fire**. Warm-restore SKIPPED. `[phase] 1→2` **921600**. last_ok **109 unlocked**. Freeze = cap LIVE **0.68**, **not** wrap **0.31**.

**P2 110–163 then `[p3-skip]`:** first dobg **0.0077@110** skip **0** KEEP vs P95. `dob_A` **0.95257** held. `dob_K` **0.020@110→0.131@163** SS **≈2.76**. leftover **0.155**. `std_ratio` **0.890**. P2→P3@**163 PASS** MTP **1.068** then **`[p3-skip]`**. End-P2 recon **0.088**.

**Val (do not score actor):** MV TM ss/@H **×0.612 / ×0.681** curve **0.331** (wm −0.196 vs real −0.320; H=**54**). DV **×0.839 / ×0.865** curve **0.165**. Lineage `wm_gain_pass` PASS (rel_err **0.388**) / `wm_gain_healthy=False` / observer-gain PASS ≠ GAIN-READY. Decomp MV real→post **×0.877** post→1step **×0.915** 1step→OL **×0.969** OL-vs-real **×0.757** lever=**autoencoder**. DV real→post **×1.112** post→1step **×0.490** lever=prior. det_r **0.375** pred_std **1.477 vs 1.36**. Event IAE **24.4**. R2 d2 **0.035** PASS rev **0.332** FAIL `smooth_pass=False` head **0.498** viol_frac **0.214** econ=**hi**. `critic_r=nan`. Paired **−45.81 vs −124.81** 9/9 expert-BC. TM json agrees with residual_board. Freeze 0.68 → val ×0.612 **dropped** (not P122 held 0.78→0.769).

**Judge:** KEEP identity-hold as **linear hygiene**. **FALSIFIED as orig-P1 PASS restorer** and **extra-P1 READY-maker**. KEEP `skip_invalid_p3`. **Not** an opscale TM KEEP (INVALID; ×0.612 worse than P116 ×0.706 / P122 ×0.769). Extra-P1 lottery **closed**. Remaining P116 confound = **#1c** local-FD Huber. Next GPU **P124 `medhuber`**. Champion **P64**. Dist champ **P26**.

## P122 `slready` — EXIT `[p3-skip]` INVALID (do not score actor / do not FALSIFY opscale)

tmux `mbrl2_p122` **gone**. pid **593350 DEAD**. sha **`f74a7e8`**. **EXIT=0** 163 jsonl iters ES `p3_skipped_invalid_observer`. nvidia **0 MiB**. No `best.pt`. Val `final.pt` expert-BC. `actor_experiment_valid=false`. jsonl NaN/Inf **0**. Env-free. `[resolved-cfg] opscale=True` `skip_invalid_p3=True` gmatch_len=54. Train start **2026-09-10 06:56:56**. `run_plan` **sr=4 H=54** `sample_rate_source=auto:tau_fast/10_or_dead_dom/2` `dead_fast=7` `tau_fast=46` `dead_dom=8`. locgfix `|du_mv|=0.4000=step`. skip_storm **0**. **Do not identity-relaunch.**

**P1 gates (lottery closed; freeze = last_ok 109 not READY):**
- **Orig-P1 @86 FAIL** last_ok **85** **0.32@MV** `@H 0.34` DV **0.84/@H 0.81** `not_noisy=False` spread_x**4.8** signflips=1. Live recon **0.0459 = 16.9×** best **0.0027**. jsonl MV **×0.994** ≠ TM **0.32**. Wrap#3 lock@76 last_ok **76** (48×) **unlocked@78** (`extra_p1=False`; orig-P1 wrap-recovery KEEP). last_ok then walked **85→109**.
- **Extra-P1 @98 FAIL** LIVE **0.53@MV** `@H 0.52` DV **0.87/@H 0.82** `not_noisy=True` spread_x**2.5**. recon **0.0087 = 3.2×**. last_ok **98** `locked=False` `grl=False`. Extra-P1 recon@95 **0.0174 = 6.4× <20×** — **no extra-P1 wrap lock**. Distinctive `already_locked + extra_p1 + grl=False` unlock **unfired**.
- **Cap @109 CAPPED (P1→P2):** LIVE **0.78@MV** `@H 0.85` DV **0.80/@H 0.80** spread_x**1.1** signflips=0 `not_noisy=True` draws[0.76,0.78,0.78]. Freeze recon **0.0051 = 1.9×** — detonated-restore **did not fire**. Warm-restore SKIPPED (`wm_best` gain-blind). `[phase] 1→2` **921600**. last_ok **109 unlocked**. Freeze = cap LIVE **0.78**, **not** wrap-locked 85 (**0.32**).

**P2 110–163 then `[p3-skip]`:** first dobg **0.0022@110** skip **0** KEEP vs P95. `dob_A` **0.95257** held. `dob_K` **0.018@110→0.169@163** SS **≈3.56**. leftover `|d_slow|/|d|` **0.76@110→0.135@163**. `std_ratio` **45495@110 → 0.947@163**. `opdev` **~0.91**. P2→P3@**163 PASS** MTP **1.049** then **`[p3-skip]`**. last_ok **109 unlocked** not-READY through EXIT. End-P2 recon **0.092** (Kalman-on / g frozen — do not score).

**Val (do not score actor):** MV TM ss/@H **×0.769 / ×0.856** curve **0.167** (wm −0.246 vs real −0.320; H=**54**). DV **×0.806 / ×0.837** curve **0.179**. Lineage `wm_gain_pass` HEALTHY (rel_err **0.231 / 0.194**) / observer-gain HEALTHY ≠ GAIN-READY. Decomp MV real→post **×0.915** post→1step **×0.789** 1step→OL **×1.143** OL-vs-real **×0.768** lever=**free_bits**. DV real→post **×0.985** post→1step **×0.622** lever=prior. det_r **0.252** pred_std **0.924 vs 1.38**. Event IAE **15.0**. R2 d2 **0.034** PASS rev **0.339** FAIL `smooth_pass=False` head **0.192** viol_frac **0.268** econ=**hi**. `critic_r=nan`. Paired **−38.57 vs −141.50** 9/9 expert-BC. TM json agrees with residual_board. Freeze 0.78 → val ×0.769 **held** (not P120/P121 wrap collapse).

**Judge:** `[p3-skip]` on walked last_ok **109** is **#1 KEEP as freeze-restorer** (vs P121 stay-lock of 91) and **FALSIFIED as orig-P1 PASS / GAIN-READY maker** (CAPPED **0.78@MV**, 0.02 short of band). Extra-P1 lottery still **closed**. **Not** an opscale KEEP/FALSIFY (INVALID; opscale still trained; `opdev` **0.91** left identity). Val ×0.769 vs P116 ×0.706 is **not** a KEEP. Next GPU **P123 `opgate`**. Champion **P64**. Dist champ **P26**.

## P121 `srmed` — EXIT `[p3-skip]` INVALID (do not score actor / do not FALSIFY opscale)

tmux `mbrl2_p121` **gone**. pid **588400 DEAD**. sha **`4422be5`**. **EXIT=0** 168 jsonl iters ES `p3_skipped_invalid_observer`. nvidia **0 MiB**. No `best.pt`. Val `final.pt` expert-BC. `actor_experiment_valid=false`. jsonl NaN/Inf **0**. Env-free. `[resolved-cfg] opscale=True` `skip_invalid_p3=True` gmatch_len=55. Train start **2026-09-10 01:06:11**. `run_plan` **sr=4 H=55** `sample_rate_source=auto:tau_fast/10_or_dead_dom/2` `dead_fast=7` `tau_fast=45` `dead_dom=8`. locgfix `|du_mv|=0.4000=step`. skip_cum **1**. **Do not identity-relaunch.**

**P1 gates (lottery closed; freeze = last_ok 91 not READY):**
- **Orig-P1 @88 FAIL** last_ok **85** **0.65@MV** `@H 0.66` DV **0.79/@H 0.79** `not_noisy=True`. Live recon **0.0199 > 5×** best **0.0026** — measured last_ok, not wrap g. Wrap lock@86 (27×) **unlocked@87** (`extra_p1=False`; orig-P1 wrap-recovery KEEP). last_ok then walked **85→90@90→91@91**.
- **Extra-P1 @101 FAIL** LIVE **0.66@MV** `@H 0.71` DV **0.84/@H 0.87** `not_noisy=True`. Wrap lock@95 last_ok **91** (35×); recon recovered **0.0146@96 / 0.0102@101 / 0.0063@111** all **<20×**. jsonl `p1_last_ok_locked=True` `gain_ready_locked=False` **95→111** — P40 extra-P1 stay-lock of a **not-READY** snapshot (P120-class).
- **Cap @111 CAPPED** LIVE **0.68@MV** `@H 0.77` DV **0.71/@H 0.72**. Detonated-freeze restore **91**; re-probe **0.61@MV** `@H 0.60` DV **0.79/@H 0.80** `signflips=1` still not READY. Warm-restore SKIPPED (`wm_best` @110 gain-blind). `[phase] 1→2` **950380**.

**P2 112–168 then `[p3-skip]`:** first dobg **0.0084@112** skip **0** KEEP vs P95. `dob_A` **0.95257** held. `dob_K` **0.017@112→0.121@168** SS **≈2.56**. leftover `|d_slow|/|d|` **0.74@112→0.20@168**. `std_ratio` **38@112 → 0.95@168**. `opdev` **~0.83**. P2→P3@**168 PASS** MTP **1.072** then **`[p3-skip]`**. last_ok **91** locked not-READY through EXIT. End-P2 recon **0.123** (Kalman-on / g frozen — do not score).

**Val (do not score actor):** MV TM ss/@H **×0.668 / ×0.683** curve **0.295** (wm −0.214 vs real −0.320; H=**55**). DV **×0.849 / ×0.889** curve **0.139**. Lineage `wm_gain_pass` HEALTHY (rel_err **0.332 / 0.151**) / observer-gain HEALTHY ≠ GAIN-READY. Decomp MV real→post **×0.979** post→1step **×0.901** 1step→OL **×0.634** OL-vs-real **×0.602** lever=**compounding**. DV real→post **×0.947** post→1step **×0.824** lever=prior. det_r **0.610** pred_std **4.96 vs 1.40**. Event IAE **28.4**. R2 d2 **0.038** PASS rev **0.303** FAIL `smooth_pass=False` head **0.418** viol_frac **0.265** econ=**hi**. `critic_r=nan`. Paired **−33.17 vs −136.98** 9/9 expert-BC. TM json agrees with residual_board.

**Judge:** `[p3-skip]` on wrap-locked last_ok **91** is **P40 extra-P1 stay-lock of not-READY + orig-P1 miss**, not an opscale verdict. sr=**4** H=**55** matched P116 — srmed **sampling KEEP**; srmed **FALSIFIED as orig-P1 PASS restorer** (P116 @87 **0.84@DV** last_ok **78**). Val MV **×0.668** is **worse** than P116 ×0.706 and P120 ×0.816 — wrap freeze, **not** an opscale FALSIFY (INVALID). Extra-P1 lottery **closed**. Stay-lock of not-READY **GPU-confirmed** (P120+P121). Next GPU **P122 `slready`**. Champion **P64**. Dist champ **P26**.

## P120 `tshome` — EXIT `[p3-skip]` INVALID (do not score actor / do not FALSIFY opscale)

tmux `mbrl2_p120` **gone**. pid **583595 DEAD**. sha **`03a93f1`**. **EXIT=0** 165 jsonl iters ES `p3_skipped_invalid_observer`. nvidia **0 MiB**. No `best.pt`. Val `final.pt` expert-BC. `actor_experiment_valid=false`. jsonl NaN/Inf **0**. Env-free. `[resolved-cfg] opscale=True` no ol1. Train start **2026-09-09 19:10:17**. `run_plan` **sr=3 H=74** `dead_dom=8` `dead_fast=6` `tau_fast=47`. locgfix `|du_mv|=step`. **Do not identity-relaunch.**

**P1 gates (lottery closed; freeze = last_ok 85 not READY):**
- **Orig-P1 @86 FAIL** last_ok **85** **0.70@MV** `@H 0.71` DV **1.00/@H 1.03** `not_noisy=True`. Wrap #3 live recon **0.0818 = 27×**.
- **Extra-P1 @98 FAIL** LIVE **0.43@MV** (`not_noisy=False`; recon 3.58×). last_ok **still 85** stay-locked (`gain_ready_locked=False`).
- **Cap @109 CAPPED** last_ok **85** **0.75@MV**; restore 85 re-probe **0.80@MV** `@H 0.84` DV **1.01/@H 1.04** still not READY. Warm-restore SKIPPED (`wm_best` @140 gain-blind). `[phase] 1→2` **943410**.

**P2 110–165 then `[p3-skip]`:** first dobg **0.0384** skip **0** KEEP vs P95. `dob_A` **0.95257** held. `dob_K` **0.018@110→0.056@165** SS **≈1.19**. leftover **0.77@110→0.55@165**. `std_ratio` **1.69e5@110 → 0.67@165**. α **0.00391**. `opdev` **~0.90**. P2→P3@**165 PASS** MTP **0.912** then **`[p3-skip]`**. last_ok **85** locked not-READY through EXIT.

**Val (do not score actor):** MV TM ss/@H **×0.816 / ×0.852** curve **0.163** (wm −0.261 vs real −0.320; H=**74**). DV **×1.011 / ×1.060** curve **0.083**. Lineage `wm_gain_pass` HEALTHY (rel_err **0.184 / 0.011**) / observer-gain HEALTHY ≠ GAIN-READY. Decomp MV real→post **×0.958** post→1step **×0.584** 1step→OL **×1.095** OL-vs-real **×0.779** lever=**free_bits**. DV real→post **×1.125** post→1step **×0.441** lever=prior. det_r **0.237** pred_std **1.49 vs 1.33**. Event IAE **23.2**. R2 d2 **0.034** PASS rev **0.353** FAIL `smooth_pass=False` head **0.234** viol_frac **0.263** econ=**hi**. `critic_r=nan`. Paired **−31.63 vs −159.86** 9/9 expert-BC. TM json agrees with residual_board.

**Judge:** `[p3-skip]` on wrap-locked last_ok **85** is **P40 stay-lock + orig-P1 miss + sr=3**, not an opscale verdict. Val TM did **not** collapse vs P116 ×0.706 (ss **better**, different H) — still **not** an opscale KEEP. extra-P1 lottery closed. Next was **P121 `srmed`** (now EXIT `[p3-skip]`). Stay-lock-only-if-READY is **P122**. Champion **P64**. Dist champ **P26**.

## P118 EXIT — `opscale` INVALID observer (do not score / do not FALSIFY opscale)

tmux `mbrl2_p118` **gone**. pid **551549 DEAD**. sha **`ff5f84c`**. **EXIT=0** 166 jsonl iters ES `p3_skipped_invalid_observer`. nvidia **0 MiB**. No `best.pt`. Val `final.pt` expert-BC. `actor_experiment_valid=false`. jsonl NaN/Inf = **one** `wm_grad_norm=inf@13`. Env-free. `[resolved-cfg] opscale=True` no ol1. Local G MV **0.0153** vs ident **2.627** (engineering `_prev_control` Δu). **Do not identity-relaunch.**

**P1 gates:** orig-P1@**87 FAIL** last_ok **85** **0.34@MV**; extra-P1@**98 FAIL** live **0.11@MV**; cap@**110** last_ok **108** **GAIN_NOT_READY 0.68@MV / 0.85@DV** `@H[0.77,0.79]` `not_noisy=True`. Detonated-freeze restored 108. Warm-restore SKIPPED (`wm_best` @100 gain-blind). Graph released.

**P2 111–166:** first dobg **0.0209** skip **0** KEEP vs P95. `dob_A` **0.95257** held. `dob_K` **0.119→0.017@111→0.0018@117** min **0.0016@119** then recovered **0.047@166** SS **≈1.00**. leftover `|d_slow|/|d|` **0.80@111→1.53@117→0.416@166**. `std_ratio` **144389@111 → 0.77@166**. α **0.00391**. `wm_op_scale_dev` **~0.93**. P2→P3@**166 PASS** MTP **1.216** then **`[p3-skip]`**.

**Val (do not score actor):** MV TM ss/@H **×0.609 / ×0.705** curve IAE/\|real\| **0.35**. DV **×0.799 / ×0.703** curve **0.24**. Lineage `wm_gain_pass` PASS (rel_err 0.39 / 0.20) / `wm_gain_healthy=False` / observer-gain PASS ≠ GAIN-READY. Decomp MV real→post **×0.882** post→1step **×0.846** 1step→OL **×1.045** OL-vs-real **×0.607** lever=autoencoder. DV real→post **×0.868** post→1step **×0.827** lever=autoencoder. det_r **0.343** pred_std **3.00 vs 3.07**. Event IAE median-of-medians **29.2**. `critic_r=nan`. `all_pass=False` (`critic_pass`). Paired **−38.39 vs −212.28** 9/9 expert-BC.

**Judge:** locgfix **already on HEAD** (`414eca4`). **Not** an opscale FALSIFY. extra-P1 lottery closed. Next was **P119 `locgfix`** (now EXIT VALID). Champion **P64**. Dist champ **P26**.

## P119 EXIT — `locgfix` VALID (do not identity-relaunch / do not launch `gop`)

tmux `mbrl2_p119` **gone**. pid **557257 DEAD**. sha **`ea7def9`**. **EXIT=0** 406 jsonl iters ES `p3_plateau` (no >+1.0% over `best_det_return=−131.981` for 200 iters) best **206**. env_steps **1645000**. nvidia **0 MiB**. `best.pt` + `final.pt`. Val ckpt **`best.pt`**. `actor_experiment_valid=true`. jsonl NaN/Inf **0**. Env-free (`CUDA_VISIBLE_DEVICES=0`, no `DREAMER_*`). `[resolved-cfg] opscale=True` no ol1. Train start **2026-09-09 14:10:33**. **`[p3-skip]` did not fire.** P3 **159–406** (248 iters) skip_iter **0** (cum skip **11** is P1 storm-1). Critic-warmup 159–168; `[return-scale] FREEZE` **2.128** @169; unfreeze @169. @406 actor **+0.116** critic **9.51** ent **−0.467** logp **0.54** clip **0.10** rscale **2.13** rtgt **0.00067** `critic_pred_target_r` **0.991**. **Do not identity-relaunch.** **Do not launch P120 `gop`** (val TM **×0.758**, not ~P118 ×0.61). Next was **P120 `tshome`** (now EXIT `[p3-skip]`).

**Teacher print (locgfix CONFIRMED):** local G `plant_fd=6/6` mean MV **2.890** / DV **−0.341** vs identified **2.709 / −0.435**. span MV **2.01** / DV **0.46**. `|du_mv|=0.4000` **=** step **0.4000**. **No** WARNING. jsonl MV O(1) since ~@5.

**Orig-P1 @91 FAIL (fair locgfix freeze miss):** live g **0.63@MV** `@H 0.61` DV **1.03/@H 0.98**. jsonl MV **×0.991** ≠ TM. extra-P1 lottery **closed**.

**Extra-P1 @101 PASS (not a freeze KEEP):** last_ok **100** GAIN-READY **0.83@MV** `@H 0.98` DV **0.92/@H 0.95** `compound_ok` 1step→OL **0.86**. Detonated-freeze restore **100**; re-probe **0.85@MV** 1step→OL **0.90**. Warm-restore SKIPPED. `[phase] 1→2` env_steps **908750**. P100-class confound.

**P2→P3 @158 PASS:** MTP median **1.180** (max=3.00 n=5) `p2_extension=0/42897`. env_steps **1276250**. P2 skip **0**. End-P2 recon **0.0527** dobg **0.0319** `dob_A` **0.95257** `dob_K` **0.167** SS **≈3.52** leftover **0.159** std **0.885**. First dobg **0.0034@102** skip **0** KEEP vs P95. vs P118 leftover **0.416** K **0.047/1.00** MTP **1.216** then `[p3-skip]`.

**Val (score actor — VALID):** MV TM ss/@H **×0.758 / ×0.922** curve IAE/|real| **0.153** (wm +0.693 vs real +0.914). DV **×0.942 / ×0.950** curve **0.080** (wm −0.390 vs real −0.414). Lineage MV rel_err **0.24** HEALTHY; DV **0.06** HEALTHY; `wm_observer_gain` HEALTHY. Decomp MV real→post **×0.960** post→1step **×0.962** 1step→OL **×0.846** OL-vs-real **×0.783** lever=**compounding**. DV real→post **×0.895** post→1step **×1.096** lever=autoencoder. det_r **0.346** R²_det **−4.27** pred_std **7.10 vs true 3.07**. Event IAE median-of-medians **20.3** (P118 **29.2**). Paired **−32.11 vs −230.94** **9/9 WIN**. cum_raw **−55585** (36). `critic_r` **+0.253** FAIL (<0.3) → `all_pass=False`. `smooth_pass=True` (mv_reversal **0.171** all-eps / **0.196** DR). hugging **0.384** / **0.316** DR; cv_viol **16.7**; mv_viol **2.79**. No `cv_d2_rms_normed` / `cv_opt_headroom` / `cv_viol_frac`. Val “[val] P3: 11 grad-clip skips” is **cumulative** P1 storm, not P3 this-iter.

**Judge:** ✅ KEEP locgfix as **teacher-unit fix** vs P118 0.015 / val ×0.609. ✅ KEEP as **P3-entry** vs P118 `[p3-skip]`. ❌ extra-P1 as freeze KEEP (orig-P1 **0.63@MV** is the fair miss; freeze last_ok **100** 0.83@MV transferred to val ×0.758, not P109-class collapse). ❌ **FALSIFIED as TM-to-P26** (×0.758 vs ×0.973). Remaining R1 hole is **compounding**. ❌ **FALSIFIED as Kalman amp** (pred_std 7.10 vs 3.07; det_r ~P118 0.343). Actor **VALID** vs baseline; **do not swap P64** (different plant). **P120 `gop` PARKED** — trigger was val TM still ~×0.61; it is ×0.758. Cadence: fair plant verdict done. GPU **free**. Do **not** extra-P1 N+1 / identity-relaunch P117/P118/P119 / rewrite locgfix / in-place `c*=gop`.

## Ranked follow-ups

**#1 P122 `slready` — Extra-P1 stay-lock only if last_ok is GAIN-READY (EXIT KEEP hygiene; FALSIFIED as READY-maker).** P120 + P121 stay-locked not-READY last_ok **85/91**. **P122 GPU-confirmed the walk:** last_ok **85→109** `locked=False` `grl=False`; freeze is cap LIVE **0.78** not wrap **0.32**. `extra_p1=` on `already_locked` **deleted** this land (`gain_ready_locked` decides). P40 READY overwrite still stay-locks. Do **not** extra-P1 N+1 / identity-relaunch P122.

**#1b P123 `opgate` — EXIT `[p3-skip]` INVALID (do not identity-relaunch).** Identity-hold **GPU-confirmed**: orig-P1 **0.31@MV**; extra-P1 **0.60@MV**; CAPPED **0.68@MV**; val MV **×0.612**. last_ok walked **85→109**.
- **Judge:** ✅ KEEP identity-hold as **linear hygiene** (opdev=0 @1–163). ❌ **FALSIFIED as orig-P1 PASS restorer**. ❌ **FALSIFIED as extra-P1 READY-maker**. ✅ KEEP `skip_invalid_p3`. ❌ **Not** a TM KEEP (INVALID; ×0.612 worse than P116 ×0.706). Do **not** opscale N+1 / extra-P1 N+1 / `gop` / sr pin / lower `gain_ready_lo`. Next **#1c**.

**#1c P124 `medhuber` — EXIT VALID (do not identity-relaunch).** SysID-median Huber while held **GPU-confirmed**. Orig-P1@**88 PASS** **0.89@MV** last_ok **88 unlocked**; extra-P1 **0**; val MV **×0.871**.
- **Judge:** ✅ KEEP as orig-P1 PASS restorer vs P123 **0.31@MV** / P116 **0.84@DV**. ✅ KEEP as TM vs P116 ×0.706 (freeze 0.89→val 0.871 held; not a P26 swap). ❌ **FALSIFIED as actor-econ** (paired **−154 vs −153** 4/9; loses to P116 **−8.24** / P64 **−4.54**). ❌ **FALSIFIED as dist champ** (det_r **0.334**). Skip **#1e**. Next **#4**.
- **Mechanism (landed):** when `op_scale_identity_held`, skip `local_g = lg_t`; Huber uses SysID-median. Nonlinear unheld keeps local FD.
- **Files:** `training/train.py` `_wm_gain_match_loss`; `tools/_smoke_rssm.py`. Tag `medhuber`. Out-dir `output/test_sim/run_p124_medhuber`.

**#1d DV plant-FD Δu (PARKED — nonlinear only).** `_plant_fd_rest_local_g` MV uses commanded WM-norm Δu (P119 locgfix); DV uses **realized Δobs** after `set_disturbance_offset`. P124 diagnostic still **0.54×** ident (0.306 vs 0.564) even under SysID Huber. If a later **unheld** plant still under-teaches DV, switch DV `du` to commanded WM-norm (same as MV). Do **not** N+1 while held.

**#1e Held `op_scale_net` bypass (PARKED — skip; #1c KEPT on unlocked last_ok).** Scale was already ≡1 (`opdev=0`). Graph-topology A/B vs P116 is **not** the next GPU. If a later held plant FALSIFIES orig-P1 after SysID Huber, then this. Else **#7** after #4.

**#2 P121 `srmed` sampling — KEEP; freeze-restorer FALSIFIED.** `run_plan` sr=**4** H=**55** source `dead_dom` CONFIRMED. Do **not** N+1 the Nyquist formula. Do **not** pin `DREAMER_SAMPLE_RATE`.

**#3 After a VALID observer on this plant:** **#1c KEPT**. Do **not** identity-regularizer λ lottery. **#4 EXIT** — next **#4b**. Skip #1e.

**#4 P125 `cvrev` — EXIT VALID (KEEP hug/DR-return vs P124; FALSIFIED as residual-closed smoothness).** One mechanism: retarget reversal from MV chatter to the val gate. CPU tests PASSED (MV bang-bang **0**; opposite/after-hold **1000** clip; monotonic/hold **0**; `mv_reversal_weights=0`).
- **EXIT VALID** pid **608718 DEAD** sha `8b25553`. 456 jsonl ES `p3_plateau` best **256** det **−856.6** (hunting-inflated; do not vs P124 **−32**). nvidia **0 MiB**. tmux gone. `best.pt` + `final.pt`. Val ckpt **`best.pt`**. `actor_experiment_valid=true`. jsonl NaN/Inf **0**. skip **0**. **opdev=0.0**. `[opscale] held identity` **1.155**. H=**57** sr=4. No `[env-override]`.
- **Orig-P1 @90 PASS GAIN-READY** last_ok **90 unlocked**. LIVE DC **[0.81,0.93] @H[0.84,0.91]** worst **0.81@MV** DV **0.93/@H 0.91** `not_noisy=True` `compound_ok` 1step→OL **0.85** R=3 draws[0.82,0.78,0.81]. Freeze recon **0.0047 ≈ 1.9×** best **0.00245**. Wrap#1+#2 recovered; last_ok walked. extra-P1 **0**. Warm-restore SKIPPED. `[phase] 1→2` **825300**.
- **P2→P3 @147 PASS:** MTP **1.473**. first dobg **0.0017@91** skip **0**. `dob_A` **0.95257** held. End-P2 leftover **0.106** (`|d_slow|/|d|` 0.042/0.395) K **0.173/3.65** std **0.936**. rscale freeze **6.953** (hunting-inflated vs P124 **1.626**). P3 **148–456**. Last P3 ent **−0.130** rtgt **0.00732** clip **0.062**. Val `critic_r` **−0.014** inverted.
- **Val (score actor — VALID):** MV TM ss/@H **×0.754 / ×0.795** curve **0.204** (wm −0.241 vs real −0.320; H=**57**). DV **×0.998 / ×1.016** curve **0.051**. Freeze 0.81→val 0.754 **dropped** vs P124 held 0.89→0.871. Decomp real→post **×0.940** post→1step **×1.030** 1step→OL **×0.762** OL **×0.736** lever=**compounding**. Lineage HEALTHY rel_err MV **0.25** / DV **0.00**. R2 d2 **0.033 PASS** rev **0.304 FAIL** head **0.202** viol_frac **0.347** `smooth_pass=False` econ=**hi**. MV rev **0.149**. R3 det_r **0.558** pred_std **1.54 vs 1.38** event IAE **22.8** ret_h **0.109** ret_t **0.188**. Paired **−283.78 vs −420.87** **9/9 WIN** `beats_baseline_pass=True` — **on trial** (hunting in `raw_reward`). `all_pass=False`. TM png agrees with residual_board.
- **Judge:** ✅ KEEP sticky-tanh as R2 **direction** vs P124 (rev 0.391→0.304; head 0.762→0.202; viol 0.583→0.347; ret_h 0.962→0.109; ret_t 0.761→0.188). Plots: CV near hi with remaining hunting, **not** mid-band. MV chatter present (allowed). ✅ KEEP as Kalman amp vs P124 crush (pred_std 1.54 vs 1.38; P124 3.77). ❌ **FALSIFIED as residual-closed smoothness** (rev still 0.304>0.25). ❌ **Not** a TM KEEP vs P124 ×0.871. ❌ Do **not** KEEP-econ / swap P64 / swap P26. Next **#4b**. Skip **#4c** (not mid-band). Skip **#7** (lever=**compounding**, not autoencoder).

**#4b P126 `rev1` — EXIT `[p3-skip]` INVALID (do not score / do not FALSIFY).** Sticky stays `tanh(1)`. `obj_auto_reversal_gain` 0.3→**1.0** (at `cv_base` cap). Env-free. No `DREAMER_*`.
- **EXIT** pid **612995 DEAD** sha **`df7d54f`**. tmux `mbrl2_p126` **gone**. 162 jsonl ES `p3_skipped_invalid_observer`. nvidia **0 MiB**. No `best.pt`. Val `final.pt` expert-BC. `actor_experiment_valid=false`. jsonl NaN/Inf **0**. skip_iter **0**. **opdev=0.0**. `[opscale] held identity` **1.187**. H=**53** sr=4. `run_plan` `obj_auto_reversal_gain=1.0`. No `[env-override]`. No `EXIT=` line (tee closed after `run_summary`; val completed). **Do not identity-relaunch P126.**
- **P1-gate CAPPED:** Orig-P1@**85 FAIL** last_ok **80 locked** **0.74@MV**; extra-P1@**97 FAIL** **0.72@MV**; Unlock@100 15.7× then re-lock@101; cap@**107 CAPPED** detonated restore **80** re-probe **0.76@MV**. `[phase] 1→2` **894440**. Freeze = wrap-locked last_ok **80**, **not** GAIN-READY.
- **P2→P3 @162 PASS then `[p3-skip]`:** first dobg **0.0015@108** skip **0** KEEP vs P95. `dob_A` **0.95257** held. End-P2 leftover **0.100** (`|d_slow|/|d|` 0.042/0.420) K **0.252/5.31** std **0.948** MTP **1.447** recon **0.128** (Kalman-on / g frozen — do not score). `[p3-skip]` KEEP.
- **Val (do not score actor):** MV TM ss/@H **×0.785 / ×0.814** curve **0.177** (wm −0.251 vs real −0.320; H=**53**). DV **×0.915 / ×0.939** curve **0.110**. Freeze 0.74–0.76→val **×0.785 held**. Decomp real→post **×0.947** post→1step **×0.983** 1step→OL **×0.789** OL **×0.741** lever=**compounding**. Lineage HEALTHY rel_err MV **0.215** / DV **0.085** ≠ GAIN-READY. R2 d2 **0.038 PASS** rev **0.329 FAIL** head **0.162** viol **0.376** `smooth_pass=False` econ=**hi**. MV rev **0.113** diagnostic. R3 det_r **0.380** pred_std **2.58 vs 1.37** event IAE **27.8** ret_h **0.063** ret_t **0.203**. `critic_r=nan` V **0** vs G **−16737** (P3 skipped). Paired **−284 vs −435** **8/9** expert-BC. TM png agrees with residual_board.
- **Judge:** ✅ KEEP `skip_invalid_p3`. ❌ **Not** a #4b FALSIFY (wrap freeze; P124+P125 orig-P1 PASS on the same observer recipe). ❌ **Not** a TM KEEP. Do **not** score expert-BC rev/head/econ. Next **P127 `rev1b`**. Skip **#4c** (P125 not mid-band; P126 wrap-cap is not mid-band evidence). Skip **#7** (lever=**compounding**).
- **Mechanism / predicted signature / falsifier:** unchanged (score only on GAIN-READY P3). Wrap-cap / `[p3-skip]` is **not** a FALSIFIER.

**#4b-retry P127 `rev1b` — EXIT VALID (FALSIFIED smoothness; KEEP hug/DR vs P124; close #4 family).** Identical env-free #4b wrap-lottery retry. No code change. No `DREAMER_*`.
- **EXIT VALID** pid **618741 DEAD** sha **`830b1b1`**. 441 jsonl budget `early_stop_reason=null` best **246** det **−842.8**. nvidia **0 MiB**. tmux gone. `best.pt` + `final.pt`. Val ckpt **`best.pt`**. `actor_experiment_valid=true`. jsonl NaN/Inf **0**. skip **0**. **opdev=0.0**. `[opscale] held identity` **1.149**. H=**55** sr=4. `obj_auto_reversal_gain=1.0`. No `[env-override]`. Train start **2026-09-11 08:08:54**. EXIT=0. **Do not identity-relaunch P127.**
- **Orig-P1 @86 FAIL (P1-gate; fair miss):** last_ok **86 unlocked**. LIVE DC **[0.69,0.83] @H[0.70,0.83]** worst **0.69@MV** DV **0.83/@H 0.83** `not_noisy=True` draws[0.66,0.69,0.69]. recon **0.0105 = 3.2×**. Extra-P1 extend **+77592 → 853512**.
- **Extra-P1 @109 PASS (freeze; P41 confound):** last_ok **109 unlocked** recon **0.0122 = 3.7×** LIVE **[0.82,0.94] @H[0.83,0.89]** worst **0.82@MV** DV **0.94/@H 0.89** `compound_ok` 1step→OL **0.94**. `[phase] 1→2` **935740**. Warm-restore SKIPPED. Freeze = extra-P1 **0.82@MV** — **not** an orig-P1 KEEP vs P125 **0.81@MV**.
- **P2→P3 @163 PASS:** first dobg **0.0078@110** skip **0**. End-P2 leftover **0.181** K **0.103/2.17** std **0.924**. MTP **1.495**. **`[p3-skip]` did not fire.** rscale FREEZE **6.931**. P3 **164–441**. Last P3 ent **−0.179** rtgt **0.0082** clip **0.133**.
- **Val (score actor — VALID):** MV TM ss/@H **×0.764 / ×0.781** curve **0.207** (wm −0.244 vs real −0.320; H=**55**). DV **×0.990 / ×0.908** curve **0.052**. Freeze 0.82→val 0.764. Decomp real→post **×0.938** post→1step **×0.817** 1step→OL **×0.926** OL **×0.683** lever=**free_bits**. R2 d2 **0.034 PASS** rev **0.349 FAIL** head **0.219** viol **0.271** `smooth_pass=False` econ=**hi**. MV rev **0.165**. R3 det_r **0.514** pred_std **1.09 vs 1.35** event IAE **15.3** ret_h **0.163** ret_t **0.242**. Paired **−284.62 vs −440.80** **9/9** hunting-in-`raw_reward`. `critic_r` **+0.318 PASS**. V **−81** vs G **−15318**. TM/DR/dist pngs agree with residual_board.
- **Judge:** ❌ **FALSIFIED as residual-closed smoothness** (rev **0.349** worse than P125 **0.304**). ✅ KEEP hug/DR vs P124 (head **0.219** vs **0.762**; ret_h **0.163** vs **0.962**). ✅ KEEP amp vs P124 crush. ❌ **Not** a TM KEEP vs P124 ×0.871. ❌ Do **not** KEEP-econ / swap P64 / swap P26. Close **#4 family** (gain at `cv_base`; clip is the ceiling). Skip **#4c** (plots hunt through hi, **not** mid-band). Skip **#7** (lever=**free_bits**). Next **#8 P128 `critsplit`**.

**#4c PARKED — skip.** P125 and P127 VALID EXITs hunt through the limit, not mid-band sit. Do **not** launch hug-aware reversal.

**#5 P120 `gop` (PARKED)** — **do not launch**. P119 val TM **×0.758** is not ~×0.61. P73 persist forbids in-place `c*=gop`.

**#6 After a later plant has a real fast DV:** min of *per-channel* median θ, not global median of every repeat. Do **not** launch this while test_sim channel medians are all 8.

**#7 Autoencoder lever (PARKED — only if EXIT lever=autoencoder and R1 still short of P26).** P116/P123 lever=**autoencoder**. **P124 / P127 lever=free_bits**. **P125 lever=compounding**. Residual `Linear(c_gain)` is the autoencoder hole (P74 skip **REVERT**). **Do not** launch `gcvres` while lever≠autoencoder. Tag `gcvres`. Do **not** launch on P129.

**#8 P128 `critsplit` — EXIT VALID (KEEP split / FALSIFIED critic-scale).** #4 family closed. Critic twohot/MC on reversal-free econ; actor λ + S on hunt. pid **624485 DEAD** sha `bb5667e`. EXIT=0 376 jsonl ES `p3_plateau` best **176** det **−1220**. `best.pt` + `final.pt`. Val ckpt **`best.pt`**. `actor_experiment_valid=true`. jsonl NaN/Inf **0**. TrainConfig vs P127 **unchanged**. Orig-P1@**88 FAIL 0.75@MV** unlocked; extra-P1@**99 PASS 0.81@MV** last_ok **99 unlocked** (P41). P2→P3@**155 PASS** MTP **1.418** no `[p3-skip]`. rscale FREEZE **6.160**. Last P3 clip **0.67** pos_adv **0.026** ratio **0.41**. Do **not** identity-relaunch.
- **Mechanism (landed; one split; no new TrainConfig / no `DREAMER_*`):** unchanged vs launch (split before outer saturate; critic on `rew_econ`; actor λ+S on hunt). Calib still read hunt sat (item 6) — that is the #8b hole.
- **EXIT (score actor — VALID):** Honest econ **KEEP**: paired **−79.89 vs −139.40** **9/9** on reversal-free `raw_reward`; |G| **15318→180**; cum_raw **−36k** vs hunting **−302k**. Critic-scale **FALSIFIED**: r **+0.058 FAIL**, V **−2.48** vs G **−180**, slope **22.88 FAIL** (not ∈ [0.25,4]). Calib bound hunt sat (`raw_min −1035` / p95 **964**) while honest G is **−180**. Hug/DR KEEP vs P124 (not mid-band). Rev **0.469** worse than P125 **0.304** is remaining mixed-A hunt → **#10 after #8b**, not an #8 smoothness FALSIFY. Close nothing on #4.
- **Judge:** ✅ KEEP split. ❌ FALSIFIED critic-scale (twohot still hunt-calibrated; shared bound ref). Next **#8b**. Do **not** MC-coef N+1.

**#8b P129 `econcal` — EXIT VALID (KEEP critic-scale).** P128 slope ≫1 after |G| dropped to honest-econ **−180**. One mechanism: twohot/bound support on reversal-free econ; S stayed on hunt. pid **638866 DEAD** sha `af3f254`. EXIT=0 441 jsonl budget best **391** det **−545**. `best.pt` + `final.pt`. Val ckpt **`best.pt`**. `actor_experiment_valid=true`. jsonl NaN/Inf **0**. TrainConfig vs P128 **unchanged**. Orig-P1@**85 FAIL** compounding-edge; extra-P1@**107 PASS 0.88@MV** last_ok **107 unlocked** (P41). P2→P3@**162 PASS** MTP **1.481** no `[p3-skip]`. rscale FREEZE **7.007**. Last P3 clip **0.39** pos_adv **0.360** ratio **0.84**. Do **not** identity-relaunch.
- **Mechanism (landed; one; no new TrainConfig / no `DREAMER_*`):** calib on `raw_reward`; `env._bound_econ_ref` = calib `|raw| p95`; hunt bound ref stays `reward_clip`; actor λ+S stayed on hunt. Critic diagnostic G uses `reward_econ_train`. Calib CONFIRMED p95 **305** `bound_econ_ref` **305**.
- **EXIT (score actor — VALID):** Critic-scale **KEEP**: r **+0.447 PASS**, V **−12.39** vs G **−4.52** (same order), slope **0.540 ∈ [0.25,4]** (`critic_pass` TRUE vs P128 r +0.058 / slope 22.88 / V −2.48 vs G −180). Honest paired **−104.25 vs −122.43** **9/9** still beats baseline. Rev **0.312** (P128 **0.469** / P125 **0.304**) is remaining mixed A → **#10**, not an #8b smoothness FALSIFY. Hug/DR: head **0.406** viol **0.490** — plots hunt through hi, **not** mid-band. Close nothing on #4. Skip **#8c**.
- **Judge:** ✅ KEEP #8b as critic-scale (bound econ ref). ❌ Do **not** KEEP-econ vs P64 / P128 **−80**. ❌ **Not** a TM KEEP (freeze 0.88→val ×0.754). ❌ Do **not** swap P26 (det_r **0.169**). Next **#10**. Do **not** MC-coef / B N+1 / clipcoef N+1.

**#8c PARKED — P129 KEEP critic-scale; do not launch.** Only if a later critic-scale FALSIFY (slope still ≫1 on matched `reward_econ_train`). Hafner 2023 twohot CE is un-normalized (`symlog` of the return; support **[-20,20]**). Bound remap `clip(raw·B/ref, −B, B)` with B=3 feeds twohot `symlog(±3)≈±1.39`. P129 econ is **mostly negative** (raw_max **+0.73** → mapped **+0.007**), so occupancy is one-sided: CPU replay of the calib range gives bound **~10–11** active bins vs unused scale-path **43** bins / mag **6.0** (the healthy distribution adaptive clip already saw). #8b makes p95 occupy ±B; if V still fails slope~1 on those units, the hole is twohot-on-a-clip, and #8c is literally “train CE on the scale-path distribution calib already scored healthy.” Andrychowicz et al. NeurIPS 2023: twohot **bin range** moves performance — do **not** raise B as a lottery. Do **not** retarget adaptive clip `twohot_active_bins<10` onto bound-path bins — P129-class occupancy **is** ~10–11 and would fire a second mechanism on #10/#8c.
- **Mechanism (one; no new TrainConfig / no `DREAMER_*`):** critic twohot/MC on **unbounded** econ (`raw_econ * reward_scale` or unshaped `reward_econ` **before** `_bound_shape`); hunt bound remap **stays** for actor λ. Do **not** raise B / N+1 `bound_training_reward_max`. Files: `training/train.py` `APCEnv.step` (econ skip `_bound_shape`) + `_realsim_actor_critic_step` (already on `rew_econ`); smoke: assert econ CE target not clipped to ±B while hunt still is.
- **Predicted signature:** slope ∈ [0.25,4] AND r≥0.3 on matched units; |V|~|G_train|; twohot mass not piled at ±B.
- **Falsifier:** Pearson still collapsed, or mid-band, or reversal-free paired loses to baseline. Then **stop and report** — do not B N+1 / MC-coef / clipcoef. Do **not** launch #8c after #8b KEEP (then #10).

**#9 PARKED until #10 EXIT KEEP smoothness — R3 Kalman NIS (spec: recompute 2TS from `ν.detach()` + live K).** P129 det_r **0.169** pred_std **1.45 vs 1.28** ret_h **0.381**; P128 det_r **0.504** pred_std **2.78 vs 1.39** (KEEP amp vs P124 crush **3.77**; still short of P26 **0.68**; P129 drop is freeze-luck, not a P26 swap). Ground-loss / pin-A / 2TS-α / feat / increment-MSE / `(ratio−1)²` amp families **closed**. Do **not** dobg / α / pin-K N+1. Rank **below #10** while R2 hunt remains (Qin/Badgwell). Rank **below #11** if #10 FALSIFIES smoothness.
- **Parked spec was broken (no K path):** `rollout_observed` already computes `nu = cv_obs - decode(prior_core)` (`models/dreamer_v4_rssm.py` ~1277). That **plant residual is ⟂ K**. `mean((ν / pred_std.detach())²)→1` with stop-grad `g` has **zero gradient**; live ν through decode would train P1 TM. Do **not** land that form.
- **`e = sg(ν) − ds` on the *existing* live `ds` also leaks g:** `d_slow = scan(α·ν)` and `ds_fast = scan(K·(ν−d_slow), A)` both depend on live ν. Smoke would see decode grads. Do **not** land that form either.
- **Mechanism (one; P2 `dob_live` only; no new TrainConfig / no `DREAMER_*`):** Bar-Shalom / Maybeck NIS on a leftover that depends on **live K** and **stop-grad ν**. Recompute 2TS from `ν_sg = ν.detach()`: `d_slow_sg = scan(α·ν_sg, 1−α)` (no K); `ds_fast = scan(K·(ν_sg − d_slow_sg), A)` (live K, pin-A); `d_nis = d_slow_sg + ds_fast`; `e = ν_sg − d_nis`. `σ̂ = sg(true-load std)` already in `_dob_ground_hp_mse` (`t_std`; grounding already uses GT). `loss = |mean((e/σ̂)²) − 1|` (or Huber around 1), **same `dob_ground_coef`**. Not z-score amp, not increment MSE, not pin-K, not feat-tail (P104/P105 closed), not `(ds.std/true.std−1)²`, not NIS on ν itself. jsonl `dob_nis` = that mean (diagnostic). KalmanNet (Revach et al. IEEE TSP 2022) trains K from a residual that depends on K — this is that residual; ν is not.
- **Predicted signature:** det_r ↑ toward P26 **without** TM drop vs this freeze / P124 ×0.871; pred_std stays vs true (not crush). Over-amp leftover≈0 ⇒ NIS≪1 pulls |d| down; under-K leftover large ⇒ NIS≫1 pulls K up. DR ret_h not worse than P129 **0.381**.
- **Falsifier:** TM collapse vs this freeze, or pred_std crush, or GAIN_NOT_READY, or smoke shows g grads from this term / K grads **0**. Then **stop and report** — do not α N+1.
- **Files:** `models/dreamer_v4_rssm.py` stash `_dob_nu = nu` next to `_dob_k_applied` (optional; loss can take `nu` from `rollout_observed`); `training/train.py` `_rssm_world_model_loss` after `dob_ground` (P2 only) — **recompute 2TS from `ν.detach()` + live K**, do not subtract live `ds`; smoke: NIS≈1 on matched leftover; `g` grads 0; K grads ≠0. Tag `dobnis`. Dist champ stays **P26**.

**#10 P130 `actorecon` — EXIT VALID (KEEP mixed-A econ / FALSIFIED residual-closed smoothness).** Largest open residual whose family was **not** closed: R2 CV hunt (#4 gain closed). One mechanism: stop mixing hunt λ into A. DreamerV3 (Hafner 2023 / Nature 2025; danijar/dreamerv3#20): return **and** baseline share S, then `A = (R−V)/S`. #8 mixed `A = (ret_hunt − V_econ)/S_hunt` is paper-deviant by design; #10 is the paper-faithful same-stream flip.
- **Mechanism (landed prior visit; one; no new TrainConfig / no `DREAMER_*`):** `_realsim_actor_critic_step` (`training/train.py`):
  1. Keep `ret_econ = λ(rew_econ)` (**10484**). Hunt λ **deleted**.
  2. `adv_raw = ret_econ - v_pred` (**10555**).
  3. `update_return_scale(ret_econ)` (**10556–10559**).
  4. `realsim_return_mean` = `ret_econ.mean()` (**10627**). Keep `realsim_reward_mean` on hunt jsonl (**10628**).
  Sticky-tanh **stays** in env (not a gain N+1). Files: `training/train.py` `_realsim_actor_critic_step`; smoke source-scan pins `adv_raw = ret_econ - v_pred` / `update_return_scale(\n            ret_econ,` / `realsim_return_mean: ret_econ`; P3 smoke asserts distinct `rew_econ` moves `critic_rew_to_tgt_var` **and** `realsim_return_mean` while hunt mean stays (`freeze_return_scale=True`). CPU smoke **ALL PASSED**. P129 rscale freeze **7.007** is hunt-spread S; #10 moves S onto econ λ so A and baseline share one stream.
- **Predicted signature:** worst-seed rev ≤ P125 **0.304** (ideally ≤0.25) **and/or** orbit_rate ↓ vs P125 **0.644 /τ** / P129 **0.537 /τ** **without** mid-band (head/viol not back to P124 **0.762/0.583**); paired still beats baseline on reversal-free econ; slope not worse than P129 EXIT **0.540**. Train canaries (not gates): `actor_ratio_clip_frac` toward P127 **~0.10** (P129 last **0.39** already better than P128 **0.67**) and `actor_pos_adv_frac` toward **~0.4** (P129 last **0.36**). MV chatter allowed. Do **not** require beating P64.
- **Falsifier:** mid-band sit, or reversal-free paired loses to baseline, or rev worse than P125 **0.304** **and** orbit_rate not improved vs P125 **0.644**. Then do **not** actor-S N+1 / drop sticky / raise clip. Next is **#11** (orbit **gate**), not #4 gain.
- **LAUNCHED** pid **643440** sha `0248876` tmux `mbrl2_p130` `output/test_sim/run_p130_actorecon`. Train start **2026-09-12 00:05:04** `device=cuda` bs=128 H=**54** sr=**4**. `[opscale] held identity` **1.205**. compile=eager `skip_invalid_p3=True`. No `[env-override]`. No `DREAMER_*`. Calib p95 **329.6** scale **1.073**. Env-free vs P129. Literature: danijar/dreamerv3#20 `A=(R−V)/S` with S = 5–95 span of **the same R**. TorchRL `DreamerV3ActorLoss` divides advantage by `_return_scale(lambda_target)` of that same λ-return.
- **Orig-P1 @88 PASS (P1-gate / freeze — RUN_HISTORY):** last_ok **88 unlocked**. LIVE DC **[0.84,0.88] @H[0.86,0.87]** worst **0.84@MV** DV **0.88/@H 0.86** `not_noisy=True` `unbiased=True` spread_x**0.8** signflips=0 `compound_ok` 1step→OL **0.92** R=3 draws[0.82,0.85,0.84]. Freeze recon **0.0109 ≈ 4.5×** best **0.00245** (<5× — LIVE probe; detonated-restore **did not fire**). `[p1→p2] recon 0.4039 → 0.0109` is start-of-P1 vs freeze, not a detonated wrap. Wrap#1 lock last_ok **21** (jsonl spike @22 recon **0.455 = 71×** best **0.0064**) unlock@**25**; wrap#2 lock last_ok **68** (jsonl spike @69 recon **0.1093 = 31×** best **0.0035**) unlock@**70**; last_ok walked **21→88 unlocked**. `p1_extension=0`. **No extra-P1.** Freeze = orig-P1 LIVE **0.84@MV** — same class as P124 **0.89@MV** / P125 **0.81@MV**, **not** P127–P129 extra-P1 P41. Warm-restore SKIPPED (`wm_best` @60 gain-blind). Graph released (nvidia **14027→4167 MiB**). `[phase] 1→2` **784080**. jsonl @88 MV **×1.009** ≠ TM **0.84**.
- **P2→P3 @144 PASS:** first dobg **0.0043@89** skip **0**. `dob_A` **0.95257** held. End-P2 leftover **0.148** (`|d_slow|/|d|` 0.053/0.357) K **0.181/3.82** std **0.861**. MTP **1.475**. **`[p3-skip]` did not fire.** rscale FREEZE **4.595** (econ S vs P129 hunt **7.007**). P3 **145–461**. Last P3 clip **0.062** pos_adv **0.532** ratio **0.997** tgt_var **0.0004** Pearson **0.99** ent **−0.134** `realsim_return_mean` **−28.8**.
- **EXIT VALID** pid **643440 DEAD**. 461 jsonl ES `p3_plateau` best **261** det **−876**. nvidia **0 MiB**. tmux gone. `best.pt` + `final.pt`. Val ckpt **`best.pt`**. `actor_experiment_valid=true`. jsonl NaN/Inf **0**. skip **0**. **opdev=0.0**. EXIT=0. **Do not identity-relaunch P130.**
- **Val (score actor — VALID):** MV TM ss/@H **×0.763 / ×0.798** curve **0.198** (wm −0.244 vs real −0.320; H=**54**). DV **×0.925 / ×0.901** curve **0.106**. Freeze 0.84→val 0.763. Decomp real→post **×0.961** post→1step **×0.931** 1step→OL **×0.857** OL **×0.743** lever=**compounding**. R2 d2 **0.033 PASS** rev **0.388 FAIL** head **0.207** viol **0.263** orbit **0.602 /τ** `smooth_pass=False` econ=**hi**. MV rev **0.210** diagnostic. R3 det_r **0.215** pred_std **1.61 vs 1.39** event IAE **15.5** ret_h **0.136** ret_t **0.182**. Paired **−29.67 vs −134.76** **9/9** reversal-free. cum_raw **−29k**. `critic_r` **+0.353 PASS** V **−7.15** vs G **−3.57** slope **0.608 PASS**. TM/DR/dist pngs agree with residual_board.
- **Judge:** ✅ **KEEP #10 as mixed-A / honest econ** (rscale **4.595** econ S; last clip **0.062** / pos_adv **0.532** toward P127; paired **−30 vs −135** vs P129 **−104 vs −122**; policy unimodal ~−0.75). ✅ KEEP critic-scale (slope **0.608** / r **+0.353**). ✅ KEEP hug vs P124 (head **0.207** vs **0.762**; not mid-band). ❌ **FALSIFIED residual-closed smoothness** (rev **0.388** worse than P125 **0.304**; orbit **0.602** worse than P129 **0.537** and still ≫ 0.15; seed 10004 hunts through hi). ❌ **Not** a TM KEEP vs P124 ×0.871. ❌ Do **not** KEEP-econ / swap P64 / swap P26. Do **not** actor-S N+1 / drop sticky / raise clip. Next **#11**. Skip **#8c**. Skip **#9** (parked until KEEP smoothness).

**#11 LANDED — `limorbit` gate (P130 EXIT FALSIFIED smoothness).** Diagnostic **already landed**; this visit ANDs it into `smooth_pass`. Diehl/Bock NMPC + Angeli 2011: a periodic orbit around the constraint is not SS; Qin/Badgwell: cycling is lost econ.
- **Landed diagnostic:** `cv_limit_orbit_rate` = `(CV−y_econ)` sign-changes × `(τ/sr) / T`. Window from plant τ/sr (test_sim 55/4=14). Mid-band never crosses `y_econ` → 0. Slow ride → 0. P125 worst **0.644 /τ**; P127 **0.671**; P128 npz **0.294**; P129 **0.537**; **P130 0.602**. Gate ≤ **0.15 /τ**.
- **FALSIFIED draft:** 1τ-window ≥2 sign-change frac (seed 10004 **0.049** while the plot hunts). Do **not** GPU-optimize that frac.
- **LANDED this visit (val-only; one mechanism; no TrainConfig / no `DREAMER_*`):** AND `worst_orbit ≤ CV_LIMIT_ORBIT_MAX` into `cv_smooth_pass(d2, rev, orbit)`. `all_pass` follows `smooth_pass`. Files: `evaluation/residual_board.py` `cv_smooth_pass` 3-arg + `smooth_pass_rule`; `evaluation/validate.py` `_cv_smooth_from_rows(d2s, revs, orbits)` + `control_quality_gates`; `tools/_smoke_rssm.py` hunt fixture orbit>1.5 now **FAILS** `smooth_pass`; slow-ride PASS. Keep `cv_limit_orbit_frac` JSON alias. Tag `limorbit`. CPU smoke **PASSED**. P130 EXIT board was scored under the 2-arg rule; 3-arg would still FAIL (orbit **0.602**).
- **Confirmed:** P125/P127/P129/P130 hunt FAIL this gate; slow ride PASS; mid-band still FAIL on head/viol (not this metric).
- **Falsifier of the gate:** future EXIT plot hunts through hi while orbit ≤0.15 — retune the diagnostic; do **not** actor-S N+1 / GPU-optimize 0.15 / reversal 0.25→0.20.

**#12 P131 `limcost` — EXIT `[p3-skip]` INVALID (do not score / do not FALSIFY).** Remaining hunt (#4 closed, #10 FALSIFIED smoothness). Do **not** put hunt back into `rew_econ` (would undo #8/#10). Do **not** actor-S N+1 / sticky / clipcoef / #8c / #9. One mechanism: actor-only λ of **y_econ violation-entry** cost (not in paired econ). Dist champ stays P26.
- **Mechanism (landed prior visit; one; no new TrainConfig / no `DREAMER_*`):** `_cv_limit_entry_cost` uses the same deadband as val `_cv_limit_orbit_rate` (`1e-3·width`). Hi side: cost when `signed>0` and `prev<=0`. Lo side: `signed<0` and `prev>=0`. Mean over CVs. APCEnv appends that cost to `_rew_orbit_trace` (not `raw_reward`). `TrajectoryBuffer.rew_orbit` pops via `pop_episode_rew_orbit` (copy-not-clear like econ); **missing → zeros** (P130 identity), never hunt. `_realsim_actor_critic_step`: `ret_orbit = λ(rew_orbit, v=0)`; `adv_raw = ret_econ - v_pred - ret_orbit`; S still on `ret_econ`. Critic twohot/MC stay on `rew_econ`. Return-to-limit is **free** (R3). Sticky-tanh stays. jsonl `actor_orbit_ret_mean`. Files: `training/train.py`; smokes in `tools/_smoke_rssm.py`. Tag `limcost`. Out-dir `output/test_sim/run_p131_limcost`. Session `mbrl2_p131`.
- **Predicted signature:** worst-seed orbit ↓ vs P130 **0.602 /τ** **and/or** rev ↓ vs P125 **0.304** **without** mid-band (head/viol not back to P124 **0.762/0.583**); paired still beats baseline on reversal-free econ; critic slope not worse than P130 **0.608**; `actor_orbit_ret_mean` > 0 in P3. Score actor only if GAIN-READY AND P3 under the **#11 3-arg `smooth_pass`**. MV chatter allowed. Do **not** require beating P64.
- **Falsifier:** mid-band sit (head/viol back toward P124 **0.762/0.583**), or reversal-free paired loses to baseline, or orbit not improved vs P130 **0.602** / rev not vs P125 **0.304**. Then do **not** α-scale N+1 / put hunt in `rew_econ` / drop sticky / raise clip. Next is **#13 `limdwell`** (impulse vs integral), **not** actor-S / #8c / #9. **`[p3-skip]` / wrap-cap is not a FALSIFIER** (P126 class) — identity-retry `limcost`, do **not** jump to #13.
- **Literature:** Diehl/Bock NMPC + Angeli 2011 (periodic orbit around a constraint is not SS); Qin/Badgwell (cycling = lost econ). Soft-constraint **entry** cost is the actor lever; keeping it out of `rew_econ` preserves #8/#10 honest paired econ (Ng 1999: do not mix a shaping that games the score).
- **LAUNCHED** pid **647479** sha `8debbe0` tmux `mbrl2_p131` `output/test_sim/run_p131_limcost`. Train start **2026-09-12 04:27:25** `device=cuda` bs=128 H=**56** sr=**4** (SysID τ=**53.5** jitter vs P130 H=54; not a pin). `[opscale] held identity` **1.133**. compile=eager `skip_invalid_p3=True`. No `[env-override]`. No `DREAMER_*`. Calib p95 **233.7** scale **1.505**. `CUDA_VISIBLE_DEVICES=0`. One GPU. **Do not identity-relaunch P131** — next is P132.
- **Orig-P1 @88 FAIL (P1-gate; fair DV miss — RUN_HISTORY):** last_ok **88 unlocked** `grl=False`. LIVE DC **[0.69,0.84] @H[0.70,0.89]** worst **0.69@DV** MV **0.84/@H 0.89** DV **0.69/@H 0.70** `not_noisy=True` `unbiased=False` spread_x**0.7** signflips=0 R=3 draws[0.69,0.74,0.69]. Compounding decomp **skipped** (`unbiased` is the DC-band AND). Live recon **0.0032 ≈ 1.28×** best **0.00251** — LIVE probe, **not** wrap g. jsonl @88 MV **×1.004** DV **×0.993** ≠ TM **0.69@DV**. Extra-P1 extend **+79704 → 876744** (cap **980600**). vs P130 orig-P1 **PASS 0.84@MV / 0.88@DV**. **Judge:** MV in-band; miss is **DV under-gain** (tight draws, not noise). **Not** a wrap-cap. **Not** an #12 actor FALSIFY. Extra-P1 lottery **closed** as READY-maker. Do **not** lower `gain_ready_lo` / special-case DV / isolation N+1 (P40 isolation-off KEEP).
- **Extra-P1 @101 FAIL (P1-gate; still not READY):** last_ok **101 unlocked**. LIVE DC **[0.70,0.86] @H[0.70,0.86]** worst **0.70@DV** MV **0.86/@H 0.86** DV **0.70/@H 0.70** `not_noisy=True` `unbiased=False` spread_x**0.8** signflips=0 R=3 draws[0.72,0.70,0.69]. recon **0.0028 ≈ 1.1×**. jsonl persist **3.11** ≠ TM (P73 closed). Extending **+79704 → 956448**.
- **Cap @111 CAPPED (P1-gate / freeze — RUN_HISTORY):** last-ok probe iter **108** (live recon **0.0384 = 15.3×** best **0.00251** — not measuring wrap g). Cap-time DC **[0.75,0.78] @H[0.74,0.79]** worst **0.75@MV** DV **0.78/@H 0.79** `not_noisy=False` spread_x**1.7** signflips=2 R=3 draws[0.75,0.72,0.73]. `[gate p1->p2] CAPPED` GAIN_NOT_READY. Detonated-freeze restore **108**; re-probe **[0.74,0.82] @H[0.73,0.84]** worst **0.74@DV** MV **0.82/@H 0.84** DV **0.74/@H 0.73** `not_noisy=True` spread_x**1.7** signflips=0 draws[0.70,0.76,0.74] — still not READY. Warm-restore SKIPPED (`wm_best` gain-blind). Graph released (nvidia **14115→4104 MiB**). `[phase] 1→2` **958170**. last_ok **108 unlocked** `grl=False`. Freeze = restored last_ok **108** **0.74@DV**, **not** orig-P1 **0.69@DV** / extra-P1 **0.70@DV**. Wrap lock@**105** **42×** unlock@**107**; freeze is walked **108**, not wrap-lock.
- **P2→P3 @167 PASS then `[p3-skip]`:** first dobg **0.0133@112** skip **0** KEEP vs P95. `dob_A` **0.95257** held. End-P2 leftover **0.286** (`|d_slow|/|d|` 0.082/0.287) K **0.073/1.54** std **0.786**. MTP **1.511** `p2_extension=0`. env_steps **1313640**. End-P2 recon **0.094** (Kalman-on / g frozen — do not score). `[p3-skip]` KEEP.
- **EXIT `[p3-skip]` INVALID** pid **647479 DEAD**. 167 jsonl ES `p3_skipped_invalid_observer`. nvidia **0 MiB**. tmux gone. No `best.pt`. Val `final.pt` expert-BC. `actor_experiment_valid=false`. jsonl NaN/Inf **0**. skip_iter **0**. **opdev=0.0**. No `EXIT=` line (tee closed after `run_summary`; val completed). **Do not identity-relaunch P131.**
- **Val (do not score actor / #12):** MV TM ss/@H **×0.765 / ×0.795** curve **0.203** (wm −0.245 vs real −0.320; H=**56**). DV **×0.909 / ×0.940** curve **0.112**. Freeze 0.74→val **×0.765 held-ish**. Decomp real→post **×1.016** post→1step **×0.846** 1step→OL **×0.864** OL **×0.724** lever=**free_bits**. DV decomp prior-lag. Lineage `wm_gain_pass` True rel_err **0.235** ≠ GAIN-READY. det_r **0.078** pred_std **2.19 vs 1.29** event IAE **24.1** ret_h **0.250** ret_t **0.134**. R2 d2 **0.035 PASS** rev **0.358 FAIL** orbit **0.486 /τ** head **0.216** viol **0.367** `smooth_pass=False` econ=**hi**. MV rev **0.181** diagnostic. `critic_r=nan` V **0** vs G **−3.56**. Paired **−43.50 vs −132.67** **9/9** expert-BC. TM png agrees (MV SS ×0.77; DV ×0.91). Dist png overshoots (det_r **0.078**; amp **not** crushed vs P124 **3.77**). `wm_diagnostics.png` critic V=0; policy expert-BC ~−0.9. DR seed 10004: hunts through hi; title IAE=0.00 is tracking tautology. `summary.png` cum_raw **−43k**. `validation_summary.n_seeds=8` vs board `n_scripted_pairs=9` — residual_board is the score.
- **Judge:** ✅ KEEP `skip_invalid_p3`. ❌ **Not** a #12 FALSIFY (lottery not-READY; P124+P125+P130 orig-P1 PASS on the same observer recipe). ❌ **Not** a TM KEEP. Do **not** score expert-BC rev/orbit/econ. **P132 EXIT `[p3-skip]` INVALID** (val MV **×0.515** expert-BC). Skip **#13** until a **VALID** #12 FALSIFY. Skip **#8c**. Skip **#9**. Champion **P64**. Dist champ **P26**.

**#12-retry P132 `limcost` — EXIT `[p3-skip]` INVALID (identity; do not score actor).** Identical env-free #12 wrap-lottery retry of P131. No code change. No `DREAMER_*`. Tag `limcost`. Out-dir `output/test_sim/run_p132_limcost`. Session `mbrl2_p132` **gone**. pid **652156 DEAD** sha `a89c321` Train start **2026-09-12 09:08:19** `device=cuda` bs=128 H=**56** sr=**4** (SysID τ=**54** jitter vs P131 τ=53.5; not a pin). `[opscale] held identity` **1.116**. compile=eager `skip_invalid_p3=True`. No `[env-override]`. Calib p95 **341.3** scale **1.03** `bound_econ_ref` **341.3**. `CUDA_VISIBLE_DEVICES=0`. One GPU. EXIT=0 166 jsonl ES `p3_skipped_invalid_observer`. **Do not identity-relaunch P132.**
- **Orig-P1 @87 FAIL (P1-gate — RUN_HISTORY):** last_ok **87 unlocked** `grl=False`. LIVE DC **[0.37,0.90] @H[0.46,0.71]** worst **0.37@MV** MV **0.37/@H 0.46** DV **0.90/@H 0.71** `not_noisy=False` `unbiased=False` spread_x**5.3** signflips=1 R=3 draws[0.37,0.59,−0.97]. Compounding decomp skipped. Live recon **0.0102 ≈ 3.8×** best **0.00270** — LIVE probe, **not** wrap g. jsonl @87 MV **×0.998** ≠ TM **0.37**. Extra-P1 extend **+79608 → 875688** (printed cap **982034**; `[gate-budget]` clamped `p1_ext_cap` **796080→185954**). Wrap lock@**72** **165×** (jsonl @**73** recon **0.4460**); unlock@**76**; last_ok walked **83→87**. vs P131 orig-P1 **FAIL 0.69@DV** tight / P130 **PASS 0.84@MV**. **Judge:** DV DC in-band; miss is **noisy MV** (sign-flip −0.97). **Not** a wrap-cap. **Not** an #12 FALSIFY. Extra-P1 lottery **closed**. Do **not** special-case DV because P131 missed on DV — this retry missed on MV noise (Ljung).
- **Extra-P1 @98 FAIL (P1-gate — RUN_HISTORY):** last_ok **98 unlocked**. LIVE DC **[0.66,0.87] @H[0.63,0.77]** worst **0.66@MV** MV **0.66/@H 0.63** DV **0.87/@H 0.77** `not_noisy=True` `unbiased=False` spread_x**2.9** signflips=1 R=3 draws[0.66,0.76,0.66]. recon **0.0130 ≈ 4.8×** best **0.00270** — LIVE probe. jsonl @98 MV **×1.017** ≠ TM **0.66**. Wrap lock@**95** recon **0.0999 = 37×** unlock@**97**; last_ok walked. Extending **+79608 → 955296**. Still not READY. Extra-P1 lottery **closed**.
- **Cap @110 CAPPED (P1-gate / freeze — RUN_HISTORY):** last_ok **110 unlocked** `grl=False`. LIVE DC **[0.63,0.88] @H[0.74,0.83]** worst **0.63@MV** MV **0.63/@H 0.74** DV **0.88/@H 0.83** `not_noisy=True` `unbiased=False` spread_x**2.5** signflips=1 R=3 draws[0.63,0.69,0.63]. Freeze recon **0.0053 ≈ 1.9×** best **0.00270** — LIVE probe; detonated-restore **did not fire**. `[gate p1->p2] CAPPED` (`cap 185954 steps reached` — P3-floor **20%** of `total_steps` **1726080**). Warm-restore SKIPPED (`wm_best` gain-blind). Graph released (reserved **13546→3598** MiB). `[phase] 1→2` **957280**. Freeze = cap LIVE **0.63@MV**, **not** orig-P1 **0.37@MV** / extra-P1 **0.66@MV**. **Not** GAIN-READY. Two extra-P1 probes then CAP (second +79608 would have exceeded the clamped budget). vs P131 cap last_ok **108** **0.74@DV**.
- **P2→P3 @166 PASS then `[p3-skip]` (phase boundary — RUN_HISTORY):** first dobg **0.0019@111** skip **0** KEEP vs P95. `dob_A` **0.95257** held. End-P2 leftover **0.115** (`|d_slow|/|d|` 0.048/0.418) K **0.177/3.74** std **0.927**. MTP **1.483** `p2_extension=0/41709`. env_steps **1315640**. End-P2 recon **0.121** (Kalman-on / g frozen — do not score). `[actor] P3 is NOT an actor experiment`. `[p3-skip]` KEEP. `[early-stop] p3_skipped_invalid_observer`. jsonl **166**. NaN/Inf **0**. skip **0**. **opdev=0.0**. No `best.pt`.
- **EXIT `[p3-skip]` INVALID** pid **652156 DEAD**. 166 jsonl ES `p3_skipped_invalid_observer`. nvidia **0 MiB**. tmux gone. No `best.pt`. Val `final.pt` expert-BC. `actor_experiment_valid=false`. jsonl NaN/Inf **0**. skip **0**. **opdev=0.0**. EXIT=0.
- **Val (do not score actor / #12):** MV TM ss/@H **×0.515 / ×0.668** curve **0.373** (wm −0.165 vs real −0.320; H=**56**). DV **×0.955 / ×0.932** curve **0.084** (wm 0.172 vs real 0.180). Freeze 0.63→val **×0.515 dropped**. Decomp real→post **×0.953** post→1step **×1.036** 1step→OL **×0.705** OL **×0.698** lever=**compounding**. Lineage `wm_gain_pass` True rel_err **0.485** ≠ GAIN-READY. det_r **0.194** pred_std **1.86 vs 1.36** event IAE **19.9** ret_h **0.096** ret_t **0.162**. R2 d2 **0.038 PASS** rev **0.291 FAIL** orbit **0.813 /τ** head **0.161** viol **0.344** `smooth_pass=False` econ=**hi**. `critic_r=nan` V **0** vs G **−2.23** (`g_raw_mean` **−254**). Paired **−80.66 vs −135.11** **9/9** expert-BC. cum_raw **−61k**. Policy mean **−0.35**. residual_board agrees with TM json (MV SS ×0.515; DV ×0.955).
- **Judge:** ✅ KEEP `skip_invalid_p3`. ❌ **Not** a #12 FALSIFY (second lottery miss; P124+P125+P130 orig-P1 PASS on the same observer recipe). ❌ **Not** a TM KEEP (×0.515 worse than P131 ×0.765). Do **not** score expert-BC rev/orbit/econ. Extra-P1 lottery **closed**. Do **not** third identity / #13 / special-case DV / extra-P1 N+1 / lower `gain_ready_lo`. Next **#12b P133 `limobs`**. Champion **P64**. Dist champ **P26**.

**#13 `limdwell` — PARKED until a VALID #12 FALSIFIES smoothness.** P131+P132+P133 `[p3-skip]` INVALID: **do not** a third identity; **do not** jump to dwell without a VALID #12 actor. Next GPU is **#12c P134 `limcert`**. #12 is still the first orbit-aux A/B (untested, not falsified). P130 DR csv replay (same deadband as `_cv_limit_entry_cost`, hi side, 9 scripted seeds): median **entry** mean **0.017** (~21 pulses / 1210-step ep) vs **dwell** mean **0.226** (~13× denser; median run ~9–20 steps). **orbit↔entry Pearson 0.999** (entry pulses *are* the orbit crossings — #12 targets R2 orbit, not a proxy). **orbit↔dwell Pearson 0.15** (seed 10006 orbit **0.602** dwell **0.19**; seed 10004 orbit **0.430** dwell **0.43** / viol **0.435** — long far-side stays, fewer crossings). With γ=**0.99** λ=**0.9**, E[ret_orbit|entry]≈0.017/(1−γλ)≈**0.16** vs P130 last-P3 |ret_econ| **28.8** (~0.5% of |A|). Dwell ≈**2.1** (~7% of |A|). If #12 FALSIFIES, the hole is impulse **magnitude vs |A|**, not “wrong residual.” α-scale of the same impulse is forbidden after a FALSIFY; dwell is a **different mechanism** (integral / exact-penalty *state* cost vs an event pulse). Predicted #13 move is more viol/headroom than orbit (weak dwell–orbit corr) — mid-band sit is the falsifier to watch.
- **Mechanism (one; no new TrainConfig / no `DREAMER_*`):** replace the entry mask `(signed>0)&(prev<=0)` with per-step far-side occupancy `signed>0` (hi) / `signed<0` (lo). Same deadband (`1e-3·width`). Return-to-limit still **0**. Still actor-only λ, bootstrap **0**, S on `ret_econ`, critic/paired econ untouched. Sticky-tanh stays. Missing `rew_orbit` → zeros. jsonl `actor_orbit_ret_mean` kept. Files: `training/train.py` `_cv_limit_entry_cost` **2393–2422** (change mask only; keep name) + `APCEnv.step` **3466** (unchanged call) + `_realsim_actor_critic_step` **10607** / `adv_raw` **10679** (unchanged λ). **HEAD smoke is entry-only:** `_test_cv_limit_entry_cost` asserts hold-in-viol **0** and return-to-limit **0**; source-scan pins `(signed > 0.0) & (prev[:n] <= 0.0)`. #13 land **must** invert: dwell=1 while violating; return-to-limit=0; deadband ride=0; delete that mask (source-scan will fail until flipped). Tag `limdwell`. Out-dir `output/test_sim/run_p135_limdwell`. Session `mbrl2_p135`. **Do not** reuse `mbrl2_p132` / `run_p132_*` / `mbrl2_p133` / `mbrl2_p134` (`limobs` occupies P133; `limcert` occupies P134).
- **Predicted signature:** worst-seed orbit ↓ vs P130 **0.602 /τ** **and/or** rev ↓ vs P125 **0.304** **without** mid-band (head/viol not back to P124 **0.762/0.583**); paired still beats baseline on reversal-free econ; slope not worse than P130 **0.608**; P3 `actor_orbit_ret_mean` larger than #12 (canary, not a gate). Score only if GAIN-READY AND P3 under #11 3-arg `smooth_pass`. MV chatter allowed. Do **not** require beating P64.
- **Falsifier:** mid-band sit, or reversal-free paired loses to baseline, or orbit not improved vs P130 **0.602** / rev not vs P125 **0.304**. Then do **not** α-scale dwell / put hunt in `rew_econ` / drop sticky. Next is **#14 plant transfer** (`distillation` — spec below), **not** #8c / #9 / actor-S.
- **Literature:** Kerrigan/Maciejowski 2000 (soft constraints + exact penalty as a *state* cost, not an event impulse); Diehl/Bock NMPC + Angeli 2011 (orbit ≠ SS); Qin/Badgwell (cycling = lost econ). Ng 1999: keep the penalty out of paired `rew_econ`.
- **If #12 KEEP smoothness:** skip **#13**; unpark **#9 leftover-NIS**.
- **If P132 `[p3-skip]` INVALID (EXIT this visit — CAPPED 0.63@MV, val MV ×0.515):** two lottery misses of the same full-recipe `limcost` (P131+P132). **Do not** a third identity. **Do not** #13 yet (#12 still untested). Extra-P1 lottery stays **closed**. Do **not** special-case DV / extra-P1 N+1 / lower `gain_ready_lo`. Next GPU **#12b P133 `limobs`** — P3-only actor on the P130 GAIN-READY freeze (Qin/Badgwell). `--init-from-ckpt` **alone** is **not** that job (it retrains P1). Spec below.

**#12b P133 `limobs` — EXIT `[p3-skip]` INVALID (do not score actor / do not FALSIFY #12).** #12 hypothesis was **not** falsified (two full-recipe misses + freeze-transfer re-gate miss). Largest residual is still R2 hunt. pid **656936 DEAD** sha `8324e76` tmux `mbrl2_p133` **gone** `output/test_sim/run_p133_limobs`. Train start **2026-09-12 13:39:29** `device=cuda` `limobs=True` `skip_invalid_p3=True` compile=eager `[opscale] held identity` **1.143** H=**54** sr=4. No `[env-override]`. No `DREAMER_*`. CVD=0. `[init]` P130 `best.pt` iter **261**. Loaded 5-level `ready=False` **0.76@MV** / DV **0.91** draws[0.77,0.76,0.76] `not_noisy=True` (matches P130 val TM **×0.763**, not orig-P1 **0.84**). `[p3-skip]` KEEP. jsonl **0**. EXIT=0 ES `p3_skipped_invalid_observer`. `actor_experiment_valid=false`. No `best.pt`. Val ckpt **`final.pt`**. **Do not identity-retry P133.**
- **Mechanism (landed prior visit):** `--observer-from-ckpt` + P130 `best.pt`. Skip P1/P2 WM; freeze g+DOB; one 5-level of the loaded blob; `skip_invalid_p3` KEEP if that probe is not READY.
- **Val (do not score actor / #12):** MV TM ss/@H **×0.780 / ×0.810** curve **0.183** (wm −0.25 vs real −0.32; H=**54**). DV **×0.924 / ×0.901** curve **0.106**. Decomp real→post **×0.960** post→1step **×0.931** 1step→OL **×0.871** OL **×0.750** lever=**compounding**. Lineage HEALTHY rel_err MV **0.22** / DV **0.08** ≠ GAIN-READY. R2 d2 **0.036 PASS** rev **0.408 FAIL** orbit **0.546 /τ** head **0.198** viol **0.251** `smooth_pass=False` econ=**hi**. R3 det_r **−0.043** pred_std **3.64 vs 1.36** event IAE **13.6** ret_h **0.188** ret_t **0.172**. Paired **−28.77 vs −181.45** **9/9**. cum_raw **−25k**. `critic_r` **+0.476** V **−7.04** vs G **−3.15** slope **0.697**. Policy unimodal ~−0.5. TM png agrees (MV SS ×0.78; DV ×0.92). Dist png overshoots (det_r **−0.043**; amp **not** crushed). DR seed 10004: hunts through hi orbit **0.437 /τ** rev **0.339** — **not** mid-band. **This is a P130 actor re-eval** (P130 paired **−29.67** / r **+0.353** / slope **0.608**), not expert-BC and **not** an entry-cost P3.
- **Judge:** ✅ KEEP `skip_invalid_p3`. ❌ P1-band re-gate of actor-best **FALSIFIED** as freeze-transfer READY (tautological with known 0.84→0.763 drop). ❌ **Not** a #12 FALSIFY (P3 never ran). ❌ **Not** a TM KEEP. Do **not** score loaded-actor rev/orbit/econ as #12. Do **not** `skip_invalid_p3=0`. Do **not** load `wm_last_ok` as the **whole** ckpt (P1 actor; would drop the P130 mixed-A A/B). Next **#12c P134 `limcert`**. Champion **P64**. Dist champ **P26**.

**#12c P134 `limcert` — source VALID freeze certificate (next GPU).** P133 FALSIFIED the P1 re-gate. One mechanism: when `--observer-from-ckpt` and the loaded 5-level is not READY, honour sibling `run_summary.json` `actor_experiment_valid=true` **and** P3 `iters>0` (P130: true / 461 / best **261**). Still freeze g+DOB; still skip P1/P2 WM; still load P130 `best.pt` (actor+critic stay mixed-A). Uncertified blobs (P132 `final.pt` `actor_experiment_valid=false`) still `[p3-skip]`. **Not** `skip_invalid_p3=0`. **Not** a TrainConfig default / **not** `DREAMER_*`. 5-level stays a diagnostic print. Do **not** set `p1_gain_not_ready_capped` when the certificate fires (else EXIT could not score P3).
- **Mechanism (one; CLI path already landed; this visit adds cert):** `_source_freeze_cert_valid(init_from_ckpt)` reads `run_summary.json` next to the ckpt. Files: `training/train.py`; `workflow/single_run.py` help; smoke: cert true on P130 `best.pt`, false on P132 `final.pt` / missing / iters=0; entry mask still HEAD. Tag `limcert`. Out-dir `output/test_sim/run_p134_limcert`. Session `mbrl2_p134`. Ckpt `output/test_sim/run_p130_actorecon/best.pt`.
- **Predicted signature:** `[limobs] source freeze certificate actor_experiment_valid=true — enter P3`; jsonl >0; then same as #12 (orbit ↓ vs P130 **0.602 /τ** and/or rev ↓ vs P125 **0.304**, not mid-band; paired still beats baseline; slope not worse than P130 **0.608**; P3 `actor_orbit_ret_mean`>0). Score only if P3 ran under #11 3-arg `smooth_pass`. MV chatter allowed. Do **not** require beating P64. Do **not** KEEP as orig-P1 TM.
- **Falsifier:** cert missing/false still `[p3-skip]` (KEEP `skip_invalid_p3` — not a #12 FALSIFY); or P3 runs and mid-band sit / reversal-free paired loses baseline / orbit not improved vs P130 **0.602** and rev not vs P125 **0.304**. Then do **not** α-scale / `skip_invalid_p3=0` / load `wm_last_ok` as whole ckpt. Next is **#13 P135 `limdwell`**.
- **Literature:** Qin/Badgwell 2003 (trust the source identification certificate; do not retune the plant model to A/B the regulator). Ljung: 5-level of val-era g is not a new ID — P130 already passed orig-P1 **0.84@MV**. Hafner DreamerV3: actor on a frozen WM is the P3 recipe; this only **enters** P3 on a validated freeze. Ng 1999: entry-cost stays out of paired `rew_econ`.
- **LAUNCHED** pid **658156** sha `c29f426` tmux `mbrl2_p134` `output/test_sim/run_p134_limcert`. Train start **2026-09-12 14:12:45** `device=cuda` bs=128 H=**54** sr=**4**. `[opscale] held identity` **1.100**. compile=eager `skip_invalid_p3=True` `limobs=True`. No `[env-override]`. No `DREAMER_*`. CVD=0. `[init]` P130 `best.pt` iter **261**. Loaded probe **ready=False 0.77@MV** / DV **0.92** draws[0.76,0.77,0.78] `not_noisy=True`. `[limobs] source freeze certificate actor_experiment_valid=true — enter P3`. `[limobs] P3-only ... cert=True`. **No `[p3-skip]`**. Calib p95 **281.9** scale **1.237** `bound_econ_ref` **281.9**. `[p3-start] return_scale=4.59` (P130 freeze). P3 jsonl started (critic-warmup). Score actor only at EXIT if P3 ran. Champion **P64**. Dist champ **P26**. One GPU.

**#14 `xferdist` — PARKED until #13 FALSIFIES (or #12 KEEP smoothness and #9 KEEP).** Envelope cap: at most two jobs on a new plant, then one return-to-`test_sim` on the worst residual. `nonlinear_sim` already had P117–P119 (locgfix KEEP / TM FALSIFY). **`distillation` is the next plant:** 4 MV (reflux/boilup/cooling/feed) × 3 CV (top/bottom/pressure) × 1 DV (feed comp); `targets_enabled=[false,false,false]` (same tracking-IAE≡0 class as test_sim). MIMO coupled CVs — SISO limit-cycle may not transfer.
- **Mechanism (one; identity recipe; no new TrainConfig / no `DREAMER_*`):** env-free `python -m workflow.single_run --simulation-dir simulation/distillation --out-dir output/distillation/run_pNN_xferdist`. Same observer+DOB+real-sim actor as HEAD after #12/#13 verdict. Not an α N+1. Tag `xferdist`. Session `mbrl2_pNN`.
- **Predicted signature:** GAIN-READY freeze (skip_invalid_p3 KEEP); if VALID, score R2 orbit/rev/head/viol + R3 ret_h under #11 3-arg `smooth_pass` vs that plant's baseline — do **not** require beating P64. Hunt may vanish (coupled MIMO) or replicate (same economic-limit cycling).
- **Falsifier:** GAIN_NOT_READY / `[p3-skip]` on job 1 (observer not ready — do not score actor; job 2 = freeze RCA on that plant, not a smoothness N+1); or VALID but hunt not better **and** test_sim R2 still open → one return-to-`test_sim`. Then **stop and report** — do not distillation N+3.
- **Do not launch while P134 is live.**
- **Prerequisite before job 1 (CPU; identity on test_sim; do not land while P134 is live):** scalar `mv_cv_gain_sign` = median of **all** MV→CV SysID repeats. test_sim is 1 pair G≈**−0.30** → hug **hi**. Distillation `control_objective.json` has **no** `cv_side_scale`; `mv_economic` is boilup `mv_1` **0.9** / feed `mv_3` **−1.2**. Nominal `distillation_sim.py` `base_target_coeffs` (reflux/boilup/cooling + separate feed_flow):
  | CV | primary \|K\| excl. feed | K | side |
  | top `cv_0` | reflux `mv_0` | **+0.16** | **lo** |
  | bottom `cv_1` | boilup `mv_1` | **+0.17** | **lo** |
  | pressure `cv_2` | cooling `mv_2` | **−0.30** | **hi** |
  Mixed-sign median of the 12 (incl. feed **−8 / −7 / +10**) is **near 0** (nominal ≈ **−0.04** if the 0.0 reflux→pressure row is dropped). That is **not** reliably “fall through to `'lo'`”: if `|median|>1e-12` (distillation ≈0.04), `_cv_econ_side` returns **one global side for every CV**. `cv_row` is used **only** in the `|g|≤1e-12` `cv_side_scale` fallback — distillation never reaches it. Either failure mode is wrong. Dual path today: train `_refresh_mv_cv_gain_sign` via `_ident_amp_over_delta` (median of every MV repeat) vs val `_episode_mv_cv_gain_sign` (median of repeats) — SISO identity, MIMO one-sign. **Mechanism (one; no TrainConfig / no `DREAMER_*`):** per-CV signed SysID from that CV's **primary MV** (largest |K| among valid MV→this CV **excluding** feed_flow — do not let |K|~8 drown reflux/boilup). Do **not** hug all CVs from boilup-only (would force all **lo**; pressure primary is cooling). Share one helper between train `#12` `side_hi` and val `_cv_econ_side(ep, k)` — **index `k`**. Vector `side_hi` in `_cv_limit_entry_cost` (mean over CVs stays the scalar aux). SISO identity. Files: `training/train.py`; `evaluation/validate.py`; smoke: test_sim side **hi**; 2-CV fixture opposite signs. Land with #14, not a test_sim GPU job. Skogestad: ill-conditioned LV; economic is boilup cost + feed throughput, **not** hug-all-CVs-to-one-bound. Deleted leftover `control_objective_alt.json`.

**Ranked after this visit (2026-09-12T19:14Z — P134 LIVE P3; sibling none):**
1. **Next visit** — P134 LIVE. One jsonl snapshot; do **not** score actor until EXIT val; do **not** wait for the next iter.
2. **#13 `limdwell` P135** — only after a VALID #12 P3 FALSIFY. Out-dir `run_p135_limdwell` / `mbrl2_p135`.
3. **#9 leftover-NIS** — only if #12 **KEEP** smoothness.
4. **#14 `xferdist`** — parked until a VALID smoothness verdict. CPU per-CV y_econ still required before distillation.
5. Critic-scale **regresses** → only then **#8c**.
6. After KEEP smoothness **and** #9 KEEP: R1 TM vs P26 still open; skip **#7** unless lever=**autoencoder**.

**Ranked refactor options (plan; do not implement while GPU busy):**
| Rank | If P134 EXIT is… | Next job | Mechanism | Falsifier |
|---|---|---|---|---|
| A | `[p3-skip]` despite P130 cert (bug) | **stop and report** | cert path failed | smoke already pins P130 true / P132 false |
| B | VALID P3, orbit/rev **KEEP** vs P130/P125 | skip #13; **#9 `dobnis`** | NIS on leftover from `ν.detach()` + live K (P2 only) | TM collapse / pred_std crush / g grads from the term |
| C | VALID P3, orbit/rev **FALSIFY** (not mid-band) | **#13 P135 `limdwell`** | occupancy mask vs entry pulse; actor-only λ | mid-band or paired loses baseline |
| D | identity-retry P133 `best.pt` without cert | **do not launch** | — | P1 band on actor-best already FALSIFIED |
| E | `skip_invalid_p3=0` on this freeze | **do not launch** | — | uncertified P3 is not an actor experiment |
| F | load `wm_last_ok` as the whole ckpt | **do not launch** | — | drops P130 mixed-A actor; two mechanisms |
| G | third full-recipe `limcost` | **do not launch** | — | two misses already |

**Do not:** identity-retry P133 without the cert path, `skip_invalid_p3=0`, identity-relaunch P131/P132, land **#13** or **#14** while P134 is live, reuse `mbrl2_p133` / `run_p133_*`, third `limcost` identity, treat P131/P132/P133 `[p3-skip]` as a #12 smoothness FALSIFY, `--init-from-ckpt` without `--observer-from-ckpt` (retrains observer), load P132 `final.pt` as the freeze, load P130 `wm_last_ok.pt` as the **whole** ckpt (drops the P130 actor), identity-relaunch P130, launch **#8c** after #8b/#10 KEEP critic-scale, identity-relaunch P128/P129, N+1 clipcoef / MC-coef / `p3_mu_ratio_clip` / **B**, require rev≤0.25 to KEEP #10 econ, swap P64/P26, ol1 N+1, persist-coef N+1 (P73), extra-P1 N+1 / lower `gain_ready_lo` / special-case DV / GPU-optimize `phase3_min_frac`, score P2 `dobg`/`dob_K` / leftover, launch `gop`, stack critic knobs, second GPU, sr pin, score P122/P123/P126/P131/P132/P133 loaded-or-expert-BC as an actor family, FALSIFY #1c (it KEPT), N+1 `wm_recon_cv_weight`, drop sticky below `tanh(1)`, launch `gcvres` while lever≠autoencoder, N+1 gain 1.0→>1, raise reward clip as a smoothness lever, GPU-optimize 0.15 / reversal 0.25→0.20, land #9 NIS after FALSIFY smoothness, land #9 as `e=sg(ν)−live ds` (decode leak), put hunt back into `rew_econ`, identity-relaunch P130 to re-score the new gate, α-scale the orbit aux after a FALSIFY, GPU-optimize tracking `iae_normed_mean` (it is 0 when targets are off), launch distillation on the scalar-median y_econ (false `'lo'`), resurrect `control_objective_alt.json`.

## RCA this visit (mode 3 — P133 EXIT; #12c land + P134 LIVE)

- SysID: P133 `run_plan` sr=4 H=**54** τ=**51.5** τ_fast **46**. `[opscale] held identity` **1.143**. Env-free. No `[env-override]`. No `DREAMER_*`. `observer_from_ckpt=true`. Loaded P130 `best.pt` iter **261**. Probe **0.76@MV / 0.91@DV** = P130 val TM **×0.763**, not orig-P1 **0.84**. Val TM **×0.780 / ×0.810** (plot ×0.78/×0.92) agrees with residual_board. P132 val MV **×0.515** stays INVALID.
- Signal: P133 pid **656936 DEAD**. nvidia **0 MiB**. tmux gone. jsonl **0**. EXIT=0 ES `p3_skipped_invalid_observer`. Disk `/home` **67% / 58G**. Not a hard failure. **P134 LIVE** pid **658156** `device=cuda` cert **true** P3 started. Do **not** score LIVE jsonl.
- Control: do **not** score P133 as #12 (P3 never ran). Val is **P130 actor re-eval**: paired **−28.77 vs −181.45**; critic r **+0.476** V **−7.04** vs G **−3.15** slope **0.697**; policy unimodal ~−0.5; DR seed 10004 hunts through hi. Last VALID actor remains **P130**: MV **×0.763** rev **0.388** orbit **0.602** paired **−29.67 vs −134.76** slope **0.608**. Hunt through hi is still open R2; #12 still untested.
- ML: extra-P1 **closed**. `--observer-from-ckpt` KEEP as skip-P1/P2 path. P1-band re-gate of actor-`best.pt` **FALSIFIED**. KEEP `skip_invalid_p3`. Do **not** `=0`. Landed source-cert so a VALID freeze can enter P3. Do **not** load `wm_last_ok` as the whole ckpt (would drop the P130 actor — a second mechanism).
- Plant: Stay on `test_sim`. Envelope intact. #14 parked while P134 trains.
- Metric: trusted = P133 val TM / residual_board / plots (MV SS ×0.78; critic |V|~|G|; DR hunts hi). **On trial (FALSIFIED):** P1 `[0.80,1.30]` as freeze-transfer gate on actor-best; generic `[p3-skip]` “expert-BC” label. **On trial (keep):** P133 det_r **−0.043** as a Kalman score of a P3-less re-val (SysID jitter vs P130 **0.215**).
- Literature: Qin/Badgwell — A/B the regulator on the **source-certified** freeze, not a val-era re-probe of the same g. Ljung: 5-level of loaded g is diagnostic. Hafner P3-on-frozen-WM still the recipe once the certificate is honoured.

Config audit this visit: Step 5 N/A (P133 not an actor EXIT; P3 never ran). Step 4 P134: CLI `--observer-from-ckpt` + `--init-from-ckpt` P130 `best.pt` **plus** source-cert (no TrainConfig default / no `DREAMER_*`); env-free; `skip_invalid_p3=True` KEEP. CONFIRMED `device=cuda` cert **true** enter P3; no `[p3-skip]`; no `[env-override]`. #❌ **0**. #🆕 source-cert helper (CLI path). Champion **P64**. Dist champ **P26**. GPU: P134 `limcert` LIVE.

