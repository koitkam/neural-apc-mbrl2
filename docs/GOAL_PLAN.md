# GOAL_PLAN — neural-APC-mbrl2

Living plan. Update every visit (live analysis and EXIT). Champions live in `docs/RUN_HISTORY.md`. **`RUN_HISTORY` appends at P1-gate / P2→P3 / EXIT only** — not LIVE@iter snapshots.

**Product:** simulator-agnostic neural APC — smooth CV on the economic limit without violating; faithful observer; unmeasured-load rejection. Envelope: learned observer + neural Kalman/DOB + neural actor-critic. No gray-box plant, no PID/LQR/MPC as the product, no DV-only FF.

Plant this cycle: **return-to-`test_sim`**. **P122 `slready` P1-gate CAPPED** then **P2 LIVE** pid **593350** sha `f74a7e8` tmux `mbrl2_p122`. Env-free. `run_plan` **sr=4 H=54** source `dead_dom`. Freeze = **live last_ok 109 unlocked** GAIN_NOT_READY **0.78@MV** / **0.80@DV** `@H[0.80,0.85]` (no detonated-restore: freeze recon **0.0051 = 1.9×** best). last_ok **walked** 85→109 `locked=False` `grl=False` through extra-P1. **Do not score actor** (`skip_invalid_p3`). Next GPU after this pid EXIT: **P123 `opgate` (#1b)**. **P121 `srmed` EXIT `[p3-skip]` INVALID.** Extra-P1 lottery **closed**. **P120 `gop` PARKED.**

## Residual board vs champions (from `validation/residual_board.json`)

| Axis | Score we trust | Champion | P116 test_sim (`best.pt`, VALID) | P121 test_sim (`final.pt`, INVALID) | P119 nonlinear_sim (`best.pt`, VALID) | Status |
|---|---|---|---|---|---|---|
| R1 TM MV ss/@H / curve | val TM + `curve_iae_normed` | P26 ×0.973 / @H ×0.880 | ss **×0.706** / @H **×0.711** curve **0.262**. lever=**autoencoder** | ss **×0.668** / @H **×0.683** curve **0.295**. wm −0.214 vs real −0.320. H=55. lever=**compounding**. Do **not** KEEP opscale | ss **×0.758** / @H **×0.922** curve **0.143**. vs P118 ×0.609 / 0.35 | OPEN vs P26; P121 wrap freeze |
| R1 TM DV ss/@H / curve | same | P64 ×0.893 / @H ×0.962 | ×0.692 / ×0.712 | ×0.849 / ×0.889 curve **0.139** | ×0.942 / ×0.950 curve **0.080** | P116 OPEN; P121 DC-ok-ish on wrap freeze ≠ READY |
| R1 compounding | 1step→OL | P64 ×0.85 | **×0.911** (better than P64) | **×0.634** OL **×0.602** post→1step **×0.901** | **×0.846** OL **×0.783** lever=compounding | test_sim family **closed** as champ; P121 wrap contraction |
| R2 CV smoothness | worst-seed d2≤0.05 AND rev≤0.25 | — | d2 **0.034** PASS; rev **0.421** FAIL. `smooth_pass=False` | d2 **0.038** PASS; rev **0.303** FAIL. expert-BC — **do not score** | d2 **0.053** FAIL; rev **0.395** FAIL. `smooth_pass=False` | OPEN (CV oscillation) |
| R2 headroom / viol | `cv_opt_headroom`, `cv_viol_frac` | — | econ=**hi** (G<0). head **0.183** / viol_frac **0.112**. Not mid-band | econ=**hi**. head **0.418** / viol **0.265**. expert-BC | econ=**lo** (G>0). head **0.116** / viol_frac **0.445** | P116 headroom OK; P119 viol OPEN |
| R3 Kalman | `det_r` + pred_std vs true | P26 det_r 0.68 | **0.676** / **1.48 vs 1.93** | **0.610** / **4.96 vs 1.40**. wrap freeze — **do not credit** | **0.346** / **7.10 vs 3.07** | P116 near champ; P121 freeze miss |
| R3 return-to-limit | `cv_return_headroom` / `cv_return_time_frac` | — | ret_h **0.135** ret_t **0.022** (returns to hi) | ret_h **0.437** ret_t **0.273**. expert-BC | ret_h **0.146** ret_t **0.030** | score only on VALID actor |
| Actor econ | paired vs baseline **and** P64; VALID only | P64 −4.54 vs −94 | **−8.24 vs −109.83** 9/9 VALID | **−33.17 vs −136.98** expert-BC. **do not score** | **−32.11 vs −230.94** 9/9 VALID. `critic_r` FAIL | Do not swap P64 |

P119/P116 boards: R2/R3 return-to-limit from npz (JSON lacked those keys); R3 event IAE from JSON seed-medians (P119 **20.3**, not the npz overwrite 64.36). P121/P120 boards written live at val; TM json agrees with residual_board (P121 MV ×0.668 / curve 0.295; P120 ×0.816 / 0.163). Do **not** treat P121 lineage `wm_gain_healthy=True` (MV rel_err **0.332**) as GAIN-READY.

## Metric audit (trusted vs on trial)

**Trusted:** val TM ss/@H/`curve_iae_normed` (MV and DV); gain decomp; `det_r` + pred_std vs true; paired econ vs baseline **and** champion when freeze GAIN-READY and P3 ran; **CV d2/reversal** (`cv_d2_rms_normed` + `cv_reversal_rate` — this **is** `smooth_pass` as of P120); `cv_opt_headroom` / `cv_viol_frac` with econ side = **sign(SysID MV→CV gain)** (G<0 hug hi / G>0 hug lo — not `cv_side_scale`); DR `cv_return_headroom` / `cv_return_time_frac` on that same bound. Skip-storm 5-level TM is freeze/RCA, not a champ score.

**On trial / do not GPU-optimize:** jsonl teacher ×1 vs SysID median; jsonl `gain_match_mv_ratio` vs local G (teacher pin ≠ GAIN-READY); ss-ratio alone; lineage `wm_gain_pass` / `wm_gain_healthy` (P121 HEALTHY rel_err **0.332** MV / **0.151** DV on a GAIN_NOT_READY freeze; P120 HEALTHY rel_err **0.18**); **mv_reversal as a smoothness gate** (now diagnostic only; `mv_reversal_rate_observed` still logged); **train `mv_reversal_penalty` (`obj_auto_reversal_gain=0.3` default)** — val `smooth_pass` is CV d2/reversal, but the reward still quiets MV chatter the product allows. P116 FAIL was CV rev **0.421** with MV rev **0.235**; quieting MV did not sit a smooth CV on the limit; **`relu(-dCV_t·dCV_prev)` as the #4 retarget** (CPU fixture: P116-class hunting rev **0.331 FAIL** / d2 **0.005 PASS** / amp-relu/rng² **6e-6** vs bang-bang **0.25** — silent on the open residual; sticky-tanh **0.331** matches val). After a VALID observer, retarget to a sticky last-nonzero **sign** surrogate of `_sign_reversal_rate` with thresh **`tanh(1)`** (#4), not the MV amplitude analogue and not `|s|≥0.5`; CV total variation as a smoothness gate; critic_r without `critic_rew_to_tgt_var`; raw dist R²; event IAE without return-to-limit; VALID 9/9 / GAIN-READY / all_pass / family-closed as “residual closed”; beating P64 to KEEP a smoothness/headroom/DR win; **`cv_side_scale` as the economic riding bound** (it is violation urgency; P116 “headroom 0.82 / never returns” was hugging **hi** scored against **lo**); **`wm_op_scale_dev~0` as “linear plant stays identity”** (metric is mean `|1+tanh−1|`; last Linear is zero only at step-0; P122 P1@109 **0.91**; P2@132 **~0.91**; P120 P2 **~0.90**; P121 P2 **~0.83**); **rest-IC Huber `|G|` span ≲ 1.3 as an opscale linear-detector** (wrong scale: P122 test_sim span MV **1.49** / |mean| **2.19** vs P119 nonlinear **2.01** / **2.89** — same class; use SysID `|K|=|amp/Δu|` max/min across valid MV repeats instead: P122 **0.349/0.298=1.170**, P119 **1.858**); **healthy recon / jsonl ×1 as a TM/freeze score** (P122 @85 recon **0.0081** jsonl MV **×1.007** then orig-P1 last_ok **85 = 0.32@MV**); **P122 probe@80 H=1 r=+0.289 / gain_fid=0.863 as a TM/freeze score** (wrap#3 lock@76 unlock@78 — mix-shift, not GAIN-READY); **P122 cap LIVE 0.78@MV as GAIN-READY** (band is [0.80, 1.30]; `skip_invalid_p3` will skip P3); **P121 val TM ×0.668 / DV ×0.849 / curve 0.295 as an opscale KEEP or FALSIFY** (sr matched P116; wrap-locked last_ok **91** 0.61@MV; expert-BC); **P120 val TM ×0.816** same class (`sr=3 H=74`; last_ok **85**); extra-P1 LIVE TM ≠ freeze TM (P120/P121); P122 freeze **is** the cap LIVE probe (last_ok **109**); residual_board event IAE from npz when JSON already has seed-median IAE (P119 board was **64.36** vs summary **20.3** — **fixed**; JSON IAE is the score); **P116 `validation_summary.fidelity_gates.smooth_pass=true`** (old MV dump) vs `residual_board.smooth_pass=false` — residual_board is the score; current `validate.py` already uses CV.

## Closed families (do not N+1)

extra-P1 as freeze (P41; **P117**; **P118**); extra-P1 as orig-P1 PASS restorer (**P121** srmed; **P122** slready CAPPED **0.78@MV**); compounding-teacher Huber (P111 traj unpromoted, P115 k1 REVERT, P116 ol1 REVERT); isolation-off KEEP as default; 2TS-α as champ; kfeat as freeze; quiet_env as freeze; decoder FF; TSSM; GRU keep-h. Extra-P1 stay-lock-if-READY is **KEEP hygiene** (P122 P1-gate: last_ok walked; not a GAIN-READY maker) — do **not** extra-P1 N+1.

## P122 `slready` — P1-gate CAPPED (P2 LIVE; do not score actor / do not land #1b on this pid)

tmux `mbrl2_p122` **alive**. pid **593350**. sha **`f74a7e8`**. Env-free. `[resolved-cfg] opscale=True` `skip_invalid_p3=True` gmatch_len=54. Train start **2026-09-10 06:56:56**. `run_plan` **sr=4 H=54** `sample_rate_source=auto:tau_fast/10_or_dead_dom/2` `dead_fast=7` `tau_fast=46` `dead_dom=8`. locgfix `|du_mv|=0.4000=step`. skip_storm **0**. jsonl NaN/Inf **0**. **Do not kill / rewrite this pid / extra-P1 N+1 / launch `opgate` until EXIT.**

**P1 gates (freeze = live last_ok 109 not READY):**
- **Orig-P1 @86 FAIL** last_ok **85** **0.32@MV** `@H 0.34` DV **0.84/@H 0.81** `not_noisy=False` spread_x**4.8** signflips=1. Live recon **0.0459 = 16.9×** best **0.0027** — measured last_ok, not live detonated g. jsonl MV **×0.994** ≠ TM **0.32**. @85 recon was **0.0081 = 3.0×** (healthy recon ≠ READY). Wrap#3 lock@76 last_ok **76** (48×) **unlocked@78** (`extra_p1=False`; orig-P1 wrap-recovery KEEP). last_ok then walked **85→87@87→98@98→109**.
- **Extra-P1 @98 FAIL** LIVE **0.53@MV** `@H 0.52` DV **0.87/@H 0.82** `not_noisy=True` spread_x**2.5**. recon **0.0087 = 3.2×** (≤5× — LIVE probe). last_ok **98** `locked=False` `grl=False`. Extra-P1 recon@95 **0.0174 = 6.4× <20×** — **no extra-P1 wrap lock** (P121 wrap@95 35× stay-locked last_ok **91**). Distinctive `already_locked + extra_p1 + grl=False` unlock path **unfired** this pid; the walk is the predicted signature.
- **Cap @109 CAPPED (P1→P2):** LIVE **0.78@MV** `@H 0.85` DV **0.80/@H 0.80** spread_x**1.1** signflips=0 `not_noisy=True` draws[0.76,0.78,0.78]. Band [0.80, 1.30] — **0.02 short**. Freeze recon **0.0051 = 1.9×** — detonated-restore **did not fire**. Warm-restore SKIPPED (`wm_best` gain-blind). `[phase] 1→2` **921600**. last_ok **109 unlocked**. Freeze = cap LIVE **0.78**, **not** wrap-locked 85 (**0.32**).

**P2 110+ (in flight, not a score):** first dobg **0.0022@110** skip **0** KEEP vs P95. `dob_A` **0.95257** held. `dob_K` **0.119→0.018@110→0.101@132** SS **≈2.13**. leftover `|d_slow|/|d|` **0.76@110→0.18@132**. `std_ratio` **45495@110 → 0.96@132**. `opdev` **~0.91**. jsonl `wm_frozen=False` is P2 Kalman-on (`wm_freeze_after_p1` off so recon innovation trains K) — g is `requires_grad=False`. Do **not** score actor / dob_K / leftover until EXIT val.

**Judge:** ✅ KEEP #1 as **not-READY extra-P1 freeze-restorer** vs P120/P121 stay-lock of last_ok **85/91**. last_ok walked; freeze is the cap LIVE **0.78** not wrap **0.32**. ❌ **FALSIFIED as orig-P1 PASS / GAIN-READY maker** (CAPPED **0.78@MV**). Extra-P1 lottery still **closed**. **Not** an opscale KEEP/FALSIFY until EXIT val (and this freeze is not READY so actor will be expert-BC). Scenario (c) from the launch spec: **#1b `opgate` after this pid EXIT**. Champion **P64**. Dist champ **P26**.

## P121 `srmed` — EXIT `[p3-skip]` INVALID (do not score actor / do not FALSIFY opscale)

tmux `mbrl2_p121` **gone**. pid **588400 DEAD**. sha **`4422be5`**. **EXIT=0** 168 jsonl iters ES `p3_skipped_invalid_observer`. nvidia **0 MiB**. No `best.pt`. Val `final.pt` expert-BC. `actor_experiment_valid=false`. jsonl NaN/Inf **0**. Env-free. `[resolved-cfg] opscale=True` `skip_invalid_p3=True` gmatch_len=55. Train start **2026-09-10 01:06:11**. `run_plan` **sr=4 H=55** `sample_rate_source=auto:tau_fast/10_or_dead_dom/2` `dead_fast=7` `tau_fast=45` `dead_dom=8`. locgfix `|du_mv|=0.4000=step`. skip_cum **1**. **Do not identity-relaunch.**

**P1 gates (lottery closed; freeze = last_ok 91 not READY):**
- **Orig-P1 @88 FAIL** last_ok **85** **0.65@MV** `@H 0.66` DV **0.79/@H 0.79** `not_noisy=True`. Live recon **0.0199 > 5×** best **0.0026** — measured last_ok, not wrap g. Wrap lock@86 (27×) **unlocked@87** (`extra_p1=False`; orig-P1 wrap-recovery KEEP). last_ok then walked **85→90@90→91@91**.
- **Extra-P1 @101 FAIL** LIVE **0.66@MV** `@H 0.71` DV **0.84/@H 0.87** `not_noisy=True`. Wrap lock@95 last_ok **91** (35×); recon recovered **0.0146@96 / 0.0102@101 / 0.0063@111** all **<20×**. jsonl `p1_last_ok_locked=True` `gain_ready_locked=False` **95→111** — P40 extra-P1 stay-lock of a **not-READY** snapshot (P120-class).
- **Cap @111 CAPPED** LIVE **0.68@MV** `@H 0.77` DV **0.71/@H 0.72**. Detonated-freeze restore **91**; re-probe **0.61@MV** `@H 0.60` DV **0.79/@H 0.80** `signflips=1` still not READY. Warm-restore SKIPPED (`wm_best` @110 gain-blind). `[phase] 1→2` **950380**.

**P2 112–168 then `[p3-skip]`:** first dobg **0.0084@112** skip **0** KEEP vs P95. `dob_A` **0.95257** held. `dob_K` **0.017@112→0.121@168** SS **≈2.56**. leftover `|d_slow|/|d|` **0.74@112→0.20@168**. `std_ratio` **38@112 → 0.95@168**. `opdev` **~0.83**. P2→P3@**168 PASS** MTP **1.072** then **`[p3-skip]`**. last_ok **91** locked not-READY through EXIT. End-P2 recon **0.123** (Kalman-on / g frozen — do not score).

**Val (do not score actor):** MV TM ss/@H **×0.668 / ×0.683** curve **0.295** (wm −0.214 vs real −0.320; H=**55**). DV **×0.849 / ×0.889** curve **0.139**. Lineage `wm_gain_pass` HEALTHY (rel_err **0.332 / 0.151**) / observer-gain HEALTHY ≠ GAIN-READY. Decomp MV real→post **×0.979** post→1step **×0.901** 1step→OL **×0.634** OL-vs-real **×0.602** lever=**compounding**. DV real→post **×0.947** post→1step **×0.824** lever=prior. det_r **0.610** pred_std **4.96 vs 1.40**. Event IAE **28.4**. R2 d2 **0.038** PASS rev **0.303** FAIL `smooth_pass=False` head **0.418** viol_frac **0.265** econ=**hi**. `critic_r=nan`. Paired **−33.17 vs −136.98** 9/9 expert-BC. TM json agrees with residual_board.

**Judge:** `[p3-skip]` on wrap-locked last_ok **91** is **P40 extra-P1 stay-lock of not-READY + orig-P1 miss**, not an opscale verdict. sr=**4** H=**55** matched P116 — srmed **sampling KEEP**; srmed **FALSIFIED as orig-P1 PASS restorer** (P116 @87 **0.84@DV** last_ok **78**). Val MV **×0.668** is **worse** than P116 ×0.706 and P120 ×0.816 — wrap freeze, **not** an opscale FALSIFY (INVALID). Extra-P1 lottery **closed**. Stay-lock of not-READY **GPU-confirmed** (P120+P121). Next GPU **P122 `slready`**. Champion **P64**. Dist champ **P26**.

## P120 `tshome` — EXIT `[p3-skip]` INVALID (do not score actor / do not FALSIFY opscale)

tmux `mbrl2_p120` **gone**. pid **583595 DEAD**. sha **`03a93f1`**. **EXIT=0** 165 jsonl iters ES `p3_skipped_invalid_observer`. nvidia **0 MiB**. No `best.pt`. Val `final.pt` expert-BC. `actor_experiment_valid=false`. jsonl NaN/Inf **0**. Env-free. `[resolved-cfg] opscale=True` no ol1. Train start **2026-09-09 19:10:17**. `run_plan` **sr=3 H=74** `dead_dom=8` `dead_fast=6` `tau_fast=47`. locgfix `|du_mv|=step`. **Do not identity-relaunch.**

**P1 gates (lottery closed; freeze = last_ok 85 not READY):**
- **Orig-P1 @86 FAIL** last_ok **85** **0.70@MV** `@H 0.71` DV **1.00/@H 1.03** `not_noisy=True`. Wrap #3 live recon **0.0818 = 27×**.
- **Extra-P1 @98 FAIL** LIVE **0.43@MV** (`not_noisy=False`; recon 3.58×). last_ok **still 85** stay-locked (`gain_ready_locked=False`).
- **Cap @109 CAPPED** last_ok **85** **0.75@MV**; restore 85 re-probe **0.80@MV** `@H 0.84` DV **1.01/@H 1.04** still not READY. Warm-restore SKIPPED (`wm_best` @140 gain-blind). `[phase] 1→2` **943410**.

**P2 110–165 then `[p3-skip]`:** first dobg **0.0384** skip **0** KEEP vs P95. `dob_A` **0.95257** held. `dob_K` **0.018@110→0.056@165** SS **≈1.19**. leftover **0.77@110→0.55@165**. `std_ratio` **1.69e5@110 → 0.67@165**. α **0.00391**. `opdev` **~0.90**. P2→P3@**165 PASS** MTP **0.912** then **`[p3-skip]`**. last_ok **85** locked not-READY through EXIT.

**Val (do not score actor):** MV TM ss/@H **×0.816 / ×0.852** curve **0.163** (wm −0.261 vs real −0.320; H=**74**). DV **×1.011 / ×1.060** curve **0.083**. Lineage `wm_gain_pass` HEALTHY (rel_err **0.184 / 0.011**) / observer-gain HEALTHY ≠ GAIN-READY. Decomp MV real→post **×0.958** post→1step **×0.584** 1step→OL **×1.095** OL-vs-real **×0.779** lever=**free_bits**. DV real→post **×1.125** post→1step **×0.441** lever=prior. det_r **0.237** pred_std **1.49 vs 1.33**. Event IAE **23.2**. R2 d2 **0.034** PASS rev **0.353** FAIL `smooth_pass=False` head **0.234** viol_frac **0.263** econ=**hi**. `critic_r=nan`. Paired **−31.63 vs −159.86** 9/9 expert-BC. TM json agrees with residual_board.

**Judge:** `[p3-skip]` on wrap-locked last_ok **85** is **P40 stay-lock + orig-P1 miss + sr=3**, not an opscale verdict. Val TM did **not** collapse vs P116 ×0.706 (ss **better**, different H) — still **not** an opscale KEEP. extra-P1 lottery closed. Next was **P121 `srmed`** (now EXIT `[p3-skip]`). Stay-lock-only-if-READY is **P122**. Champion **P64**. Dist champ **P26**.

## P118 EXIT — `opscale` INVALID observer (do not score / do not FALSIFY opscale)

tmux `mbrl2_p118` **gone**. pid **551549 DEAD**. sha **`ff5f84c`**. **EXIT=0** 166 jsonl iters ES `p3_skipped_invalid_observer`. nvidia **0 MiB**. No `best.pt`. Val `final.pt` expert-BC. `actor_experiment_valid=false`. jsonl NaN/Inf = **one** `wm_grad_norm=inf@13`. Env-free. `[resolved-cfg] opscale=True` no ol1. Local G MV **0.0153** vs ident **2.627** (engineering `_prev_control` Δu). **Do not identity-relaunch.**

**P1 gates:** orig-P1@**87 FAIL** last_ok **85** **0.34@MV**; extra-P1@**98 FAIL** live **0.11@MV**; cap@**110** last_ok **108** **GAIN_NOT_READY 0.68@MV / 0.85@DV** `@H[0.77,0.79]` `not_noisy=True`. Detonated-freeze restored 108. Warm-restore SKIPPED (`wm_best` @100 gain-blind). Graph released.

**P2 111–166:** first dobg **0.0209** skip **0** KEEP vs P95. `dob_A` **0.95257** held. `dob_K` **0.119→0.017@111→0.0018@117** min **0.0016@119** then recovered **0.047@166** SS **≈1.00**. leftover `|d_slow|/|d|` **0.80@111→1.53@117→0.416@166**. `std_ratio` **144389@111 → 0.77@166**. α **0.00391**. `wm_op_scale_dev` **~0.93**. P2→P3@**166 PASS** MTP **1.216** then **`[p3-skip]`**.

**Val (do not score actor):** MV TM ss/@H **×0.609 / ×0.705** curve IAE/\|real\| **0.35**. DV **×0.799 / ×0.703** curve **0.24**. Lineage `wm_gain_pass` PASS (rel_err 0.39 / 0.20) / `wm_gain_healthy=False` / observer-gain PASS ≠ GAIN-READY. Decomp MV real→post **×0.882** post→1step **×0.846** 1step→OL **×1.045** OL-vs-real **×0.607** lever=autoencoder. DV real→post **×0.868** post→1step **×0.827** lever=autoencoder. det_r **0.343** pred_std **3.00 vs 3.07**. Event IAE median-of-medians **29.2**. `critic_r=nan`. `all_pass=False` (`critic_pass`). Paired **−38.39 vs −212.28** 9/9 expert-BC.

**Judge:** locgfix **already on HEAD** (`414eca4`). **Not** an opscale FALSIFY. extra-P1 lottery closed. Next was **P119 `locgfix`** (now EXIT VALID). Champion **P64**. Dist champ **P26**.

## P119 EXIT — `locgfix` VALID (do not identity-relaunch / do not launch `gop`)

tmux `mbrl2_p119` **gone**. pid **557257 DEAD**. sha **`ea7def9`**. **EXIT=0** 406 jsonl iters ES `p3_plateau` (no >+1.0% over `best_det_return=−131.981` for 200 iters) best **206**. env_steps **1645000**. nvidia **0 MiB**. `best.pt` + `final.pt`. Val ckpt **`best.pt`**. `actor_experiment_valid=true`. jsonl NaN/Inf **0**. Env-free (`CUDA_VISIBLE_DEVICES=0`, no `DREAMER_*`). `[resolved-cfg] opscale=True` no ol1. Train start **2026-09-09 14:10:33**. **`[p3-skip]` did not fire.** P3 **159–406** (248 iters) skip_iter **0** (cum skip **11** is P1 storm-1). Critic-warmup 159–168; `[return-scale] FREEZE` **2.128** @169; unfreeze @169. @406 actor **+0.116** critic **9.51** ent **−0.467** logp **0.54** clip **0.10** rscale **2.13** rtgt **0.00067** `critic_pred_target_r` **0.991**. **Do not identity-relaunch.** **Do not launch P120 `gop`** (val TM **×0.758**, not ~P118 ×0.61). Next was **P120 `tshome`** (now EXIT `[p3-skip]`).

**Teacher print (locgfix CONFIRMED):** local G `plant_fd=6/6` mean MV **2.890** / DV **−0.341** vs identified **2.709 / −0.435**. span MV **2.01** / DV **0.46**. `|du_mv|=0.4000` **=** step **0.4000**. **No** WARNING. jsonl MV O(1) since ~@5.

**Orig-P1 @91 FAIL (fair locgfix freeze miss):** live g **0.63@MV** `@H 0.61` DV **1.03/@H 0.98**. jsonl MV **×0.991** ≠ TM. extra-P1 lottery **closed**.

**Extra-P1 @101 PASS (not a freeze KEEP):** last_ok **100** GAIN-READY **0.83@MV** `@H 0.98` DV **0.92/@H 0.95** `compound_ok` 1step→OL **0.86**. Detonated-freeze restore **100**; re-probe **0.85@MV** 1step→OL **0.90**. Warm-restore SKIPPED. `[phase] 1→2` env_steps **908750**. P100-class confound.

**P2→P3 @158 PASS:** MTP median **1.180** (max=3.00 n=5) `p2_extension=0/42897`. env_steps **1276250**. P2 skip **0**. End-P2 recon **0.0527** dobg **0.0319** `dob_A` **0.95257** `dob_K` **0.167** SS **≈3.52** leftover **0.159** std **0.885**. First dobg **0.0034@102** skip **0** KEEP vs P95. vs P118 leftover **0.416** K **0.047/1.00** MTP **1.216** then `[p3-skip]`.

**Val (score actor — VALID):** MV TM ss/@H **×0.758 / ×0.922** curve IAE/|real| **0.153** (wm +0.693 vs real +0.914). DV **×0.942 / ×0.950** curve **0.080** (wm −0.390 vs real −0.414). Lineage MV rel_err **0.24** HEALTHY; DV **0.06** HEALTHY; `wm_observer_gain` HEALTHY. Decomp MV real→post **×0.960** post→1step **×0.962** 1step→OL **×0.846** OL-vs-real **×0.783** lever=**compounding**. DV real→post **×0.895** post→1step **×1.096** lever=autoencoder. det_r **0.346** R²_det **−4.27** pred_std **7.10 vs true 3.07**. Event IAE median-of-medians **20.3** (P118 **29.2**). Paired **−32.11 vs −230.94** **9/9 WIN**. cum_raw **−55585** (36). `critic_r` **+0.253** FAIL (<0.3) → `all_pass=False`. `smooth_pass=True` (mv_reversal **0.171** all-eps / **0.196** DR). hugging **0.384** / **0.316** DR; cv_viol **16.7**; mv_viol **2.79**. No `cv_d2_rms_normed` / `cv_opt_headroom` / `cv_viol_frac`. Val “[val] P3: 11 grad-clip skips” is **cumulative** P1 storm, not P3 this-iter.

**Judge:** ✅ KEEP locgfix as **teacher-unit fix** vs P118 0.015 / val ×0.609. ✅ KEEP as **P3-entry** vs P118 `[p3-skip]`. ❌ extra-P1 as freeze KEEP (orig-P1 **0.63@MV** is the fair miss; freeze last_ok **100** 0.83@MV transferred to val ×0.758, not P109-class collapse). ❌ **FALSIFIED as TM-to-P26** (×0.758 vs ×0.973). Remaining R1 hole is **compounding**. ❌ **FALSIFIED as Kalman amp** (pred_std 7.10 vs 3.07; det_r ~P118 0.343). Actor **VALID** vs baseline; **do not swap P64** (different plant). **P120 `gop` PARKED** — trigger was val TM still ~×0.61; it is ×0.758. Cadence: fair plant verdict done. GPU **free**. Do **not** extra-P1 N+1 / identity-relaunch P117/P118/P119 / rewrite locgfix / in-place `c*=gop`.

## Ranked follow-ups

**#1 P122 `slready` — Extra-P1 stay-lock only if last_ok is GAIN-READY (P1-gate KEEP hygiene; FALSIFIED as READY-maker).** Align extra-P1 wrap stay-lock with skip-storm P92. **P120 + P121** stay-locked not-READY last_ok **85/91**. **This pid GPU-confirmed the walk:** last_ok **85→109** `locked=False` `grl=False` through extra-P1@98 LIVE **0.53@MV** and cap@109 LIVE **0.78@MV**. Extra-P1 recon@95 **6.4× <20×** — no extra-P1 wrap lock, so the `already_locked + extra_p1 + grl=False` unlock branch **did not fire**; orig-P1 wrap#1–#3 unlock already KEEP. Freeze is cap LIVE **0.78**, not wrap **0.32**. Do **not** identity-relaunch P121. Do **not** rewrite this pid. Do **not** extra-P1 N+1.
- **After EXIT of this pid:** hygiene-delete `extra_p1=` on the `already_locked` path (`gain_ready_locked` already decides). Bundle with #1b land, **not** a second GPU knob. P40 READY overwrite still stay-locks via `gain_ready_locked`.
- **Falsifier (READY overwrite):** still untested on GPU this pid (never GAIN-READY). CPU smoke **PASSED**. Cap 0.78 is **not** that falsifier.

**#1b P123 `opgate` — NEXT GPU after P122 EXIT (CAPPED after extra-P1 walk still <0.8).** P116 orig-P1 **PASS 0.84@DV** had **no** `opscale=` banner (pre-P118). P118+ env-free `opscale=True` on test_sim; P120/P121 orig-P1 **0.70/0.65@MV** then wrap-locked not-READY (INVALID — not an opscale verdict). This pid walked last_ok and still CAPPED **0.78@MV** with `opdev` **0.91** (left identity). LPV `scale=1+tanh(MLP(OP))` on group `g` is extra OP-scheduled gain on a single-K FOPDT — jsonl teacher still ×1 while gate TM shorts. **Do not** land on the P122 pid.
- **Mechanism:** freeze `op_scale_net` at identity when SysID per-channel `|K|=|amp/Δu|` **max/min** across valid MV repeats ≤ **1.3** (unitless). P122 MV **8** valid `|K|` **0.298–0.349** span **1.170** → hold. P119 nonlinear MV **0.706–1.312** span **1.858** → opscale **stays**. Reuse `_resolve_gain_match_targets` `amp/delta` loop (`train.py` ~L9265), do **not** a second parser / rest-IC Huber span. No new TrainConfig name. P119 locgfix+opscale KEEP on the OP-varying plant.
- **Group-`g` hole (must pin, not just init):** `op_scale_net` has no `dob_` prefix so `DreamerV4.set_world_model_trainable` L1903–1907 treats it as group **`g`**. P1 calls `g=True` **before seed fill** (L13195) and again at phase boundaries. Smoke `_test_p118_opscale` L7043 **asserts** `g=True` → all `op_scale_net` params `requires_grad=True`. Freezing only inside `init_op_scale_net` is **undone at P1 start**. Land: set `dynamics.op_scale_identity_held=True` from plant_id **after** ident write, then at the **end** of `set_world_model_trainable` if that flag: `requires_grad_(False)` on `op_scale_net`. Net stays so serve is `1+tanh(0)≡1`.
- **Predicted signature:** orig-P1 last_ok in `[0.8,1.3]` (P116-class); `opdev` stays ~0 after iter 1 (this pid **0.91@109** already left identity — on trial); banner `opscale=held` not a lying `True`; `[resolved-cfg]` shows the gate. If VALID: score TM vs P116 ×0.706 (fair A/B: P116 had no opscale).
- **Falsifier:** orig-P1 still <0.8 with identity scale → opscale was **not** the miss (then freeze-gate/piece, **not** opscale N+1). VALID TM **worse** than P116 ×0.706 → identity-scale hurt TM — KEEP opscale. Do **not** extra-P1 N+1 / `gop` / sr pin. Smoke must fail if `g=True` re-enables a held net; span 1.86 still trains.
- **Files:** `models/dreamer_v4.py` `set_world_model_trainable` (pin **after** the g/dob loop ~L1922). `models/dreamer_v4_rssm.py` `init_op_scale_net` (net still exists). `training/train.py` `_resolve_gain_match_targets` / after plant_id: compute MV `|K|` max/min, set `op_scale_identity_held`; `_print_resolved_cfg` L4521 currently **hardcodes** `opscale=True` — print `held`/`True`. Smoke: `_test_p118_opscale` — identity-held survives `g=True`. Env-free. **Do not** land on the P122 pid.

**#2 P121 `srmed` sampling — KEEP; freeze-restorer FALSIFIED.** `run_plan` sr=**4** H=**55** source `dead_dom` CONFIRMED. Orig-P1@88 FAIL **0.65@MV** last_ok **85** (wrap-adjacent 5×). EXIT val MV **×0.668**. Do **not** N+1 the Nyquist formula. Do **not** pin `DREAMER_SAMPLE_RATE`. Lookback `derive_sample_rate_for_lookback` KEEP.

**#3 After a VALID observer on this plant:** score opscale vs P116 R1 TM (autoencoder ×0.706; P116 had **no** opscale). If TM << P116 with sr matched → **#1b** SysID `|K|` max/min gate (not rest-IC span). If TM ≥ P116 → KEEP opscale as default even on linear. **Do not** identity-regularizer λ lottery. P121 EXIT `[p3-skip]` is **not** that verdict. P122 P1-gate CAPPED **0.78@MV** / `opdev` **0.91** is **not** that verdict (not READY; actor will be expert-BC). #1b is the next GPU **because** this pid CAPPED after the walk.

**#4 R2 CV reversal (after a VALID observer on this plant — GAIN-READY, not expert-BC).** One mechanism: retarget the existing reversal term from MV chatter to the **val gate**. Today `obj_auto_reversal_gain=0.3` builds `mv_reversal_weights` and `objective_runtime` penalises `relu(-du_t·du_prev)`. Val `smooth_pass` is `_sign_reversal_rate` on CV (nonzero-Δ sign flips / steps, deadband **1e-3·bound width**; skips holds). P116 VALID FAIL is CV rev **0.421** (d2 **0.034** PASS) hugging hi — frequent **small** hunting, not large bang-bang. CPU `tools/_test_cv_reversal_surrogate.py`: hunting rev **0.331 FAIL** / d2 **0.005 PASS** / `relu(-dCV·dCV_prev)/rng²` **6e-6** vs bang-bang **0.25**; sticky-tanh mean **≈ val rev** on hunting and **0** on a monotonic ride. Copying the MV amplitude formula onto CV would not close R2 (gamed: d2 already PASS).
- **Mechanism:** zero `mv_reversal_weights`. Per-step CV penalty = `relu(-s_t·s_sticky)` where `s=tanh(dCV/(deadband))` (same 1e-3·width as val) and `s_sticky` is the last `|s|≥tanh(1)` **sign** (`tanh(1)` ⇔ `|d|>deadband`; `|s|≥0.5` would penalize sub-deadband holds val ignores). A hold then opposite move still counts. Scale as a fraction of `cv_base`, hard-cap at `cv_base`. Same `_knob_float` field `obj_auto_reversal_gain`; leftover `OBJ_AUTO_REVERSAL_GAIN` **delete** when touching (`_knob_float` already ignores leftover_key). No new TrainConfig name. Soft tanh is required: hard `sign` has no gradient.
- **Predicted signature:** worst-seed `cv_reversal_rate` ≤ 0.25 with d2 still ≤ 0.05; `cv_opt_headroom` not worse; `cv_return_headroom` / `cv_return_time_frac` not slower (monotonic ride to the limit is one sign, not a flip storm). MV reversal may rise (allowed).
- **Falsifier:** mid-band sit or slow DR return; or rate still FAIL because tanh saturates only on large dCV (then raise sticky threshold toward `tanh(1)`, do **not** fall back to amplitude relu). Do **not** stack critic knobs / rtgt / MV-relu N+1 / d2 loss (already PASS).
- **Files:** `utils/auto_weights.py` `reversal_weights` (~L847–893) → zeros (keep `obj_auto_reversal_gain` as the CV-scale knob). `utils/objective_runtime.py` (~L679–702) replace `relu(-du_t·du_prev)` with `relu(-s_t·s_sticky)` on CV; need prev CV + sticky sign in the step cache (today only `prev_control` / `prev_prev_control`). `tools/_test_cv_reversal_surrogate.py`, `tools/_test_mv_reversal.py` (MV penalty → 0). Env-free. **Do not** land on the P122 pid.

**#5 P120 `gop` (PARKED)** — **do not launch**. P119 val TM **×0.758** is not ~×0.61. P73 persist forbids in-place `c*=gop`.

**#6 After a later plant has a real fast DV:** min of *per-channel* median θ, not global median of every repeat. Do **not** launch this while test_sim channel medians are all 8.

**Do not:** extra-P1 N+1, ol1 N+1, identity-relaunch P117/P118/P119/P120/P121, launch `gop` / `#1b opgate` while P122 is LIVE, stack critic knobs, second GPU, sr pin as engineering default, score P2 `dob_K` / actor before EXIT val.

## RCA this visit

- SysID: test_sim **θ_dom=8**. P122 `dead_fast=7` `tau_fast=46` → sr=4 H=**54** (source `dead_dom`). locgfix `|du_mv|=step`. SysID MV `|amp/Δu|` max/min **1.170** (8 valid; |K| **0.298–0.349**); DV span **1.206**. P119 **1.858**. Rest-IC Huber span does **not** split them. Buffer lap ≈65.6 iters — wrap#3 @77 is second FIFO mix, not LPV.
- Signal: P122 P1-gate CAPPED pid **593350** sha `f74a7e8`. Orig-P1@86 last_ok **85 = 0.32@MV** (live recon 16.9×). Extra-P1@98 LIVE **0.53@MV** last_ok walked. Cap@109 LIVE **0.78@MV** / **0.80@DV** last_ok **109 unlocked**. Extra-P1 recon@95 **6.4×** — no wrap lock. jsonl NaN/Inf **0**. Disk `/home` **65% / 61G**. Keep P122+P121+P120+P119+P118+P117+P116+P64+P53. P2 first dobg **0.0022** skip **0**; leftover **0.76→0.18@132**; K **0.018→0.101**. Heartbeat ~30 s/iter in P2 (rest-ic graph released; nvidia **~4.2 GB** vs P1 **14.7 GB**).
- Control: do not score actor (freeze not READY; `skip_invalid_p3`). R2 still open on P116 (CV rev 0.421, d2 PASS). DCS `mv_rate_limit` is an actuator constraint (KEEP); train MV-relu is the wrong residual.
- ML: jsonl MV ×1 at orig-P1 ≠ TM **0.32**. `opdev` **0.91@109** already left identity (on trial). `wm_best` gain-blind (warm-restore SKIPPED). #1 KEEP as freeze-restorer (walk vs P121 lock); **FALSIFIED as READY-maker**. Extra-P1 `already_locked` unlock **unfired** (never locked in extra-P1). #1b freeze-at-init is **not** enough: `set_world_model_trainable(g=True)` re-enables `op_scale_net` (group `g`; smoke asserts it).
- Plant: env-free HEAD. opscale ON vs P116 still unscored until a GAIN-READY EXIT at matched sr. P116 had no opscale and orig-P1 **PASS**. This pid CAPPED after a walked last_ok — cleaner opscale signal than P120/P121 wrap freezes, but **still not READY**, so EXIT val will be expert-BC. Next GPU is identity-hold `#1b`, not another extra-P1.
- Metric: do not GPU-optimize jsonl ×1 / `opdev` / rest-IC span-as-linear / MV reversal gate / amplitude-weighted CV relu / `|s|≥0.5` as val-deadband / healthy-recon-as-TM / lineage HEALTHY as freeze score / wrap-lock LIVE TM / cap 0.78 as GAIN-READY. Trusted R2 is `_sign_reversal_rate` + d2 + headroom on `econ_side=sign(G)`. Sticky thresh **`tanh(1)`**. Freeze score is last_ok re-probe / val TM, not jsonl ×1. Opscale gate = SysID `|K|` max/min, not rest-IC span. First-fill and second-lap wrap are replay-buffer mix-shift, not an LPV signature.
- Literature (docs/papers PDFs not on host; HTTPS blocked): LPV input scaling is for OP-varying DC (equal-% / 1/feed; Qin/Badgwell APC, Seborg). A single-K FOPDT does not need it; extra `1+tanh` DoF can steal DC-gain (gain↔disturbance class). Stage-1 curriculum puts that MLP in group `g` — identity-hold must survive `g=True`, else the gate is a no-op. Economic APC allows MV move. Sign-flip **rate** (Shinskey hunting) is not amplitude-weighted chatter. Stay-lock-if-READY is P92 applied to extra-P1 — KEEP as hygiene, not as a READY-maker. FIFO wrap unlock when recon <20× is the correct SysID response (do not freeze a wrap-contaminated snapshot unless that snapshot was GAIN-READY).

Config audit this visit = P1-gate (no Step 5). #❌ **0**. #🆕 **0**. Step 4 (P122 LAUNCH) still holds: env-free HEAD vs P121; no new TrainConfig / no `DREAMER_*`; only `_should_lock_last_ok` extra-P1 READY gate. Train start `device=cuda` bs=128 eager skip_invalid_p3. `run_plan` sr=**4** H=**54** source `dead_dom`. No `[env-override]`. Hardcoded `opscale=True` banner + group-`g` re-enable of a held net are **#1b land**, not this pid. Do not rewrite this pid.

P117 process died mid-P2 (SIGKILL). P118 completed P2 then `[p3-skip]`. P119 completed P2 then **P3 then EXIT VALID**. P120 completed P2 then **`[p3-skip]`**. P121 completed P2 then **`[p3-skip]`**. P122 P1-gate CAPPED, P2 LIVE. Do not identity-relaunch P117–P121.

